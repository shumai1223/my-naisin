# S3 / S4 / S6 — 学校ページ層・内部リンク孤児・E-E-A-Tの施策発散

- 作成: 2026-08-10 / 対象群: S3（学校ページ層）・S4（内部リンク・孤児）・S6（E-E-A-T）
- 係数は全て `ops/COEFFICIENTS.md` から引用（再導出しない）。人間クリック下限=17／上限=46（28日・D1ローリング・取得日2026-08-10）、遷移率下限0.198%／上限0.537%、CTR曲線は新曲線（1位46.4%…）、表示分母215,748（実URLのみ）。**下流は必ず下限を使う**（COEFFICIENTS §7規約）。
- `ops/BAR.md`（学校ページ差別化因子・B-4必須ゲート）・`ops/DISTANCE.md`（収益距離・施策A〜D）・`ops/MONEY.md`（出血5・a4・a8）・`ops/CORRECTIONS.md`（C-1・C-6a・C-6b）を土台にし、file:line は本書執筆時点（2026-08-10）で再読了して確認済み。**BAR §5-1が使った遷移率0.455%はCOEFFICIENTS §2が不採用にした値なので、本書でBARの計算を再利用する箇所は必ずCOEFFICIENTS採用値（0.198%〜0.537%）で計算し直す**（S3-2で実施）。
- 本書は調査・提案のみ。`src/` は1行も変更していない。デプロイ・env変更・本番D1書き込み・送信は一切行っていない。
- **既存ページの改善は「新規公開」ではない**——学校ページ3,089枚は47/47県すでにindex解禁済み（wave7・2026-08-09完了、`src/lib/school-page-lookup.ts:50-98` の `INDEXED_SCHOOL_PAGE_PREFECTURE_CODES` に全47県が実在することを確認済み）。本書のS3案は全てこの**既存**ページ群への追記・修正であり、「1波5県・2〜3日おき」の新規公開ルールの対象外。各案の該当箇所で明示する。
- 全12案、**うち自社にしか作れないもの6案**（S3-1・S3-4・S3-5・S6-1・S6-2・S6-3）。

---

## S3: 学校ページ層（5案）

### S3-1: 学校固有の多年度（募集人員・応募者数・倍率）を新設セクションで描画する

| 項目 | 内容 |
|---|---|
| **ID** | S3-1 |
| **施策名** | 学校ページに、その学校固有の過去年度分の募集人員・応募者数・倍率を新設テーブルで表示する |
| **現状の実測** | `src/data/competition-rates/*.ts` 47ファイルは学校別倍率レコード**17,142件**（うち過年度**11,690件**）を保有するが、`src/lib/school-page-lookup.ts:109`（本書で再読了・確認済み。BAR/task起動時点の「:106」から数行ズレている＝コメント追記によるドリフト。line番号は必ず実ファイルを見て引用すること）の `const currentYearRecords = rates.records.filter((r) => !r.fiscalYear);` が過年度分を丸ごと捨て、`src/app/pref/[code]/school/[schoolCode]/page.tsx:122-139` の「今季の入試倍率」セクションは常に1年度分のみを描画する。実例（千葉県立一宮商業高等学校・`chiba.ts:233,411,618`）: 令和8年度 quota120/applicants107/rate0.89、令和7年度120/122/1.02、令和6年度160/132/0.83——**3年度とも進研ゼミの掲載値（`czemi.html`）と完全一致**（`ops/BAR.md` §2-4）。3年度以上を保有するのは**42/47県**（hokkaido/nara/niigata/oita/sagaの5県は2年度のみ・`ops/CORRECTIONS.md` C-6a） |
| **変更内容** | **`school-page-lookup.ts:109`のフィルタ自体は削除しない**——この行は「今季の入試倍率」の1年分の合計（現行UX）に必要なロジックであり、単純に外すと2026-08-09に一度発生・修正済みの事故（同一学科の複数年度レコードが`departmentRates`に混入し`totalQuota`等が無意味な合算値になった。commit `b51d296`・`school-page-lookup.ts:31-36`のwave6コメントに経緯記録済み）と**同じクラスのバグを再現する**。正しい設計は別経路を新設すること: ①`school-page-data.ts`に新規純粋関数`buildSchoolYearHistory()`を追加。`buildSchoolPageDataForPrefecture()`が既に確立した`matchByName`（学校名→schoolCode突合）を再利用し、**未フィルタの`rates.records`**を学校名→`fiscalYear`（未指定は`sources[0].fiscalYear`扱い、`competition-rate.ts:31-35`のコメント通り）でグループ化、年度ごとに`quota`/`finalApplicants`/`rate`を合算する（`buildSchoolPageDataForPrefecture:89-92`と同じ合算ロジックを年度別に適用するだけ）。②`school-page-lookup.ts`に`getPrefectureSchoolYearHistory(code)`を新設し、この未フィルタ経路をエクスポート。③`page.tsx`の122-139（現行の単年度ブロック）はそのまま残し、その**直後**（141行目の`SchoolPageConvertCTA`より前）に新規テーブルを追加する——`SchoolPageConvertCTA`(141)/`SchoolPageParentBridge`(143)の**相対順序を一切変えない**（G7参照）。既存の`categoryTrends`（県全体の傾向・173-213行）は削除せず残す（学校固有×県全体の2軸は補完関係であり、これは`src/lib/competition-rate-history.ts:1-13`のコメントが「学校粒度の推移が必要になった場合はY-2と同じ設計を年度別に拡張する形で別途対応する（本モジュールのスコープ外）」と明示的に将来課題として書き残していた項目そのもの） |
| **EV(円/月)** | **円は出せない**。理由: 多年度データの掲載が検索順位・CTRへ与える因果は自社で未検証（`ops/BAR.md` V-5「HighSchool JSON-LD の順位影響は未検証」・V-6「短縮形の効果は未測定」と同型の未検証項目）。BAR §5-2シナリオ1〜3は「バーを超えた場合の理論上限」を試算しているが、それは短縮形(S3-3)・季節(自社未実測)・CTR曲線どおりの実現(未検証)を同時に仮定した複合シナリオであり、本施策単体の寄与を分離できない。価値は差-1・差-4（`ops/BAR.md` §3）の**必須ゲートB-4「倍率が3年度以上」を42/47県で即座に満たす**こと（現状は必須ゲート4/5・B-4のみ落第・`ops/BAR.md:353`）であり、円ではなく被リンク獲得・データライセンス文脈（TIER X/Ω系・👤判断領域）に効く |
| **工数(h)** | **8h**（新規純粋関数2h＋lookup新設1h＋新規UIコンポーネント2h＋page.tsx組込0.5h＋回帰テスト2h〔chiba一宮商業の3年度実値120/107/0.89・120/122/1.02・160/132/0.83で検証＋既存の単年度hero統計が新経路の影響を受けないことを保証する不変条件テストを追加＝2026-08-09事故の再発防止〕＋42県スポットチェック0.5h） |
| **EV/h** | 出せない |
| **効き始める時期** | 8/31までに着手すべき（最も工数が大きい単一施策のため早期着手が必要）。**構造変更に該当しうるため11/15フリーズ前に完了必須** |
| **リスク(C8)** | 低。捏造ゼロ（既存の教委一次データをそのまま多年度描画するだけ・独自推定なし＝Y-0適合）。3,089枚に同一テンプレートの新セクションを追加する点は`ops/THREATS.md`脅威5(TH-3)「scaled content abuse」の一般監視対象に含まれる（新規リスクではなく既存監視の延長・GSC「手動による対策」ページの確認は👤未実施のまま=P-4を参照） |
| **人間ゲート(C7)** | 不要（loop実装可・デプロイのみ👤）。データはすべて既存の教委一次ソースのみ使用 |
| **保護者到達への影響(G7)** | `SchoolPageParentBridge`(143行目)は**位置を一切変更しない**（新テーブルはその前に挿入・143行目より後のJSXは無変更）ため順序規約は遵守。ただし新テーブルが6年度分（東京都・山口県は最大6年度）まで描画されるとページの縦の高さが増え、`SchoolPageParentBridge`まで到達するスクロール距離が伸びる。**これは順序違反ではないが監視対象とする**（kill_criteria参照） |
| **kill_criteria** | デプロイ後28日でGA4 `share_to_parent`（学校ページ発生分・`SchoolPageParentBridge.tsx:39`の`tool:'school-page'`で識別可能）またはD1 `parent_funnel_events`の学校ページ由来が、導入前の同期間比で明確に低下したら、5年度以上のprefectureは「直近3年度のみ表示＋詳細開閉」に圧縮する。既存の単年度hero統計（`school.totalQuota`等）の値が新機能デプロイ後に1件でも変化したら（回帰テストの不変条件違反）即ロールバック |
| **確信度** | データの存在・42/47県の充足状況＝断定（`ops/CORRECTIONS.md` C-6a）。順位・CTRへの因果効果＝未検証 |

**✅既に対応済み（T-A1として本書とは別のタスク名で完了・2026-08-24にloopが再確認し本書へ記載漏れを追記）。** `src/lib/school-page-data.ts`の`buildSchoolHistoryForPrefecture()`（本案が指定した設計と同一の未フィルタ`rates.records`から学校名→fiscalYearでグループ化するロジック）・`school-page-lookup.ts:113-117`での合流・`groupSchoolHistoryByDepartment()`・`page.tsx:181-`の新規テーブル描画（`SchoolPageParentBridge`→`SchoolPageConvertCTA`→`ParentLeadCTA`→学科別内訳→多年度推移の順で描画され、`SchoolPageParentBridge`の位置は変更されていないためG7規約も遵守）まで全て実装済み。`school-page-lookup.ts:109`の今季用フィルタも本案の指定通り削除されていない（別経路として新設）。`school-page-data.test.ts`に単体テスト・東京都日比谷の実データによる不変条件テスト・2026-08-09事故のリグレッションテスト（`totalQuota`等がhistoryを混ぜても変化しないことの検証）・全47県横断の不変条件機械検証が揃っている。**本案として新たに実施すべき残作業は無い**（LOOP_CONTRACT.md §0のT-A1行にkill_criteria判定日2026-09-09と記載済み）。

### S3-2: 学校ページと県別まとめページに収益CTA（`ParentLeadCTA placement="prefecture"`）を追加する

| 項目 | 内容 |
|---|---|
| **ID** | S3-2 |
| **施策名** | 学校ページ233枚・県別まとめ47枚に既存の県面オファーを1行で配線する |
| **現状の実測** | `grep -n "AffiliateAd\|ParentLeadCTA\|selectLeadOffer\|goHref" src/app/pref/[code]/school/[schoolCode]/page.tsx src/app/pref/[code]/page.tsx` → **0ヒット**（`ops/MONEY.md` §4-b・本書で再確認）。学校ページ自体は`SchoolPageConvertCTA`(141行目・`/reverse`と`/juku-shindan`への内部リンクのみ)と`SchoolPageParentBridge`(143行目・LINE/共有のみ)しか持たず、`AffiliateAd`/`ParentLeadCTA`は1つも描画していない（コンポーネント2本を本書で直接読了し確認済み）。県別まとめは`src/app/pref/[code]/page.tsx`のimport一覧に`ParentShare*`系すら存在せず換金・共有コードとも無い |
| **変更内容** | `src/app/pref/[code]/school/[schoolCode]/page.tsx`の143行目（`<SchoolPageParentBridge .../>`）の**直後**、145行目（学科別内訳セクション）の**直前**に`<ParentLeadCTA prefectureCode={prefecture.code} placement="prefecture" />`を1行追加（既存の`PLACEMENT_LEAD_OVERRIDES.prefecture`・`lead-config.ts:163`で解決・新規案件不要。MONEY出血5と同一の最小修正）。同様に`src/app/pref/[code]/page.tsx`にも同じ1行を追加（挿入位置はimportに`ParentShare*`系が無いことを確認済みのため任意・702行目のCTA直前を推奨）。**S3-1で新設する多年度テーブルより後**（＝`SchoolPageParentBridge`より後）に置くことで、G7の順序規約（ResultSection.tsx:294-297の「保護者共有導線より下に置かない」原則をここにも適用した独自の予防線。当該ファイル自体にはこの規約の明記コメントは無いため本書の推奨として明示する）を満たす |
| **EV(円/月)** | 学校ページ28日実績: 表示605・**クリック7**（`ops/raw/gsc-pages-28d.json`・BAR/BRIEF/DISTANCE三者一致）。県別まとめは28日でGSC表示0（`ops/raw/BRIEF.md`§2「`/pref/{code}`は1インプレッションも無い」）→現時点のEV寄与¥0。<br>解決オファー＝`moshimo-manabuterasu`（オフシーズン・全国型34県、`affiliate-economics.ts:96`でcpaYen8000/convRateLow0.04を本書で再確認＝EV¥192/click）。ただし`prefecture`は`SEASONAL_PLACEMENTS`の一員（`lead-config.ts:311-317`）で、冬(11/1-12/31)・入試直前(1/1-2/15)は自動的に`sora-juku-text`(EV¥240、`affiliate-economics.ts:110`で再確認)またはkanto/kansai13県は地盤塾(EV¥360)へ切り替わる（`seasonal.ts:80-82`、`lead-config.ts:333-339`）。<br>月間クリック=7×30/28≈7.5。<br>**現状（オフシーズン・¥192基準）**: 下限 7.5×0.198%×¥192=**¥3/月**、上限 7.5×0.537%×¥192=**¥8/月**。<br>**2月相当の季節（仮置き11.7倍・Google Trends代理値・自社未実測・`ops/BAR.md`§5-1と同型の仮定。ただし2月は季節スワップ発動中のためEV基準を¥240へ差し替える点がBARの計算との違い）**: 表示605×11.7=7,079、現状CTR1.16%のまま=82クリック/28日→月間88クリック。下限 88×0.198%×¥240=**¥42/月**、上限 88×0.537%×¥240=**¥113/月**（**BAR §5-2シナリオ2の¥200/月はCOEFFICIENTSが不採用にした遷移率0.455%を使っており過大。本書はCOEFFICIENTS採用値で計算し直した**） |
| **工数(h)** | 0.5h（2ファイルに1行ずつ） |
| **EV/h** | 現状ベース ¥6〜16/h（季節仮定込みなら¥84〜226/h。ランキングには保守的な現状ベースを採用） |
| **効き始める時期** | 8/31までに着手すべき（最小工数・低リスク） |
| **リスク(C8)** | 無し。既存の県面オファーを配線するだけで新規訴求文言なし |
| **人間ゲート(C7)** | 不要（loop可・デプロイのみ👤） |
| **保護者到達への影響(G7)** | `SchoolPageParentBridge`の**後**に挿入するため位置は不変。県別まとめは`ParentShare*`系が元々存在しないため押し下げる対象が無い（import一覧を本書で確認済み） |
| **kill_criteria** | デプロイ後28日でこの枠のクリックが0のまま（母数7クリック/28日は元々小さいため判定が難しい点に注意）なら、季節切替（11/1）後に再評価する。GA4 `share_to_parent`の学校ページ由来がデプロイ前後で低下したら`SchoolPageConvertCTA`と`SchoolPageParentBridge`の間に挟む案に変更する |
| **確信度** | 配線内容・EV式＝断定（コード確認済み）。季節仮定の実効値＝仮置き（根拠明記） |

### S3-3: 学校名の短縮形（「〜高校」）をtitle・meta description・h1・内部リンクのアンカーテキストに追加する

| 項目 | 内容 |
|---|---|
| **ID** | S3-3 |
| **施策名** | 学校ページと、そこへ向かう内部リンクのアンカーテキストに、正式名「〜高等学校」に加えて短縮形「〜高校」を併記する |
| **現状の実測** | 自社ページに短縮形「〜高校」は**0回**（正式名「〜高等学校」16回のみ・`self.html`実測）。一方GSC実クエリは短縮形が**8,326クエリ/32,528表示/1,064クリック**、正式名は**998クエリ/1,392表示/14クリック**＝表示23倍・クリック76倍（`ops/raw/gsc-queries-full-28d.json`全13,958行集計・`ops/BAR.md`§0-6）。競合3サイトは全て短縮形を使用済み（minkou4回・進研ゼミ12回・高校偏差値.net17回・`ops/BAR.md`§2-1 #1）。加えて内部リンクのアンカーテキストも全て正式名: 県別まとめの学校ロスター（`src/app/pref/[code]/page.tsx:695` `{s.schoolName}`）・学校ページの近隣校リンク（`src/app/pref/[code]/school/[schoolCode]/page.tsx:229` `{n.schoolName}`）・パンくず（同`:102`）が全て`schoolName`（＝マッチ後の正式フルネーム）をそのまま使用（本書で3箇所とも直接確認） |
| **変更内容** | `src/lib/school-name-match.ts:84`の`SCHOOL_TYPE_TOKEN_SPLIT_PATTERN = /^(.+?)(高等学校\|高校)(.*)$/`（分校名の埋め込みまで考慮済みの既存正規表現・47都道府県の実データで検証済み）を再利用し、`school-page-data.ts`に`deriveShortName(fullName: string): string`を新設（ベース名＋短縮形サフィックスを再構成。分校名がある場合はそのまま残す）。①`page.tsx:53`のtitleに「{school.schoolName}（{shortName}）の入試倍率・募集人員」、②`:54`のdescriptionに短縮形を追記、③`:111`のh1に短縮形を併記、④県別まとめ`:695`と近隣校リンク`:229`のアンカーテキストに短縮形を追加。**新規ページは作らない・既存47/47県の既存ページのtitle/meta/リンクテキストのみ変更**（新規公開ルールの対象外） |
| **EV(円/月)** | **円は出せない**。理由: 短縮形追加が順位・CTRへ与える因果は自社未検証（`ops/BAR.md` V-6「短縮形の効果は未測定」と同一）。参考として`ops/PHASE0_FINDINGS.md`出血3・出血4が同種の現象（`/naishin-oru/4`が「八王子実践高校」等の学校名クエリを4.13位・**クリック0**で受けている）を実測しているが、**この2つの生成ページ(`/naishin-oru/[grade]`・`/blog/naishinten-average-score`)は本文中に個別の学校名を1件も含んでいない**ことを本書で確認済み（`naishin-oru/[grade]/page.tsx`・`naishinten-average-score.ts`を全文読了・「北園高校」「向丘高校」等の文字列は0件）。したがって「汎用ページから該当校ページへ内部リンクを張る」という直感的な対策は**この2ページには適用不能**（リンクを張る文脈自体が存在しない）——Googleが独自の関連性判定で学校名クエリにこれらの汎用ページを充てているとみられ、S4での「内部リンク追加」では解決できない。取りうる対策は**学校ページ自身のエンティティ信号を強めて自然に取って代わる**ことのみで、それが本施策とS3-4。なお該当クエリの一部（「一条高校」「上溝高校」）は公立で自社に既存ページがある可能性が高い一方、「八王子実践」「中京大中京」は私立（学校マスタは公立のみ・Y-0範囲外）で自社には受け皿ページ自体が無く、本施策の対象外（新規に私立ページを作る判断はしない＝「新規ページを作る案は原則却下」） |
| **工数(h)** | 1.5h（`deriveShortName`関数＋テスト0.5h、title/description/h1/2箇所のアンカー変更1h） |
| **EV/h** | 出せない |
| **効き始める時期** | 8/31までに着手すべき（低コスト） |
| **リスク(C8)** | 無し。教委一次データの学校名から機械的にサフィックス変換するのみで、誤った学校名を捏造するリスクは無い（`SCHOOL_TYPE_TOKEN_SPLIT_PATTERN`は既存の学校名突合で47都道府県分検証済みのロジックを再利用するため、分校名混入等の事故は起きにくい） |
| **人間ゲート(C7)** | 不要（loop可・デプロイのみ👤） |
| **保護者到達への影響(G7)** | 無し（title/meta/アンカーテキストの文言変更のみでコンポーネントの配置順は変えない） |
| **kill_criteria** | BAR V-6の推奨手順（1県だけで実施し4週間GSC追跡）に従い、まず1県（工数最小のため学校数が少ない県、例tottori/yamanashi・wave2コメント参照）でのみ実施し、4週間後に該当県の学校ページ群の加重平均順位・CTRが変化しなければ「短縮形単独では効果が測れない」と明記し、残り46県への展開判断を保留する |
| **確信度** | クエリ側のギャップ（23倍/76倍）＝断定。施策効果＝未検証 |

### S3-4: 実測済みの多年度データからHighSchool + FAQPage構造化データを生成する

| 項目 | 内容 |
|---|---|
| **ID** | S3-4 |
| **施策名** | 学校ページに`HighSchool`構造化データと、募集人員・応募者数・倍率の実数に基づく`FAQPage`構造化データを追加する |
| **現状の実測** | 自社学校ページのJSON-LDは`Organization`/`ImageObject`/`Person`/`WebSite`/`BreadcrumbList`の2ブロックのみで、**`HighSchool`/`FAQPage`は0**（`self.html`実測・`ops/BAR.md`§8末尾）。競合minkouは`HighSchool`+`AggregateRating`+`PostalAddress`+`FAQPage`の3ブロックを持つ（`minkou.html` JSON-LD抽出）。ただしminkouの`AggregateRating`は口コミ21件由来（`ops/BAR.md`§2-2）——**自社にはレビューデータが無いため`AggregateRating`は追加しない**（捏造禁止・Y-0の精神をレビュー捏造にも適用） |
| **変更内容** | 新規コンポーネント`SchoolPageStructuredData`を追加し、①`HighSchool`（`name`=正式名、`alternateName`=S3-3の短縮形、`address`、`url`=canonical、`sameAs`は無し=未検証情報を作らない）、②`FAQPage`を学校固有の実数のみから生成（例:「Q. {school.schoolName}の今年度の入試倍率は？」「A. {school.overallRate}倍です（募集{school.totalQuota}名に対し応募{school.totalApplicants}名、{出典教委名}公表・{最終確認日}時点）」）。既存の`FAQPageSchema`コンポーネント（`naishin-oru/[grade]/page.tsx:7,111`で使用実績あり）と同型の実装パターンを再利用する。**推定値・断定的な合格可能性のQ&Aは作らない**（「この内申点なら合格できますか」等は既にNaishinOruページが「一次情報が無いため掲載しない」と明記済みの方針を継承） |
| **EV(円/月)** | **円は出せない**。理由: `ops/BAR.md` V-5「学校ページにHighSchool JSON-LDを足した場合の順位影響は因果が自社データから分離できない」と明記の未検証事項そのもの。ただし`ops/BAR.md`§2-3の欠落項目チェックリスト（26セル中×18件）のうち#20(HighSchool)・#20b(FAQPage)を埋める＝差別化因子としての価値（円換算不能・§3差-2/差-4文脈） |
| **工数(h)** | 2.5h（構造化データコンポーネント新設2h＋FAQ文面のY-0適合レビュー0.5h） |
| **EV/h** | 出せない |
| **効き始める時期** | 9月中（S3-1完了後に着手すると多年度FAQが作れるため依存関係を推奨するが、必須ではない＝単年度でも実装可能） |
| **リスク(C8)** | 低。JSON-LDの内容が実際の可視テキストと一致していること（Google のstructured data policy）を要確認——FAQの回答文言は既存の可視ページ本文の言い換えに留め、非表示の追加情報を注入しない設計とする |
| **人間ゲート(C7)** | 不要（loop可・デプロイのみ👤） |
| **保護者到達への影響(G7)** | 無し（非表示メタデータのみ） |
| **kill_criteria** | 該当なし（構造化データの追加自体はリスクがほぼ無いため撤回条件を設けない）。ただしSearch ConsoleでFAQPage/HighSchoolのリッチリザルトエラーが出た場合は該当フィールドを修正 |
| **確信度** | 欠落の事実＝断定。順位影響＝未検証（BAR V-5を継承） |

### S3-5: 多年度データが2年度しかない5県（hokkaido/nara/niigata/oita/saga）に、もう1年度分を追加収集する

| 項目 | 内容 |
|---|---|
| **ID** | S3-5 |
| **施策名** | 5県の教委一次資料からもう1年度分（3年度目）の募集人員・応募者数を追加収集し、S3-1のB-4必須ゲートを47/47県で満たす |
| **現状の実測** | `src/data/competition-rates/*.ts`のうちhokkaido・nara・niigata・oita・sagaの5県は`sources[]`に令和8・令和7年度の**2年度分のみ**（`ops/CORRECTIONS.md` C-6a・python走査で確認済み）。BAR §0-1の「新規データ収集ゼロで全47県がバーを超えられる」という主張はこの5県には成立しない（同C-6aが訂正済み） |
| **変更内容** | 5県それぞれの教委が公表する令和6年度（2024年度）公立高校入学者選抜の確定志願者数資料を検索・取得し、`competition-rates/{pref}.ts`の`records[]`に`fiscalYear: '令和6年度（2024年度）'`付きレコードを追加、`sources[]`にも該当URL/docTitle/fetchedAtを追加する。**新規のコード変更ではなくデータ収集作業**。既存パターン（chiba.ts:31-38が記録する令和6年度は画像PDFで`pdftoppm -r 300`によるビジョン解析を要した前例）を踏まえ、5県のうち画像PDFの県はビジョン解析が必要になる可能性を織り込む |
| **EV(円/月)** | **円は出せない**（S3-1と同一理由を共有——本施策単体はS3-1の多年度テーブルが表示する年度数を2→3に増やすだけで、独立したEVの発生源ではない） |
| **工数(h)** | **15h**（5県×平均3h。テキストPDFなら1.5h/県、画像PDFでビジョン解析が要る場合は4h/県。`ops/BAR.md`差-1の「県ごとの判断を伴う転記労働」がそのままコストの本体） |
| **EV/h** | 出せない |
| **効き始める時期** | 9月中に着手、**フリーズ前(11/14)までに完了**（データ追記がフリーズ規約の対象かどうかは`ops/CLOCK.md` CL-3が未裁定のため、裁定を待たずに済むよう11/15より前に終える） |
| **リスク(C8)** | 無し。既存の教委一次ソース収集パターンをそのまま5県分繰り返すのみ |
| **人間ゲート(C7)** | 不要（loop実行可・データはすべて公開の教委一次資料） |
| **保護者到達への影響(G7)** | 無し |
| **kill_criteria** | 該当なし。ただし1県あたり収集に4h超かかる場合（画像PDF+解析困難）は、他の優先タスクとの兼ね合いで後回しにする判断を許容する（B-4ゲートは42/47県で既に成立しており、残り5県は「揃える」価値はあるが緊急ではない） |
| **確信度** | 断定（C-6aで2年度のみと確認済み） |

---

## S4: 内部リンク・孤児（4案）

**前置き（必ず読む）**: `ops/DISTANCE.md`は611行のGSCページ全数を対象に収益面までの距離を計算し、**∞ページ（内部リンクで到達不能）は0枚**と結論している（dA/dAff/dSいずれの定義でも・§3.3）。`npm run check:orphans`（`scripts/check-orphans.mjs`）も独立系統で静的ルート107・オーファン0を確認済み（`ops/PHASE0_FINDINGS.md` M17）。**「孤児ページを探して直す」という施策はこの案件では成立しない**（実測ゼロ）。本節が扱うのはDISTANCE §4.2が発見したより本質的な問題——「**距離1の中身が面によって桁で違う**」（611ページ中530ページが既に距離1なのに、実アフィリクリックは面ごとに0%〜23%まで幅がある）。以下4案はDISTANCE §7の施策A〜Cを本書の様式で再整理し、COEFFICIENTS採用値との整合を確認したもの、および孤児化を今後も防ぐための監視施策1本。

### S4-1: `/hogosha`への到達距離を3→2にする（`StaticToolEntryLinks`を高流入4面に追加）

| 項目 | 内容 |
|---|---|
| **ID** | S4-1 |
| **施策名** | 検索流入ゼロだが人間クリック転換率が最も高い`/hogosha`への内部リンクを、サイト最大流入4面に追加する |
| **現状の実測** | `/hogosha`はGSC表示0・クリック0（検索から誰も来ない）にも関わらず、内部referer付きの実アフィリクリック43件中**16件（37%）**を生んでいる（`ops/DISTANCE.md`§4.2実測表）。`/hogosha`への文脈リンクを持つのは16ルート（`ops/CORRECTIONS.md` C-5が7→16に訂正済み。ただし追加9ルートはいずれも低・ゼロトラフィック面）が、月間クリック上位の`/`(1,879)・`/tokyo/total-score`(908)・`/kanagawa/s-value`(489)・`/blog`共通枠(1,497)は**いずれも`/hogosha`まで距離3**（`ops/DISTANCE.md`§4.2末尾）。⚠️この16件の実態については`ops/CORRECTIONS.md`§2が新たな論点を提起している——root_only referer（パス無し）の33件中`parent-lp`(=`/hogosha`系)由来は17件あるが、**「そのページに`parent-lp`のplacementが実在しない＝自己矛盾＝bot」という判定軸も同時に成立しうる**（DEADWIRE DW-3の見立てと対立）。本施策の効果測定には、この対立を解く検証（S9-1・`ops/proposals/S1-S9-S11.md`所収・Cowork発注）が実質的な前提になる |
| **変更内容** | 既存コンポーネント`StaticToolEntryLinks`（`/hogosha`+`/juku-shindan`への静的リンク2本組・`src/components/StaticToolEntryLinks.tsx:16`）を、新規面に追加する。対象: ①`src/app/HomeClient.tsx`の`ResultSection`(587-601行目・本書で確認)の**後**、既存`ParentLeadCTA`(643行目)付近——`ResultSection`内部で既に保護者導線の順序規約(`ResultSection.tsx:294-297`)を守っているため、その外側（後）に置けば自動的に安全。②`src/app/tokyo/total-score/page.tsx`・`src/app/kanagawa/s-value/page.tsx:357`付近（`ops/DISTANCE.md`§7.5が既に特定した箇所と同一）。③`src/components/BlogRelatedLinks.tsx`（既存の共通リンク11本を生成する箇所・`:33,60,75,90,106,123`）に1本追加。新規ページは作らない・既存コンポーネントの再配置のみ |
| **EV(円/月)** | `ops/DISTANCE.md`§7.2の計算をそのまま引用（対象クリック4,773/28日→5,114/月、係数=内部リンクCTR仮置き1〜5%×`/hogosha`到達後アフィリCTR仮置き8〜20%×`convRateLow`0.015×`CONFIRM_RATE`0.6×CPA¥11,500）: **下限¥423/月・中位¥1,588/月・上限¥5,293/月**。この計算はCOEFFICIENTS§1/§2の「GSCクリック→サイト全体アフィリクリック」の遷移率とは別の独立したファネル（内部リンククリック→`/hogosha`到達→アフィリクリック）のため、COEFFICIENTS値による補正対象ではない。ただし前述のroot_only論点が「16件は自己矛盾＝bot」の側に決着した場合、この施策の前提（`/hogosha`は到達させれば効く）自体が揺らぐ点に注意 |
| **工数(h)** | 2h（4箇所へのコンポーネント追加＋G7順序確認） |
| **EV/h** | **¥212〜2,647/h（中位¥794/h）**——本書群で最もEV/hが高い |
| **効き始める時期** | 8/31までに着手すべき |
| **リスク(C8)** | 無し |
| **人間ゲート(C7)** | 不要（loop可・デプロイのみ👤） |
| **保護者到達への影響(G7)** | `ops/DISTANCE.md`§8.3が施策Aを事前点検済み: `/hogosha`は`ParentShareBanner`を描画する面（`src/app/hogosha/page.tsx:209`）のため、**到達を増やすことが保護者到達そのものを増やす**（利益相反なし・✅推奨）。HomeClient挿入位置のみ本書独自の追加確認（`ResultSection`の外側＝橋①②より必ず後） |
| **kill_criteria** | S9-1（`/hogosha`のroot_only referer実機検証・`ops/proposals/S1-S9-S11.md`）の結果、16件が「人間由来と断定できない」（root_only=bot側の判定）になった場合、本施策のEV試算を「未検証」に格下げし、実施済みなら効果測定を4週間延長する |
| **確信度** | 距離構造・現状の16件実測＝断定。EV係数（内部リンクCTR・到達後CTR）＝仮置き（`ops/DISTANCE.md`§7.1に根拠明記） |

**✅2026-08-24対応済み(loop)。** 詳細は`ops/PROPOSALS.md` §2-A の該当行、または`ops/proposals/S2-S12.md`のS2-4を参照（同一施策・重複ID）。

### S4-2: ブログ共通リンク枠に`/hiyou`（高CPA面）への導線を1本追加する

| 項目 | 内容 |
|---|---|
| **ID** | S4-2 |
| **施策名** | 全51記事に共通で載る11本のリンクに、高CPA面（`/hiyou`）を1本追加する |
| **現状の実測** | ブログ共通リンク11本（`src/components/BlogRelatedLinks.tsx:33,60,75,90,106,123`が出所）に高CPA面（`/hiyou`/`/suisen-nyuushi`/`/hogosha`/`/mendan`/`/juku-shindan`）は**0本**。記事本文51本中でも1本のみ（`education-cost-junior-high-to-high-school.ts`）が`/hiyou`等にリンクしている（`ops/DISTANCE.md`§6.3/6.4）。ブログ実記事のGSC 28日クリックは1,497だが、内部referer付きアフィリクリックは0件（`ops/DISTANCE.md`§6.5） |
| **変更内容** | `BlogRelatedLinks.tsx`の共通枠に`/hiyou`への1本を追加する（新規面は作らない） |
| **EV(円/月)** | `ops/DISTANCE.md`§7.3を引用: 対象クリック1,497/28日→1,604/月、係数=内部リンクCTR仮置き1〜5%×`/hiyou`アフィリCTR仮置き10〜20%×0.015×0.6×¥11,500。**下限¥166/月・中位¥332/月・上限¥1,660/月** |
| **工数(h)** | 0.5h（1ファイル1箇所） |
| **EV/h** | **¥332〜3,320/h（中位¥664/h）** |
| **効き始める時期** | 8/31までに着手すべき |
| **リスク(C8)** | 無し |
| **人間ゲート(C7)** | 不要 |
| **保護者到達への影響(G7)** | `ops/DISTANCE.md`§8.3確認済み: ブログには`ParentShareInvite`/`ParentShareLinkButton`が元々存在せず（`blog/[slug]/page.tsx`の描画ツリーで確認済み）、押し下げる対象が無い（✅安全） |
| **kill_criteria** | デプロイ後28日で`/hiyou`のD1クリックに変化が無ければ（元々月0〜4件と母数が小さいため判定が難しい点に注意）、内部リンクCTRの仮置き係数自体を疑い、`cta_view`計装（`CtaViewTracker`）で実測に置き換える |
| **確信度** | 現状の欠落＝断定。EV係数＝仮置き（DISTANCE §7.1根拠明記） |

### S4-3: 距離2の30ルート（81枚・59クリック/28日）に収益CTAを追加し距離1にする

| 項目 | 内容 |
|---|---|
| **ID** | S4-3 |
| **施策名** | ページ自身に収益CTAが1つも無い距離2の30ルートに、既存の`ParentLeadCTA`を1本ずつ追加する |
| **現状の実測** | `ops/DISTANCE.md`§3.3が特定した30ルート（`/hensachi/gyakusan/hayamihyou`18クリック・`/total-score/mantenkan`15クリック・`/[prefecture]/naishin-omomi`(35枚)6クリック等）は距離2（dA）・距離3（dS）で、28日合計59クリック・4,394表示 |
| **変更内容** | 30ルートの`page.tsx`にそれぞれ`ParentLeadCTA`を1本追加（面に応じた`placement`を明示・S1-1〔`ops/proposals/S1-S9-S11.md`〕と同一パターンを流用） |
| **EV(円/月)** | 月間63クリック(59×30/28)。**COEFFICIENTS §1/§2は「GSCクリック→サイト全体アフィリクリック」の遷移率を定義しているため、この施策には直接適用できる**（DISTANCE施策A/Bと異なり内部リンクを1つ挟まない直接CTAのため）: 下限 63×0.198%×(0.015×0.6×¥11,500)=63×0.00198×¥103.5=**¥13/月**、上限 63×0.537%×¥103.5=**¥35/月**。`ops/DISTANCE.md`§7.4は独自のアフィリCTR仮定（0.50%/4.05%/15%）で¥33/¥264/¥978/月と試算しているが、COEFFICIENTS採用値(0.198%〜0.537%)は0.50%に近いため¥13〜35という本書の下限〜上限はDISTANCEの¥33（0.50%行）と同じ桁で整合する。**DISTANCEの15%(¥978)は「`/hogosha`相当の高密度面にした場合」という別の仮定であり、単純追加では成立しない** |
| **工数(h)** | 3h（30ルートへの一律追加。ほぼ同一JSXの反復のためファイルあたり平均6分） |
| **EV/h** | **¥4〜12/h**——本書群で最も低い。優先度は最下位 |
| **効き始める時期** | 9月中（低優先度） |
| **リスク(C8)** | 無し |
| **人間ゲート(C7)** | 不要 |
| **保護者到達への影響(G7)** | `ops/DISTANCE.md`§8.3確認済み: 対象30ルートは`ParentShare*`/`UnlockGate`/`ParentWindowBridge`を1つも描画していない（各`page.tsx`をgrepで確認済み・✅安全） |
| **kill_criteria** | EV/hが本書群で最下位のため、8/31までに他の全案（S3-1〜5・S4-1・S4-2・S6-1〜3）に着手した後、時間が余った場合のみ着手する。着手しない場合も損失は無い（現状維持） |
| **確信度** | 現状・EV式＝断定。EV係数の一部（convRateLow=0.015のFP相談群）＝仮置き（`affiliate-economics.ts:91,92,94`） |

### S4-4: `check:orphans`をCI（`check:all`）に組み込み、孤児化の再発を防ぐ

| 項目 | 内容 |
|---|---|
| **ID** | S4-4 |
| **施策名** | 現状オーファン0を確認済みの`check:orphans`スクリプトを定常監視に組み込み、今後のページ追加（S3-1〜5等）で孤児が発生しないことを機械的に保証する |
| **現状の実測** | `scripts/check-orphans.mjs`は存在し実行すればgreen（静的ルート107・ソース1,170ファイル・オーファン0・`ops/PHASE0_FINDINGS.md` M17）だが、`package.json:24`の`check:all`は`check:consistency && check:forbidden`のみで**`check:orphans`を含まない**（本書で`package.json`を直接確認）。学校ページ層は今後もS3-1〜S3-5のような追記が続く見込みで、3,089枚規模のページ群で孤児が発生した場合に検知する仕組みが定常運用に乗っていない |
| **変更内容** | `package.json:24`の`check:all`スクリプトに`&& npm run check:orphans`を追加する1行変更。CI/pre-pushフックへの組み込みは既存の`check:all`呼び出し箇所（存在すれば）に自動的に波及する |
| **EV(円/月)** | **円は出せない**。理由: 現状オーファン0（実害が発生していない）ため、これは将来のリグレッション予防（保険）であり発生確率×影響の期待値を計算する未知数（孤児発生確率）が無い |
| **工数(h)** | 0.5h（1行追加＋実行時間が許容範囲か確認〔3,089ページ規模でのスクリプト実行時間を計測〕） |
| **EV/h** | 出せない（保険的価値） |
| **効き始める時期** | 8/31までに着手すべき（低コスト・S3-1〜5より前に入れておくと以後の変更を即座に検証できる） |
| **リスク(C8)** | 無し |
| **人間ゲート(C7)** | 不要 |
| **保護者到達への影響(G7)** | 無し |
| **kill_criteria** | 該当なし（監視系）。実行時間が過大（目安30秒超）でCIを遅延させる場合は週次実行に頻度を落とす |
| **確信度** | 断定（`package.json`実測・`check:orphans`の存在と現状green実績はPHASE0_FINDINGS M17で確認済み） |

---

## S6: E-E-A-T（3案）

**前置き**: `ops/BAR.md`§2-5・§3差-1〜差-6が確立した通り、**Y-0憲法（学校別偏差値・ボーダーの独自推定禁止＋1データ点1出典）そのものが競合に対する差別化因子**である。46/47県が公的ドメイン（`.lg.jp`/`.go.jp`/`.ed.jp`等）のURLを引用（119本）。進研ゼミは商用二次ソース（育伸社）を購入し「※倍率数値は株式会社育伸社の資料を掲載しています」と自ら明記（`czemi.html`）、高校偏差値.netは年度ラベルが1年ずれ（「2023年0.83」＝実際は令和6年度）かつ教委一次にも進研ゼミにも存在しない値「2024年0.64」を掲載（`ops/BAR.md`§2-5）。以下3案はこの堀を**強化**するもので、競合の欠陥を名指しで公開比較する形は取らない（景表法・名誉毀損リスクを避けるため、自社の手法を自己記述するだけに留める設計とした——詳細は各案のリスク欄）。

### S6-1: 学校ページの数値（募集人員・応募者数・倍率）の直下に、その年度固有の一次資料URLを表示する

| 項目 | 内容 |
|---|---|
| **ID** | S6-1 |
| **施策名** | 学校ページの倍率数値のすぐ下に、その年度のデータが実際に出典とする教委PDFのURL・資料名・取得日を表示する |
| **現状の実測** | 現在、学校ページ上で「出典」として表示されているのは`SchoolPageNaishinNote.tsx:76-85`の`prefecture.sourceUrl`（`src/lib/prefectures.ts`由来・例:「千葉県教育委員会 入学者選抜」）のみで、これは**内申点計算方式の一般的な出典**であり、募集人員・応募者数・倍率の数値そのものの出典ではない。数値の実際の出典は`CompetitionRateRecord`とは別に`PrefectureCompetitionRateFile.sources[]`（`competition-rate.ts:47-53`）が保持し、chiba.tsでは令和8/7/6年度でそれぞれ異なるPDF URL（`r8kakuteiippan.pdf`/`r7kakuippan.pdf`/`r60216ippan.pdf`）を持つ（本書で`chiba.ts:51-68`を直接確認）。**この`sources[]`は学校ページのどこにも描画されていない**（`page.tsx`を全文読了し確認・`SchoolPageNaishinNote`が使うのは`prefectures.ts`の別データ）。つまりBAR §2-1 #19「出典の明示○」は内申点計算方式のページ内リンクの評価であり、倍率の数値そのものへの出典リンクは実際には無い | 
| **変更内容** | S3-1の新設多年度テーブル（および現行の単年度セクション122-139）の直下に、`sources[]`から`fiscalYear`一致（未指定は`sources[0]`）で該当PDFの`url`/`docTitle`/`fetchedAt`を表示する行を追加する。1年度=1出典（Y-0憲法②）をUI上でも1対1に対応させる |
| **EV(円/月)** | **円は出せない**（信頼シグナルは逸失収益として計測経路が無い・`ops/THREATS.md`脅威5〔TH-3〕・脅威8〔TH-6〕と同型の扱い）。価値は`ops/BAR.md`差-2「1データ点1出典の監査可能性」の実装をS3-1が拡張する多年度データにも一貫適用すること | 
| **工数(h)** | 1.5h（S3-1の新規UIコンポーネントに出典行を追加。S3-1と同時実装が効率的） |
| **EV/h** | 出せない |
| **効き始める時期** | 9月中（S3-1と同時） |
| **リスク(C8)** | 無し |
| **人間ゲート(C7)** | 不要 |
| **保護者到達への影響(G7)** | 無し |
| **kill_criteria** | 該当なし |
| **確信度** | 現状の欠落＝断定（コード確認済み）。効果＝円換算不能につき評価対象外 |

### S6-2: `/genten-archive`・`source-history.ts`を、倍率データの一次ソース確認履歴にも拡張する

| 項目 | 内容 |
|---|---|
| **ID** | S6-2 |
| **施策名** | 内申点計算方式のみを対象にしている既存の「一次ソース確認履歴アーカイブ」を、募集人員・応募者数・倍率データの確認履歴にも拡張する |
| **現状の実測** | `src/lib/source-history.ts:1-10`のコメントが自ら明記する通り、この履歴基盤は「`prefectures.ts`が保持する『現在の』sourceUrl/sourceTitle/lastVerified」（＝内申点計算方式）専用に設計されており、`competition-rates/*.ts`の`sources[]`（倍率データの年度別出典）は対象に含まれていない。`/genten-archive`・`/reliability`（本書で`reliability/page.tsx`を読了・確認）も同様に`PREFECTURES`配列のみを走査し、倍率データの検証履歴には触れていない。BAR §3差-2が「一次ソース確認履歴アーカイブを既に公開している」ことを差別化因子として挙げているが、**その対象範囲は内申点計算方式のみで、差-1の本体（募集人員・出願者数の実数）は含まれていない**という取りこぼしがある |
| **変更内容** | `/genten-archive`（または`/reliability`）に新セクションを追加し、47県×`competition-rates/*.ts`の`sources[]`全件（chiba なら3件・47県で概ね130〜140件相当）を都道府県別・年度別に一覧表示する。既存の`getAllSourceHistories()`と同型の集計関数を`competition-rate.ts`のデータに対して新設する。新規ページは作らない（既存ページの新セクション） |
| **EV(円/月)** | **円は出せない**（信頼シグナル、`ops/THREATS.md`と同型の扱い）。価値はBAR差-2の適用範囲を差-1の本体（17,142レコード）にまで広げること。「一次ソース確認履歴を持つサイト」自体が競合4サイト中どこにも存在しない（BAR §2-1確認済み） |
| **工数(h)** | 3h（新規集計関数＋既存ページへのセクション追加＋47県分のレンダリング確認） |
| **EV/h** | 出せない |
| **効き始める時期** | 9月中 |
| **リスク(C8)** | 無し |
| **人間ゲート(C7)** | 不要 |
| **保護者到達への影響(G7)** | 無し |
| **kill_criteria** | 該当なし |
| **確信度** | 現状の対象範囲の欠落＝断定（コード確認済み） |

### S6-3: `/reliability`に、自社のデータ検証方針（一次資料主義・突合テスト）を自己記述する透明性セクションを追加する

| 項目 | 内容 |
|---|---|
| **ID** | S6-3 |
| **施策名** | 「なぜこのデータを信頼できるか」を、自社の検証プロセス（一次資料のみ使用・公式小計との突合テスト・独自推定をしない範囲の明示）として`/reliability`に自己記述する |
| **現状の実測** | `/reliability`（`src/app/reliability/page.tsx`）は現状、検証日・件数などの**数値サマリー**（`verifiedPrefectures.length`等）のみで、**「どう検証しているか」という方法論の説明が無い**（本書でページ全文の構成を確認）。一方`competition-rate.ts:88-116`の`checkAgainstSubtotal()`（公式公表の「計」行との自己集計突合）や、Y-0憲法の3原則（学校別偏差値・ボーダーの独自推定禁止／1データ点1出典／機械可読不能は正直にスキップ）は、コードコメントとしては存在するが**ユーザー向けページには一切露出していない** |
| **変更内容** | `/reliability`に新セクション「データの検証方法」を追加し、①一次資料（教育委員会公表資料）のみを出典とする方針、②公式公表の「計」行との自己集計突合を実施している事実（`checkAgainstSubtotal`の存在。実装詳細は開示せず方針のみ）、③学校別の偏差値・合格ボーダーは独自推定せず掲載しないという制約、を平易な文章で説明する。**競合名（進研ゼミ・高校偏差値.net等）は一切出さない**——自社の方針を自己記述するだけに留め、他社データの正確性への言及は行わない |
| **EV(円/月)** | **円は出せない**（信頼シグナル） |
| **工数(h)** | 1.5h（文章作成＋既存ページへの1セクション追加） |
| **EV/h** | 出せない |
| **効き始める時期** | 9月中（優先度は本書群で最も低いが低コスト） |
| **リスク(C8)** | **検討した上で回避した論点**: 「競合のデータは古い／誤っている」と名指しで比較する構成は、`ops/BAR.md`§2-5が確認した事実（高校偏差値.netの年度ラベルのずれ・存在しない値0.64）自体は正確だが、**公開ページで他社を名指し批判する形は景表法・信用毀損リスクの検討コストに見合わないため採用しない**。本施策は自社の方針の自己記述のみに限定する |
| **人間ゲート(C7)** | 不要（ただし公開文言のため、デプロイ前に👤の最終確認を推奨） |
| **保護者到達への影響(G7)** | 無し |
| **kill_criteria** | 該当なし |
| **確信度** | 現状の欠落＝断定。効果＝円換算不能につき評価対象外 |

---

## 1. EV/h 順の一覧表（本群12案）

| 順位 | ID | 施策名 | EV/月（中央値目安） | 工数(h) | EV/h |
|---|---|---|---:|---:|---:|
| 1 | S4-1 | `/hogosha`到達3→2（StaticToolEntryLinks 4面追加） | ¥423〜5,293（中位¥1,588） | 2.0 | **¥212〜2,647（中位¥794）** |
| 2 | S4-2 | ブログ共通枠に`/hiyou`リンク追加 | ¥166〜1,660（中位¥332） | 0.5 | **¥332〜3,320（中位¥664）** |
| 3 | S3-2 | 学校ページ・県まとめに収益CTA追加 | ¥3〜8（季節仮定込み¥42〜113） | 0.5 | ¥6〜16 |
| 4 | S4-3 | 距離2の30ルートに収益CTA追加 | ¥13〜35 | 3.0 | ¥4〜12 |
| — | S3-1 | 学校固有の多年度データ描画 | 円換算不可（順位・CTR因果が未検証） | 8.0 | — |
| — | S3-3 | 短縮形をtitle/h1/meta/アンカーに追加 | 円換算不可（未検証） | 1.5 | — |
| — | S3-4 | HighSchool+FAQPage構造化データ | 円換算不可（未検証） | 2.5 | — |
| — | S3-5 | 5県の3年度目データ収集 | 円換算不可（S3-1に従属） | 15.0 | — |
| — | S4-4 | `check:orphans`をCIへ組込 | 円換算不可（保険的価値） | 0.5 | — |
| — | S6-1 | 学校ページに年度別一次ソースURL表示 | 円換算不可（信頼シグナル） | 1.5 | — |
| — | S6-2 | 一次ソース確認履歴を倍率データにも拡張 | 円換算不可（信頼シグナル） | 3.0 | — |
| — | S6-3 | データ検証方針の自己記述 | 円換算不可（信頼シグナル） | 1.5 | — |

**合計工数: 約40h。直接EVを算出できた4案の合計（下限〜上限）: ¥605〜7,006/月。** 残り8案は測定不能・信頼シグナル・保険・従属施策のため直接比較できないが、単体工数はS3-1(8h)とS3-5(15h)を除き全て3h以下。

---

## 2. 時間軸別の4分類

### 8/31までに着手すべきもの（低リスク・低工数・即効果 or 最優先の差別化）
- S4-1（`/hogosha`到達3→2、本群最高EV/h）
- S4-2（ブログ→`/hiyou`リンク追加）
- S3-2（収益CTA追加、最小工数）
- S4-4（`check:orphans`のCI組込、以後の変更全てを守る前提条件）
- S3-1（学校固有の多年度データ描画、本群最大工数のため早期着手が必要）
- S3-3（短縮形、低コストだが1県限定で開始しBAR V-6の検証を並行）

### 9月中
- S3-4（構造化データ、S3-1完了後が望ましい）
- S3-5（5県の3年度目データ収集）
- S6-1（年度別一次ソースURL表示、S3-1と同時実装）
- S6-2（一次ソース確認履歴の拡張）
- S6-3（データ検証方針の自己記述）

### フリーズ前(11/14)まで
- S4-3（距離2の30ルート、EV/h最下位のため他案完了後の余力で実施）
- S3-1・S3-5は「9月中に着手」だが**完了の絶対期限はここ**（構造変更に該当しうるため）

### NEXT_SEASON（判定・効果確認が11月以降または2027年にずれ込むもの）
- S3-1・S3-3・S3-4の効果検証（順位・CTRへの因果は自社未検証のため、11/15冬季節スワップ後、および2027年2月「山」到来後でないと実測できない）
- S3-2の季節仮定込みEV（¥42〜113/月）の実効性確認は11/1の季節スワップ後
- S4-1のEV確定は、S9-1（`ops/proposals/S1-S9-S11.md`所収・Cowork発注のroot_only referer実機検証）の結果が出てから

---

## 3. 円が出せなかった案の一覧と理由

| ID | 理由 |
|---|---|
| S3-1 | 多年度データの掲載が検索順位・CTRへ与える因果は自社未検証（`ops/BAR.md` V-5/V-6と同型）。価値はB-4必須ゲートの充足・差別化因子の強化であり、円ではなくTIER X/Ω文脈（👤判断領域）に効く |
| S3-3 | 短縮形追加の順位・CTRへの因果は自社未検証（`ops/BAR.md` V-6そのもの）。クエリ側のギャップ（表示23倍・クリック76倍）は断定できるが、施策効果への転写は仮定 |
| S3-4 | 構造化データの順位影響は自社データから因果分離不能（`ops/BAR.md` V-5と同一） |
| S3-5 | S3-1に従属する施策で、独立したEVの発生源ではない |
| S4-4 | 現状オーファン0（実害なし）のため、これは将来のリグレッション予防という保険的価値であり、孤児発生確率という未知数が無いと期待値計算ができない |
| S6-1 | 一次ソースURLの可視化は信頼シグナルであり、逸失収益として計測する経路が無い（`ops/THREATS.md`が脅威5・8で確立した扱いを踏襲） |
| S6-2 | 同上（一次ソース確認履歴の拡張も信頼シグナル） |
| S6-3 | 同上（データ検証方針の自己記述も信頼シグナル）。加えて競合比較の形は景表法リスクを避けるため採用せず、円換算の対象となる比較優位性の主張自体を行わない設計にした |

**円が出せた4案（S4-1・S4-2・S3-2・S4-3）は、いずれも既存の収益導線コンポーネント（`ParentLeadCTA`/`StaticToolEntryLinks`）を配線するだけの直接施策であり、COEFFICIENTS §1/§2の遷移率、または`ops/DISTANCE.md`が自ら仮置きと明記した内部リンクCTR係数のいずれかに基づく。円が出せなかった8案は、いずれも「検索エンジンの評価アルゴリズムへの因果」または「信頼という非市場財」を含む施策であり、この2種類はこの案件のどの成果物（MONEY/DISTANCE/BAR/DEADWIRE/THREATS）でも一貫して円換算を拒否してきた領域と同じである。**
