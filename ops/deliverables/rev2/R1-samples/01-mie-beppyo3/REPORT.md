# 見本1(単純): 三重県「令和9年度 資料5〈別表3〉」 1頁・3列

## 出典

| 項目 | 内容 |
|---|---|
| 県 | 三重県 |
| 発行元 | 三重県教育委員会(三重県ホームページに掲載) |
| 資料名 | 令和９年度資料５＜別表３＞ 後期選抜の選抜方法の(1)において調査書の「各教科の学習の記録」等により選ぶ人数を募集定員の110%又は120%に設定する高等学校、学科・コースの一覧 |
| PDFのURL | https://www.pref.mie.lg.jp/common/content/001264583.pdf |
| 掲載ページ | https://www.pref.mie.lg.jp/TOPICS/m0045100512.htm |
| URLの確認 | 2026-09-26 に掲載ページを WebFetch で読み、リンク文字列「令和９年度資料５＜別表３＞」の href が /common/content/001264583.pdf であることを確認 |
| 取得日 | 2026-09-21(リポジトリ内の保存ファイル ops/baselines/mie-r9/b001264583.pdf の更新日時) |
| 頁数 | 1頁 |
| PDFのSHA-256 | `0886161e661cc27bacc05539047649b3e760748b548e4072e798f494d44dce92` |
| 難しさ | 単純(3列。ただし縦の結合セル2か所と、点線の列区切りがある) |

元のPDFは改変していない(読み取りのみ)。このフォルダには元PDFのコピーを置かず、頁画像(100dpi)だけを置いた。

## このフォルダの3点セット

| 種類 | ファイル |
|---|---|
| 変換前(PDFの頁画像) | `pdf-page-1.png` |
| 変換後(Excel) | `01-mie-beppyo3.xlsx`(シート: 別表3・題名と注記・出典と凡例) |
| 変換後(CSV・UTF-8 BOM付き) | `別表3.csv` |
| 検算レポート | `REPORT.md`(このファイル) |

## 変換の結果

- シート「別表3」: 18行 × 3列(見出し行を除く)
  - 列: 後期選抜の選抜方法の(1)において調査書の「各教科の学習の記録」等により選ぶ人数 / 高等学校名（全日制課程） / 学科・コース名
- シート「題名と注記」: 表の外の文字 9ブロック

## 変換の方法

- PyMuPDF `find_tables()` で表の外枠・見出し行・結合セルの矩形を取得。
- `find_tables()` の行データは点線の列区切りを拾えず「神戸 普通科」「四日市 普通科・国際科学コース」が1セルに潰れた(見出し行以外の18行中3行)。そのため行は `get_text("words")` の単語座標から作り直した: 見出しセルのx範囲で列を決め、「学科・コース名」列の1語=1行とした。
- 「選ぶ人数」列は find_tables の結合セル矩形(縦に6行・12行)に入る行へ値を引き継いだ。「四日市西」は2行分の結合セルなので、2行目に引き継いだ。
- 表の外の文字(題名・※の注記)は「題名と注記」シートにそのまま入れた。

## 検算: 何を何と照合したか

- **照合の相手**: 変換に使った PyMuPDF とは別のプログラム **poppler の `pdftotext -bbox`(version 23.13.0)** がPDFから読み取った全単語と、その座標。単語は1文字ずつに割り、各文字の中心座標を求めた。
- **(1) セルごとの照合**: 出力したセル1つ1つについて、そのセルを取り出したPDF上の矩形に入る文字を集め、出力セルの文字と**文字の種類と個数**(空白・改行は無視)が一致するかを調べた。
- **(2) 取りこぼしの確認**: 見出し領域にも、どのセルの矩形にも入らなかったPDFの文字を数えた(0なら、頁の文字はすべて出力のどこかに入っている)。
- **(3) 見出しの確認**: 見出し領域の文字が、出力の列名のどこかに現れるかを調べた(列名は複数段の見出しを1行にまとめたため、文字の種類だけを確認)。
- **照合しなかったもの**: 縦の結合セルを下の行に引き継いだ値(PDFには1回しか書かれていないため)。件数は下に書いた。

## 検算の結果

| 頁 | PDFの文字数(pdftotext) | うち見出し領域 | セルに入った文字 | 照合したセル数 | 食い違いセル | どのセルにも入らない文字 |
|---:|---:|---:|---:|---:|---:|---:|
| 1 | 581 | 59 | 522 | 46 | 0 | 0 |
| 計 | 581 | 59 | 522 | 46 | 0 | 0 |

- 照合したセル: 46(表の外の題名・注記ブロックを含む)
- 結合セルの引き継ぎで照合から外したセル: 17
- **食い違い: 0件**
- **どのセルにも入らなかったPDFの文字: 0字**
- 見出し領域の文字のうち列名に現れない文字: 0種
- 陰性対照(検算が誤りを見逃さないかの試験。出力の1セルにわざと誤りを入れて同じ検算を実行): 1文字を別の文字に置き換え → 食い違いが1件増えた / 1文字を重複させる → 食い違いが1件増えた / 同じ行の2セルの値を入れ替え → 食い違いが2件増えた

## この検算で分かること・分からないこと

- 分かること: 出力の各セルの文字が、PDFの同じ場所に書かれている文字と同じであること(写し間違い・欠け・余分・別の行や列への入れ違いの検出)。頁の文字がすべて出力のどこかに入っていること。
- 分からないこと: PDFのテキスト情報そのものが誤っている場合(文字化けしたPDF・画像だけのPDFなど)。この見本では、頁画像と出力を見比べて、テキスト情報が見た目と同じであることを確認した(作業したAIが頁画像を見て行った。見た範囲:全18行と題名・注記)。
- 同じ行の中で文字の並び順だけが入れ替わる誤りは、文字の種類と個数の照合では検出できない(セルの中身が1語か数字の表では起こりにくい)。

## できなかったこと・限界

- 縦の結合セルは「全行に値を入れる」形で平らにした(Excelで絞り込み・並べ替えができるように)。引き継いだセルは灰色の斜体で区別し、検算の対象からは外している(PDFに1回しか書かれていない文字のため)。

## 処理時間(実測)

- 抽出・画像化・Excel/CSV書き出し・検算の実行: 3.1秒(スクリプト `scripts/pdf-table-service/build_sample_mie.py` + `make_sample.py`)。スクリプトを書く時間は含まない。

---
作成: My Naishin 運営 / 生成: `python scripts/pdf-table-service/make_sample.py build_sample_mie ops/deliverables/rev2/R1-samples/01-mie-beppyo3`
