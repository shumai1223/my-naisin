# T-S13A: 倍率データバンクのセルフサーブ商品化（GAUNTLET 8審問 全通過・今季の本命）

- 起票: 2026-08-11 / 優先度: **主食（T-C1と並走）** / 実行主体: loop単独（価格と送信は👤）
- 原典: `ops/VERDICT.md` §2・§3（通過条件12個）／ `ops/proposals/S13.md` §3-A
- 見積: **19〜28h**（A-1〜A-5）＋ 前提条件の是正
- C7人間ゲート: **価格の数字（C1-12）・本番反映・対外送信**のみ

## なぜこれが本命なのか（8審問官が別々の理由でPASSにした）

| 審問官 | PASSの理由 |
|---|---|
| G0 | 配布対象は教委公表値のみ。`competition-rate.ts:28` が「公表値をそのまま転記・独自計算はしない」と型に明記 |
| G1 | loop側19〜28h。👤ゲートは価格と送信のみで9/7までに収まる。価値のピーク＝2月に対し逆算バッファ十分 |
| G2 | **必要な成功回数が6候補中で最小。** 床¥1,000,000 に対し Pro換算 **8.5契約年**／年額ライセンス¥1,000,000なら**1本**（アフィリ換算は98〜222件） |
| G3 | 配布は `GET /api/...` で**新規indexページを1枚も生まない**ため scaled content abuse の構成要件に当たらない |
| G4 | 教委がPDFを削除済で他社が再取得不能な過年度アーカイブは、**時間でしか積めない差** |
| G5 | 匿名化比較で勝つ。「機械可読・出典付き・多年度の学校別倍率」を持つ競合が**1社も存在しない** |
| G6 | `src/data` 読み取りと既存テーブルの範囲で loop 単独完遂可能。課金インフラは本番点火済み |
| G7 | 保護者ファネルに構造的に接続しないため、押し下げも離脱も起こさない |

**買い手の実在が実測で確認できている唯一の案**でもある: 進研ゼミ（＝商談済みのベネッセ）が出している一宮商業の
R6/R7/R8 倍率が自社データと**3年度完全一致**し、出典は**株式会社育伸社**（`ops/BAR.md:229-233`）。
＝**この種のデータをベネッセが第三者から買っている**という直接証拠。

---

## ⛔ 実装順序（fail-closed・これを崩したら不採用に戻る）

```
A-0 前提条件の是正（C1-1〜C1-4, C1-8, C1-9）
      ↓  ★A-1 が緑になるまで A-2 以降を1行も出さない（G1・G2・G5が独立に同じ条件を出した）
A-1 47県の利用条件台帳
      ↓
A-2 配布API（CSV / JSON）
      ↓
A-3 有償境界をティア判定で施行
      ↓
A-4 /developers にデータ商品節
      ↓
A-5 api_hits_daily 日次ロールアップ
```

> 【G3の原文】C1-4 は「A-1 が形式的な作業ではなく、**実弾を1発見つける作業**である」ことの証明である。
> **したがって A-1 を省略・後回しにする実装順序は認めない。**

---

## A-0: 前提条件の是正（着手はここから）

### A-0-1 ★★ 特定商取引法に基づく表記が存在しない（C1-8・**今すでに稼働中の欠落**）

**本番 `/developers` は「月額 ¥9,800〜」＋Upgradeボタンを既に配信しているのに、
`src/` `public/` に「特定商取引」の文字列が0件。** 有料サービスを提供しながら法定表記が無い状態。

- **やること**: `/tokushoho`（等）を新設し、`/developers` と Stripe Checkout の**双方から到達可能**にする
- 記載事項: 事業者名（**親名義**）・所在地・連絡先・価格・支払時期・提供時期・**解約/返金条件**
- ⚠️ **氏名・住所は PII なので loop はプレースホルダで枠だけ作り、実値は👤が入れる**（C7）
- ⚠️ これは S13-A の前提であると同時に、**S13-Aをやらなくても直すべき**独立の欠落

### A-0-2 `/terms` の「無料」記述（C1-9）
`src/app/terms/page.tsx:43` が「本サービスは…**無料**のWebアプリケーションです」と書いている。
有料APIを売る以上、有料サービス条項を追加する。

### A-0-3 レコード単位の出典キー（C1-1）
`CompetitionRateRecord` に `sourceIndex` 等を追加する。
**現状 `sources` はファイル単位**で、`competition-rate.ts:57` 自身が「各recordsは由来を追わない」と認めている。
`fiscalYear` で突合する設計では **tokyo/fukuoka/aomori/gifu の 2,173レコード（17,142の12.7%）が一意に解決できず**、
A-2 が約束する「出典列付きCSV」がこの4県で **C0-②「1データ点1出典」を満たせない**。

### A-0-4 `checkAgainstSubtotal` を実データに走らせる（C1-2）
この検査器は `competition-rate.ts:98` で「DoD『突合テスト』の実体」と自称しているのに、
**実データに対して1度も呼ばれていない**（テストは手書き3行フィクスチャのみ・`src/app` でヒット0）。
→ **47県の実データに対して走らせる不変条件テストを、CSV配布器と同じコミットで入れる。**

### A-0-5 有償エクスポートに混ぜてはいけないもの（C1-3・C1-4）
- **`PREFECTURE_HIGH_SCHOOL_DATA` を1行も混ぜない**（398行すべて非一次・226行が自社推定。E-0で削除済みだが再混入を防ぐ）
- **`src/data/competition-rates/fukuoka.ts` の育伸社・英進館由来レコード（98校 191レコード）を機械的に除外し、
  除外をテストで施行する。** 47ファイルの `sources[].url` を全件ホスト分類した結果、
  商用第三者由来は fukuoka.ts のみ（kyoto=京都府教委ドメイン／okayama=県立高校共同サイトは実質公的）
- ⚠️ **育伸社はベネッセの供給元**。その仕入元を無断で再配布しながらベネッセに売り込むことになる

---

## A-1: 47県の利用条件台帳（★これが緑になるまで A-2 以降を出さない）

各県のデータについて、**再配布してよいか**を一次ソースで確認して台帳にする。

| 列 | 内容 |
|---|---|
| `prefecture` | 県コード |
| `sourceUrl` | 引用元URL（教委の公表ページ） |
| `sourceHost` | ホスト分類（`.lg.jp` / `.go.jp` / `.ed.jp` / 商用 / その他） |
| `redistribution` | `ok` / `ng` / `unknown` ← **unknown は商品に載せない** |
| `evidence` | そう判断した根拠（利用規約の該当文言・問い合わせ回答の要旨） |
| `verifiedAt` | 確認日 |

**既に持っている材料**（再取得しない）:
- 三重県教育委員会「**出典を明記のうえ紹介いただくことについては差し支えございません**」（2026-08・Gmail）
- 岐阜県高校教育課からも回答あり（リンク不可・引用可否は要再確認）
- 46/47県が公的ドメインのURLを引用済み（119本）

⚠️ **「公表されているから再配布してよい」とは限らない。** 各県の利用規約・著作権表示を実際に開いて確認する。
判断できない県は `unknown` にして**商品から外す**（C8 fail-closed）。47県揃わなくてよい。

---

## A-2: 配布API

```
GET /api/competition-rates/csv        全量（出典列付き・BOM付きUTF-8）
GET /api/competition-rates/{code}     県別JSON
```

- **A-1で `redistribution: 'ok'` の県のみ**を出す
- 各レコードに**出典URLと確認日**を付ける（A-0-3 の `sourceIndex` が前提）
- **新規のindexページを1枚も作らない**（G3のPASS理由がこれ。HTMLページ化しないこと）
- 既存の `/api/naishin/csv` と同じ配布パターンを踏襲

---

## A-3: 有償境界をティア判定で施行（C1-7・**ここが「👤なしで売れる」の成否を分ける**）

**現状の問題**: `src/lib/api-tiers.ts:83` の pro（¥9,800）は `dataLicense: false`、
`:96-97` の scale のみ `dataLicense: true` かつ `monthlyPriceJpy: -1`（個別見積り）。
→ **「¥9,800を払っても倍率データは買えない」。**

これを直さないと、**「👤の介在なしに買える」は偽のまま**で、律速は候補2・3と同じ👤の対外交渉に戻る。

**2つの解き方（どちらかを選ぶ・👤の価格判断が要る＝C1-12）**:
- (a) pro（¥9,800）に `dataLicense: true` を与える → **セルフサーブで売れる。8.5契約で¥1,000,000**
- (b) scale に価格の数字を入れる（`STRIPE_PRICE_SCALE` の設定＝👤）

⚠️ **価格の数字は C7。loop は「どちらの解き方を採るか」の選択肢と実装だけ用意し、数字は👤が入れる。**

---

## A-4: `/developers` にデータ商品節（C1-5・C1-6）

**C1-5: 訴求の芯を「継続アーカイブ」に置く。**
「42/47県 × 複数年度の継続アーカイブ（教委がPDFを削除済で再取得不能な過年度・saga等はWaybackにも無い）＋今後の年次追加」。
⚠️ **直近1〜3年度の単年スナップショットを主商品にしたらFAILに転落する**
（進研ゼミ＝育伸社購入で3年度完全一致＝**直近年度は買えば埋まる**ことが自分の証拠で証明済み）。

**C1-6: 不足を3つ明記する。** 隠すと購入後に発覚して即解約になる。
1. **公立のみ**（私立は含まない）
2. **`finalRate`＝最終応募倍率のみ**。合格者数・実質倍率は無い
3. **年度は平均3.0年度**（県により1〜5年度）

あわせて: サンプルCSV（10校ぶん）＋仕様書1枚。

---

## A-5: `api_hits_daily` 日次ロールアップ（C1-10）

**なぜ要るか**: 現状 `logApiHit()`（`src/app/api/mcp/route.ts:826`）の実体は `console.log` で永続化先が無く、
`api_usage` の INSERT はキー有りのみ（`src/lib/api-keys.ts:160`）＝**匿名のAPI/MCP利用は原理的に1行も記録されない**。
→ **営業材料の分子が存在しない。** 「月◯◯回使われています」が言えない。

**⛔ 実行順序を厳守（C1-10・今日の事故と同型を防ぐ）**:
```
① バックアップ
② migration適用
③ SELECT name FROM sqlite_master WHERE name='api_hits_daily' で存在確認
④ その後に書き込みコードをpush
```
**②③を飛ばして④をやると、集計は空にフォールバックする一方でINSERTが毎回失敗し、
新規記録が黙って捨てられる**（2026-08-11 に実際に起きかけた）。

---

## A-6: セルフサーブ年額プラン（商談を不要にする経路）★2026-08-11 追加

### 決定事項（👤・2026-08-11）
**契約主体は「親権者名義」。** 選択肢（親名義／親の同意付き本人名義／法人化／民法6条の営業許可＋未成年者登記）
のうち、**既存の口座と確定申告の建て付けと整合する**ことを理由に親権者名義を採用する。
→ **特商法表記（A-0-1）の事業者名も親権者名義**で統一する。
→ 契約文草案 `docs/data-license-agreement-draft.md` の当事者欄も同様。
⚠️ **loop は氏名・住所を記録しない（PII・C7）。枠だけ作り、実値は👤が入れる。**

### なぜ要るか
年額ライセンスは通常「商談 → 見積 → 稟議 → 契約 → 請求」で数週間かかる。
**👤の対外アクション回数が今季の律速**（ユタ不在14日・週数時間）なので、
**商談を経ずに買える道を1本用意しておくと、取りこぼしを拾える。**

⚠️ **これは「商談を消す」施策ではない。** ベネッセ規模の相手は与信・稟議・法務レビューが
組織のプロセスとして固定されているため、必ず人と話したがる。
**セルフサーブが効くのは小〜中規模で意思決定が速い相手**（模試会社59社・教育出版50社・EdTech36社の下位層）。

### 日本の法人購買で商談が発生する5つの理由と、その潰し方

| # | 理由 | 潰し方 | 主体 |
|---|---|---|---|
| ① | **価格が分からない**（現状 `api-tiers.ts:97` が `monthlyPriceJpy: -1`＝個別見積り） | **価格を公開する。**稟議書に「¥XXX,XXX/年」と書けるようにする。**これが最大の障壁** | 👤（数字）→ loop（実装） |
| ② | **カード決済しかない** | **請求書払い・銀行振込に対応。**法人経理はカードを嫌う。Stripeは日本で銀行振込／請求書発行に対応しているので**設定の問題**（要確認） | loop（実装）＋👤（Stripe設定） |
| ③ | **契約書が要る** | **利用規約に再配布権の条項を入れ、クリックラップで成立させる。**`docs/data-license-agreement-draft.md` の内容を規約に落とす | loop（起案）→👤（レビュー） |
| ④ | **稟議に出す材料がない** | **稟議セットをダウンロード可能に**: 仕様書1枚／サンプルCSV（10校）／**不足3点の明記**（C1-6）／更新頻度／出典一覧 | loop |
| ⑤ | **見積書が要る** | Stripeの見積機能、または `/developers` からセルフで見積を出せる導線 | loop |

### 実装順序
```
1. A-1〜A-2 完了（売り物が外に出る）        ← 無いと全部意味がない
2. 価格を決める（👤・C7）                  ← ②の前提。ここが律速
3. pro に dataLicense を付ける or 年額プラン新設
4. Stripe に年額プランと請求書払いを設定（👤）
5. 利用規約に再配布権の条項（クリックラップ）
6. /developers に稟議セット
7. セルフ見積の導線
```
**loop単独でできるのは 1・3・5・6・7。2と4が👤。**

### DoD
- 価格が `/developers` に**数字で**表示されている（「個別見積り」ではない）
- 年額プランがStripeに存在し、**銀行振込／請求書払いが選べる**
- 利用規約に再配布権の範囲が明記され、購入フローで同意が取れる
- 稟議セット（仕様書・サンプルCSV・不足3点・更新頻度・出典一覧）がダウンロードできる
- **一連の流れを👤が実際に通しで試して、誰とも話さずに購入完了できることを確認**（レッドチーム検証）

---

## C1-11（G7）: 触ってはいけない場所

**実装を `/api`・`/developers` 配下に閉じる。**
Consumer向けページ（`ResultSection` / `HensachiResultFlow` / 学校ページ）に**一切触れない**。
G7のPASS理由がこれなので、触った時点で判定が変わる。

---

## DoD（8/31時点）

1. A-1 の台帳が緑（`unknown` の県は商品から除外され、除外がテストで施行されている）
2. `GET /api/competition-rates/csv` と `/{code}` が本番稼働
3. 無償版／有償版の境界がティア判定で施行されている
4. `/developers` にデータ商品節＋サンプルCSV＋仕様書1枚。**不足3点が明記されている**
5. `api_hits_daily` 日次ロールアップ稼働
6. **特定商取引法に基づく表記が公開され、`/developers` と Stripe Checkout の双方から到達可能**
7. `tsc --noEmit` exit 0 ／ `jest` 全green

## kill_criteria

- A-1 で `redistribution: 'ok'` が **10県未満** → 商品として成立しないため中止し、👤に報告
- 育伸社由来の除外後に残るレコードが **50%未満** → 同上

## 進捗（2026-08-12・loop）

**A-0-1・A-0-2 完了・push済（`1b1cdcd`）。**

- A-0-1: `/tokushoho`（特定商取引法に基づく表記）を新設。`/developers`・`/terms`・
  Stripe Checkout（`custom_text[submit][message]`にMarkdownリンク）の3方向から到達可能。
  事業者名・所在地等のPIIはプレースホルダ（「準備中」）のみ・実値は👤が入力（C7）。
  価格欄は`TIER_POLICIES`/`formatTierPrice()`から動的算出（ハードコード数値の二重管理を避けた）。
- A-0-2: `/terms`の「本サービスは…無料のWebアプリケーションです」という断定を、
  有償API/データプラン（`/developers`）の存在を踏まえた記述に修正。
- 新規ページ追加に伴う既知の罠（rich-results-*系4テストのEXEMPT_ROUTES・
  page-registry.tsのsitemap登録簿）を自己検知・自己修正済み。
- tsc実exit0・フルjest 255 suites 4366 tests green。

**残り（A-0-3・A-0-5・A-1〜A-6）は次回セッションへ。**

### ✅ A-0-4は追加コード不要で実質完了（2026-08-12・実データ棚卸し済み）

A-0-4本文の「`checkAgainstSubtotal`は実データに一度も呼ばれていない（テストは手書き3行フィクスチャのみ・
`src/app`でヒット0）」という記述は、**検索範囲が`src/app`限定だったための誤認**と判明した。
実際には`src/data/competition-rates/__tests__/`配下の**45/47県のテストファイルが実データ
（`XXX_COMPETITION_RATES.records`）に対して`checkAgainstSubtotal`を呼び出し、公式PDFの
グランドトータル・学校単位「計」行との完全一致を検証済み**（`grep -c officialSubtotals:`と
テストファイルの`checkAgainstSubtotal`呼び出し回数を全47県で機械的に突合して確認）。
残り2県（gunma・hokkaido）は`officialSubtotals: []`（原資料PDFに「計」行自体が印字されていない）
のため検証対象が存在せず、これはギャップではない。**A-0-4はDoDを満たしており次回以降の
主食候補から除外してよい。**

### ⚠️ A-0-5の前提「fukuoka.tsは育伸社・英進館由来」は実態と食い違う可能性（要検証・機械的除外は保留）

A-0-5本文は「fukuoka.tsの育伸社・英進館由来レコード（98校191レコード）を機械的に除外」と指示するが、
**既存の`fukuoka.test.ts`を読むと、この98校191レコードの検証は`officialSubtotals`（教育委員会公式PDF
=`pref.fukuoka.lg.jp`のグランドトータル・県立quota22,200/applicants22,854・市組合立quota2,120/
applicants2,350）との完全一致テストであり、育伸社（ikushin.co.jp）・英進館（eishinkan.net）は
`fukuoka.ts`冒頭コメントを読む限り「学科別内訳の裏取り（cross-check）」用途で使われているに
留まる可能性が高い**（例: 「英進館北九州地区...の学科別内訳の裏取りに使用」）。

`sources[]`が**ファイル単位**（A-0-3の指摘どおり）のため、個々のレコードがどの資料に由来するか
現状は機械的に判別できない。**「育伸社・英進館由来」と断定して98校191レコード＝ほぼ全量を
除外することは、実際には教育委員会公式PDFで裏付けが取れている正当なデータまで誤って
排除するリスクがある**（逆に、本当に商用資料が主要な情報源になっている一部レコードを
見逃すリスクも残る）。

### ✅ 2026-08-12追記: A-0-3のsourceIndexバックフィルでA-0-5の真相が判明（訂正）

fukuoka.tsのR8（令和8年度・現行年度）分にsourceIndexをバックフィルした際（`cb8dde5`）、
レコード単位の出典が判明したことで上記の懸念は**部分的に誤りだった**と訂正する。
実際の provenance は年度によって全く異なる:

- **R8（現行・191件）**: 90校中48校（99レコード）は英進館の「一括引用元」記事が学科別内訳の
  実際の主要典拠、残り42校+市組合立8校（92レコード）は県教委公式PDFが主要典拠
  （英進館は学科別内訳の裏取りのみ）。**混在。**
- **R7（170件）**: 県教委公式PDF（2026-08-08時点404のためWayback Machine経由で取得）が
  唯一のsource。**商用サイトは一切関与していない。**
- **R6（191件・98校）**: `fukuoka.test.ts`に明記の通り「**県教委原本はWayback未クロールのため
  育伸社の県全体版PDFを代替採用**」＝**このR6データセットの唯一の情報源が育伸社
  （ikushin.co.jp）である。** A-0-5本文が指していた「育伸社・英進館由来レコード
  （98校191レコード）」は、**このR6データセットそのものと数値が完全一致する**
  （98校・191レコード）。**つまりA-0-5本文の指摘は、R6データセットに関しては正確だった。**

**訂正した結論**: A-0-5の機械的除外は、**R6（fiscalYear: '令和6年度（2024年度）'）の191
レコード全件を対象にすれば正確に実行できる**（`sourceIndex: 8`＝育伸社ソースのレコードを
フィルタする、または単に`fiscalYear === '令和6年度（2024年度）'`で絞り込む）。
R7・R8は原則として公式PDFが主要典拠のため除外不要（ただしR8の48校99レコードは英進館が
学科別内訳の主要典拠である点を出典表記に反映する必要がある＝A-2実装時に検討）。

**対応**: 機械的除外は今回は実行せず、[[loop-question-note]]に事実関係の食い違いを記録した。
次にA-0-5へ戻るセッションは、①`fukuoka.ts`の全コメントを読み直し「英進館/育伸社が
"唯一の情報源"だったレコード」と「教育委員会PDFで検算済みだが英進館で学科別内訳のみ
裏取りしたレコード」を分離できるか再調査する、②分離できないなら育伸社・英進館への
言及があるコメント範囲の学校のみを保守的に除外候補とする、③どうしても切り分けが
つかない場合は👤に判断を仰ぐ、のいずれかで進めること。**A-0-3（レコード単位の出典キー）を
先に実装すれば、この切り分け自体が機械的に可能になる**ため、実装順序を
A-0-3→A-0-5に入れ替える価値がある（本文書「⛔実装順序」はA-0内の順序を規定していないため
矛盾しない）。

### ✅ 2026-08-12完了: A-0-5実装完遂（`d899c3c`）

A-0-3完了後、`CompetitionRateRecord`に`commercialSourceOnly?: boolean`フィールドと
`licensableRecords(file)`ヘルパーを追加。上記「訂正した結論」通り、R6（`fiscalYear ===
'令和6年度（2024年度）'`）の191レコード全件に`commercialSourceOnly: true`を機械的に付与し、
`licensableRecords()`がこれを除外する設計にした。R7（教委公式PDF単独）・R8（現行年度・
英進館は48校99レコードで裏取りに留まらず学科別内訳の主要典拠だが公式PDFとの突合検算済み）
は除外対象外のまま維持（R8の出典表記反映はA-2実装時の課題として持ち越し）。
回帰テスト2件を追加（fukuoka191件除外の厳密検証＋他46都道府県に意図しない除外が
起きていないことの横断確認）。tsc実exit0・フルjest256suites4383tests green。

**A-0（前提条件の是正）はA-0-1〜A-0-5全て完了。残りA-0系はなし。次はA-1（47県利用条件
台帳・進行中・現在mieのみok確定、東京/愛知/兵庫/栃木/島根にGmail下書き設置済み）を継続。**

### 🟡 A-0-3（レコード単位出典キー）: 型・解決関数は実装済み・バックフィル本体は未着手（push済`10a652b`）

`src/lib/competition-rate.ts`に`CompetitionRateRecord.sourceIndex?: number`（後方互換）・
`resolveRecordSourceIndex()`（`sourceIndex`明示 → `fiscalYear`が同一年度に単一sourceしか
マッチしない場合の自動解決 → 複数/0マッチはnull、の優先順）・`countUnresolvedSources()`を実装。

**実データで再計測した結果、本文の「tokyo/fukuoka/aomori/gifuの2,173レコード全件が
出典未解決」という前提は過大だったと判明**: `fiscalYear`が同一年度内で単一sourceにしか
一致しないレコードは自動解決できる。真に手動バックフィル（`sourceIndex`を明示的に
埋める作業）が必要なのは以下の**1,634件**のみ:

| 県 | 総レコード数 | 出典未解決 | 内訳 |
|---|---|---|---|
| tokyo | 944 | 944（全件・残る唯一の対象） | 全5年度とも同一年度に3つの公表資料PDFが存在するため |
| gifu | 409 | **✅0件（2026-08-12・`459a4b6`）** | 各年度の2sourcesが「変更後出願者数」(学校別詳細)と「総括表」(集計のみ)の明確なペアと判明、機械的に確定 |
| fukuoka | 552 | **✅0件（2026-08-12・`cb8dde5`）** | 令和8年度分は各sourceのdocTitleの「一括引用元」学校リストで機械的に確定（副産物としてA-0-5の真相も判明） |
| aomori | 268 | **✅0件（2026-08-12・`7041dc0`）** | 令和6年度分は地域別6分割PDF(東青/西北五/中弘南黒/上十三/下北むつ/三八・全て県教委原本)へ学校名の記載順で機械的に確定 |

この4県以外に同型の曖昧さを持つ県が無いことも横断テストで確認済み（`src/lib/__tests__/
competition-rate.test.ts`の`countUnresolvedSources`系15件・既知値のリグレッションガード）。

**✅2026-08-12完了: tokyo(944件)全件バックフィル完遂(`8eb1119`)。** gifu/fukuoka/aomori
（`459a4b6`/`cb8dde5`/`7041dc0`）に続き、A-0-3の対象4県すべてが完了した。tokyoは令和8年度だけで
3つのPDF（普通科（コース・単位制以外）+島しょ／コース制・単位制・海外帰国生徒対象／専門学科・
総合学科、の学科区分別ファイル分割）があり、これが5年度分（令和4〜8年度）繰り返される計15source
構成だった。各sourceの`docTitle`に明記された学科区分（`department`フィールドの値と対応）と
`fiscalYear`の組み合わせで一意に機械的に割り当て（PDF再読み込み不要）。tokyo.test.tsの既存
assertion3箇所（新島R5=sourceIndex9・新宿単位制R5=10・日比谷R4=12）にsourceIndexを追記して
回帰解消。`src/lib/__tests__/competition-rate.test.ts`の`KNOWN_UNRESOLVED_COUNTS`から
tokyoを削除（空オブジェクトに）し、`countUnresolvedSources()`が全47県で0を返すことを
横断テストで確認済み。tsc実exit0・フルjest256suites4381tests green。

**A-0-3は完全に完了した。次はA-0-5（fukuoka.ts R6分191件の育伸社単独ソース除外）に着手すること**
（A-0-3完了によりレコード単位のsourceIndexで機械的に切り分け可能になった＝上記「訂正した結論」節参照）。

## A-1: 47県利用条件台帳（着手済・push`8fa87dd`）

`src/lib/data-license-ledger.ts`を新設。**全47県を既定`unknown`（C8 fail-closed）でシード**、
`redistributableOkPrefectures()`で"ok"県だけを機械的に抽出できる（A-2はこの関数の結果しか
出さない設計にすること）。

**現時点で確定しているのは`mie`（三重県）のみ**: 既存のGmail証跡（`出典を明記のうえ紹介
いただくことは差し支えございません`）により`redistribution: 'ok'`。被リンクは不可だが
出典明記のうえでの引用・紹介は明示的に許諾されている。

**gifu・fukuokaは意図的にunknownのまま維持**（誤ってokにしない）:
- gifu: 回答内容が被リンク可否のみで、再配布・引用可否には触れていない
- fukuoka: A-0-5（育伸社・英進館由来の疑い）が未解決なため、たとえ県教委から
  再配布許諾が取れても現状の全レコードをそのまま商品に載せられない

**残り44県は完全に未着手。** kill_criteria（`redistribution: 'ok'`が10県未満なら中止）を
満たすには、最低あと9県の一次ソース確認が必要。

**2026-08-12追記(受動確認は低歩留まりと判明・能動的な問い合わせへ切替)**: 東京都・愛知県の
公式サイト著作権ページ（`kyoiku.metro.tokyo.lg.jp/administration/web/rights.html`等）を
直接WebFetchで確認したが、いずれも「無断複製・転用不可、私的使用・引用は著作権法の例外として
可」という**定型の著作権表記のみ**で、**商用データ商品への再配布可否には一切言及がない**
（mie/gifuの回答は「特定の問い合わせに対する個別回答」だからこそ再配布に関する情報が
得られた・生成AIの学習データにもなりうる公開ページの定型文だけでは判断材料にならない）。
→ **A-1の残り44県は、受動的なページ閲覧ではなく能動的な問い合わせ（Gmail下書き→👤送信）で
進める必要があると判明。**

**この日、以下10件にGmail下書きを作成済み**（**送信は👤のみ**）:
東京都（`r-7352800501521611304`）・愛知県（`r7195798074036736633`）・
兵庫県（`r1063960211272106151`）・栃木県（`r-2675291460729885004`）・
島根県（`r-4201360430766765206`）・高知県（`r8032582389933337260`）・
宮崎県（`r-2307275797011926923`）・香川県（`r-7802089159276290729`）・
静岡県（`r2506739895852617040`）・秋田県（`r-7505186791934263034`）。
文面はmie/gifuへの過去の問い合わせ（引用・被リンクの可否を問う一般的な文言）とは別物の
**新規テンプレート**で、「出典明記のうえでの第三者への再提供（データ商品への収録）」の
可否を明示的に問う内容にした。既存の`data/outreach-queue.json`の`kyoiku-*`エントリ
（T-C1由来・22県分のemail確認済み）を連絡先として再利用したが、**このA-1向け下書きは
T-C1のcandidate/queuedとは別の目的**（T-C1=B2Bターゲットリスト構築、A-1=データ再配布許諾の
確認）のため、outreach-queue.jsonには追加していない（スキーマが合わないため）。

**本日のA-1向け下書き累計=10件**（1晩の上限10-15件に到達に近いため、次回セッション以降に
継続すること）。

**次のセッションが進める場合**: ①`data/outreach-queue.json`の`kyoiku-*`エントリ（22県email
確認済み・北海道等は問い合わせフォーム経由）のうち未使用分（fukuoka/ishikawa/fukui/shiga/
saga/kagoshima/okinawa/wakayama/tottori/ibaraki/iwate等）を順に使い、同型の下書きを作成する
（**1晩の下書き上限10-15件は[[fable5-loop-protocol]]のT-C1向けルールと合算した方が安全**）
②返信が来た県から`DATA_LICENSE_LEDGER`を更新する（Gmail検索`in:inbox newer_than:Nd`で
定期確認）③フォーム経由の県（北海道・神奈川・群馬等）はフォーム送信自体が👤ゲートのため、
文面だけ用意して次のフェーズへ回す。

## 時計（`ops/CLOCK.md`）

| 期限 | 到達点 |
|---|---|
| **8/31** | 上記DoD 1〜7 |
| **9/7**（👤の国内最終日） | 👤が「データ提供 or 塾SaaS」の2択を1通で出せる状態。価格決定・契約文レビュー済み・下書き設置済み |
| **11/14**（フリーズ前最終日） | 冬の倍率速報を契約先へ配信できる更新フィードが動いている |

⚠️ **価値が最大になるのは倍率公表期＝2月。契約は遅くとも1月に成立している必要があり、
実質の勝負は 9/7 か 9/23〜10/31。**
⚠️ 新規ドメインの評価遅延（6〜16週）は**この案には効かない**（検索順位を経由しない収益）。
これは S13-A の数少ない構造的な利点。
