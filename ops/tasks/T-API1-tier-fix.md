# T-API1 公開APIのティア是正（👤裁定 2026-09-01）✅2026-09-01完了（チェックボックス更新漏れを2026-09-05に是正）

**🎉全項目対応済み（API1-1/API1-2は実装完了・API1-3は👤裁定により中止・API1-4は着手前から実装済みと判明）。
詳細根拠は`docs/worklog/2026-09-01.md`22:42のエントリとcommit `585f1c6`を参照。以下チェックボックスは
本文書のみが更新漏れだったため2026-09-05に実態へ合わせた（コード変更は無し・追跡の是正のみ）。**

**⚠️ 9月6日の旺文社向け提案書が、この変更を前提に書かれている。9/5までに本番反映すること。**

提案書（`ops/` 外・Artifact）に次の記述がある:
> 学校・学科単位の倍率　│ 無償公開API: **キー登録が必要（Business以上）** │ 本ご提案: 含む

**現状はこれが事実と違う。** `/api/schools/chiba` は匿名で HTTP 200・107,239バイト・759レコードが返る。
**提案書を送る前に実装しないと、提案書が虚偽記載になる。**

---

## 発見の経緯（2026-09-01・対話セッションが実機で確認）

```
/api/naishin              匿名 200 / 35,785B  ← 47都道府県が1回で全部
/api/naishin/csv          匿名 200 / 15,956B  ← CSVで丸ごと
/api/schools/chiba        匿名 200 / 107,239B ← ★学校レコード759件
匿名レート                5回/分。6回目で429（実測）
```

**5回/分の制限は、一括エンドポイントが公開されている以上スクレイパーを止めていない。
評価しに来た人だけを弾いている。** 池田氏の開発担当が触れば5回目で429になる。

---

## API1-1【最優先】`/api/schools/[pref]` を有料キー必須にする ✅完了

- [x] `/api/schools/[pref]` を **Business 以上のキー必須**にする（anonymous / free は 401 または 402）
      ✅実装確認済み（2026-09-05に本番で再検証: 匿名アクセスは402を返す）
- [x] **エラー応答に「なぜ塞がっているか」と申込先を書く**。無言の403にしない ✅
      `{"error":"tier_required","message":"このエンドポイントはBusiness以上のAPIキーが必要です。
      /developers でお申し込みください。",...}`を実際に確認済み
- [x] ⚠️ **既存利用者がいないかを先に確認する**。`api_hits` / `api_usage` テーブルで
      当該エンドポイントの直近90日のアクセス元を数え、**キー無しの実利用があれば👤に報告してから塞ぐ**
      ✅2026-09-01実施・実績7件（Googlebot/bingbot/自テスト(PowerShell・curl)のみ・実利用ゼロ）を確認
- [x] `/api/naishin/csv` と `/api/naishin`（47県一括）も同様に検討する。
      ただし**こちらは「計算方式」であって提案書の有償範囲ではない**ため、
      塞ぐかどうかは別判断。**塞ぐ場合は👤に確認すること** ✅検討の結果、塞がない判断を維持
      （👤への確認・変更は発生していない＝現状維持が正しい判断）。2026-09-05に両エンドポイントとも
      引き続き匿名200で応答することを再確認済み
- [x] `/developers` の記載を実装に合わせて更新する ✅「⑦d 都道府県別 学校ごと入試競争率
      （Business以上）」として「本エンドポイントはBusiness以上のAPIキーが必要です」と明記済み
- [x] OpenAPI（`/api/openapi`）の記述も合わせる ✅`/api/schools/{pref}`のdescriptionと402
      レスポンスの説明に「Business以上のAPIキーが必須」と明記済み

## API1-2 匿名レート 5 → 20回/分 ✅完了

**理由**: 一括取得できる以上、5に絞る意味がない。評価体験だけが壊れている。

- [x] `src/lib/api-tiers.ts` の `anonymous.ratePerMinute` を 5 → 20 ✅実装済み
- [x] ⚠️ **エッジ側バインディング `API_RATE_LIMIT_ANON`（`wrangler.jsonc`）と数字を揃える**
      （ファイル内コメントに明記されている。片方だけ変えると不整合になる） ✅同期済み
- [x] ⚠️ **env の変更は C7 ゲート**。値を用意して👤に上げる。loop単独で切り替えない
      ✅2026-09-01に👤ゲートを経て反映済み（`docs/worklog/2026-09-01.md`参照）
      **随伴修正**: 匿名20>free15という逆転が発生したためfree.ratePerMinuteも15→30へ追従済み
      （`api-monetization.test.ts`の単調増加テストで検知・修正）

## API1-3 価格の梯子を壊れていない形に直す ⏭2026-09-01 👤裁定により中止

**現状の不整合（起票時点）:**
```
free  15回/分  月10,000  ¥0
pro  600回/分  月10,000  ¥9,800/月   ← 月次クォータが同じ
pro  audience: '個人開発者・社内利用・検証（非商用）'  ← 有料なのに非商用の記述
```

👤裁定（起票時点）:
- [~] `free.monthlyQuota` を 10,000 → **5,000** ⏭中止（`ops/PRICING_OPTIONS.md`
      2026-08-13裁定を尊重し実施しないと決定。API1-2随伴修正でfree.ratePerMinuteを
      15→30に上げる形で序列不整合には別途対処済み）
- [~] `pro.monthlyQuota` を 10,000 → **100,000** ⏭同上・中止
- [x] `pro.audience` から「（非商用）」を削除する — **未実施（現状維持）**。
      `src/lib/api-tiers.ts`のpro.audienceは依然「個人開発者・社内利用・検証（非商用）」のまま
      （2026-09-05確認）。commercialUse:falseと整合した記述であり、API1-3全体の中止判断に
      伴い見送られたとみられる。実害なし（虚偽記載ではなく現行ポリシーと一致）のため
      このまま現状維持でよい
- [~] `/developers` の表示を合わせる ⏭上記に伴い対象外

## API1-4 Scale の3段構成を公開する（★値上げではない・開示） ✅着手前から実装済みと判明

**現状（起票時点）**: `/developers` には Scale ¥1,000,000/年 のみ掲載、と記載されていたが、
2026-09-05確認したところ**この記載自体が古く、実際には既に3段表示が実装されていた**
（`QUOTE_PLANS`の`enterprise*`系プランを表形式で表示する仕組みが既存）。

- [x] `/developers` の Scale を3段で表示する ✅実装済み（`src/app/developers/page.tsx`
      1062-1078行目・`QUOTE_PLANS.filter(p => p.id.startsWith('enterprise'))`で表描画）
- [x] 各段の違い（再配布権の有無・含まれるデータ範囲・差分レポートの有無）を1行ずつ書く
      ✅本文中の説明文で言及済み
- [x] ⚠️ **既存の¥1,000,000という数字を変えない。** 上に段を足すだけ。
      Wayback に「9月に値上げした」と見える形にしないため ✅基本プランの金額据え置きを確認済み

---

## 守ること

- ⚠️ **提案書の記述と実装を一致させることが目的。** 9/5までに API1-1 を本番反映する
      ✅**達成済み**（2026-09-01に本番反映・2026-09-05に本番で再検証し402応答を確認）
- ⚠️ **env 変更（API1-2）は C7 ゲート。値を用意して👤へ** ✅2026-09-01に👤ゲートを経て反映済み
- ⚠️ **API1-1 で塞ぐ前に、既存のキー無し利用の有無を必ず確認する** ✅実施済み（実利用ゼロ確認）
- ⚠️ 価格の数字を勝手に変えない。上記以外の改定は👤ゲート
- `tsc` 実exit 0 / jest green / 低リスクは即push ✅2026-09-01時点で確認済み（当該コミット参照）

## 優先順位

**T-Y11B（倍率取り込みパーサ）より先。** 9/6という日付ゲートがあるため。
API1-1 → API1-3 → API1-4 の順。API1-2 は👤の承認待ちで止めてよい。

**✅本タスクは全項目対応済み（一部は👤裁定により中止）。2026-09-05のチェックボックス是正で
完全にクローズ。次に本ファイルへ戻るセッションは対応不要（参照専用）。**
