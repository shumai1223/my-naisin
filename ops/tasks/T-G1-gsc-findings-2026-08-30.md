# T-G1 GSC 精査（2026-08-30）から出た4件

対話セッションが 2026-08-30 14:56 に GSC を全次元で引き直して出た宿題。
**⚠️ この文書の数値はすべて対話セッションが集計したもの。loop は自分で引き直して確認してから動くこと。**
（前科: 第2ラウンドで対話セッションが渡した「私立59.3%」が誤りで、loop の原典確認により 38.8% と訂正された）

再現コマンド:

```
$env:GSC_SITE_URL="sc-domain:my-naishin.com"
node --use-system-ca scripts/gsc.mjs query --dimensions page  --days 28 --limit 25000
node --use-system-ca scripts/gsc.mjs query --dimensions date  --days 120 --limit 500
node --use-system-ca scripts/gsc.mjs query --dimensions page,query --days 7 --limit 25000
```

**⚠️ `--limit` を日数以上にすること。** `--dimensions date --days 90 --limit 90` は
GSC が clicks 降順で返して 90 行で切るため、**低クリック日が落ちて日次カーブが歪む**。
実際これで対話セッションは「8月は加速中」と誤読しかけた（正しくは 7月344c/日 → 8月196c/日）。

---

## 前提として共有しておく発見（作業ではないが判断の土台）

### 断片URL（#付き）は「未回収の機会」ではなかった → バックログから削除済み

```
totals API（無次元）   表示  82,430   クリック 1,900
page 次元の合計        表示 101,057   クリック 1,910
差                    表示 +18,627   クリック   +10
断片URLの表示合計       表示  16,285  ← 差の 87% を説明
```

表示は23%ズレるのにクリックは0.5%しかズレない＝**断片は親の同じ掲載枠の二重計上**。
回収できる15,000表示は存在しない。今後この件を再着手しないこと。

### 校名クエリには 0.8% の天井がある（カニバリではない）

個別校名クエリ（一般語を除く厳格フィルタ・直近7日）:

| 受けているページ | クエリ数 | 表示 | クリック | CTR |
|---|---:|---:|---:|---:|
| ブログ記事など | 4,014 | 5,584 | 45 | 0.81% |
| 学校ページ本体 | 2,328 | 5,969 | 47 | 0.79% |

**どちらが出ても同じ。** 記事から学校ページへ入れ替えても1クリックも増えない。
2〜3位で0クリックの実例: 横浜清風高校 偏差値(25表示/位2.0)、横浜創英高校 偏差値(25/2.0)、
みなと総合高校 偏差値(11/2.0)、東京高校偏差値(19/2.1)、健大高崎 偏差値(20/2.6)、浦和東高校 偏差値(18/2.7)。

原因は Y-0憲法（学校別偏差値・ボーダー推定を永久に出さない）の構造的帰結。**直せない。直したら堀が消える。**

### ただし倍率だけが機能している

学校ページの検索意図別（直近7日・page×query の可視ぶん）:

| 意図 | クエリ数 | 表示 | クリック | CTR |
|---|---:|---:|---:|---:|
| 倍率 | 775 | 3,170 | 37 | **1.17%** |
| 偏差値 | 431 | 680 | 4 | 0.59% |
| 内申／ボーダー | 139 | 314 | 0 | **0.00%** |
| 校名のみ・その他 | 977 | 1,794 | 6 | 0.33% |

→ **T-N1-2（前年度47県の倍率収集・9/08-22予定）と Y-11（冬の倍率速報体制）が本命**であることの実測裏付け。

なお学校ページのクリックの89%・表示の75%は GSC が匿名化する超ロングテールから来ている
（page次元 23,907表示/423c に対し page×query は 5,969表示/47c しか見えない）。
3,089枚のロングテール資産として設計どおりの形。

---

## G1-1 学校ページ: 県別CTRベースラインの記録と、短縮形パイロットの判定日設定

### 状況

`src/app/pref/[code]/school/[schoolCode]/page.tsx` の title は既に倍率前面:

```
`${displayName}の入試倍率・募集人員 | My Naishin`
```

**「倍率をタイトルに出す」は実装済み。追加でやることは無い。**

同ファイルに `SHORT_FORM_PILOT_PREFECTURE_CODES = ['aichi']`（S3-3・165ff14・**08-24 23:11 デプロイ**）。
コード内コメントの根拠: 「短縮形（例:「一宮商業高校」）が正式名（「〜高等学校」）の23倍表示・76倍クリック」。

**判定はまだできない。** GSC 最新日は 08-27 ＝ デプロイ後わずか2〜3日ぶん。実測:

```
aichi        8/14-20 2,950表示 40c 1.36%  →  8/21-27 2,148表示 31c 1.44%   +0.09pt
aichi以外46県 8/14-20 26,527表示 461c 1.80% → 8/21-27 21,759表示 392c 1.74%  +0.06pt
```

差は誤差。コード内コメント自身が「4週間CTRを追跡してから展開県を広げる」と書いている。

### やること — ✅2026-08-30完了(loop)

- [x] 下記の県別ベースライン（28日）を `ops/baselines/school-page-ctr-2026-08-30.md` に保存する
      — **4週間後の比較を後出しにしないため、先に確定させて置く** →
      loopが`scripts/gsc.mjs query --dimensions page --days 28 --limit 25000`を自分で引き直し、
      `/pref/<code>/school/`で正規表現フィルタして47都道府県別に再集計。全体値（表示69,775/
      クリック1,150/CTR1.65%・露出URL2,039件）が対話セッション提示値と完全一致することを確認済み
- [x] `ops/tasks/T-R1-recurring-checks.md` に **2026-09-21 に短縮形パイロットを判定する**項目を追加 →
      週次W-8として追加（判定ルール込み）
- [x] 判定ルールを**先に**書いておく（後から都合よく決めない）:
      `aichi の school ページCTR上昇 − aichi以外46県の同期間CTR上昇 ≧ +0.30pt` なら46県へ展開、
      それ未満なら **展開しない**（パイロットのまま据え置き。撤去はしない）→
      ベースラインファイル・W-8の両方に明記済み
- [x] **それまで 3,089枚の title/description を触らないこと。** 2,039/3,089枚が
      直近28日でインデックスに入り始めた最中で、全枚数の同時変更は評価をリセットする恐れがある →
      本セッションでは触っていない（N5-3aの内部リンク追加のみ・title/descriptionは無変更）

### ベースライン（28日 = 2026-07-31〜08-27 / 全体 69,775表示 1,150クリック CTR 1.65%）

| 県 | 露出URL | 表示 | クリック | CTR | 平均位 |
|---|---:|---:|---:|---:|---:|
| kanagawa | 99 | 6,622 | 76 | 1.15% | 11.9 |
| tokyo | 122 | 6,386 | 55 | 0.86% | 12.1 |
| aichi ★パイロット | 156 | 6,087 | 92 | 1.51% | 9.4 |
| fukuoka | 95 | 4,333 | 81 | 1.87% | 8.8 |
| saitama | 82 | 4,295 | 43 | 1.00% | 10.3 |
| hyogo | 75 | 3,445 | 54 | 1.57% | 9.0 |
| shizuoka | 87 | 2,638 | 36 | 1.36% | 9.6 |
| kyoto | 53 | 2,365 | 56 | 2.37% | 8.7 |
| niigata | 70 | 2,319 | 35 | 1.51% | 8.9 |
| akita | 44 | 1,317 | 9 | 0.68% | 8.9 |
| okinawa | 58 | 1,522 | 16 | 1.05% | 10.0 |
| iwate | 59 | 973 | 7 | 0.72% | 10.9 |
| oita | 38 | 739 | 8 | 1.08% | 8.9 |
| chiba | 14 | 710 | 6 | 0.85% | 10.4 |
| tokushima | 24 | 374 | 4 | 1.07% | 9.2 |

全県ぶんは上記コマンドで引き直して保存すること（この表は表示上位＋G1-4対象のみ抜粋）。

---

## G1-2 `/mie/naishin` が7日で -78%

```
8/14-20  1,931表示 14クリック
8/21-27    422表示  4クリック   ← -78%
```

**単独で落ちている。** 同型の県別ツール（kanagawa +581表示、hyogo +537表示）は伸びているので
サイト全体の変動ではない。

### やること — ✅2026-08-30完了(loop・原因特定)

- [x] `--dimensions page,query --days 28` で `/mie/naishin` のクエリを引き、**どのクエリを失ったか**を特定する →
      失ったクエリは無い。単一クエリ「三重高校」（28日累計imp1,220・pos2.0固定）が突出しており、
      これが変動の主因と判明
- [x] `--dimensions page,date --days 28` で`/mie/naishin`の日次推移を確認 →
      **2026-08-14(imp828・pos2.4)・08-15(imp753・pos2.6)に突発スパイクがあり、それ以外の日は
      通常運転（imp30〜150・pos4〜8）**。8/14-20週の1,931表示のうち1,581(82%)がこの2日間だけで
      発生していた。8/21-27週の422表示はスパイク前(8/10前後)の水準と同程度で、**「通常運転への回帰」
      であって「新たな下落」ではない**
- [x] `--dimensions query,date --days 28` で「三重高校」というクエリ単体の**サイト全体**（全ページ合算）
      の日次推移を確認 → **2026-08-14(imp635)・08-15(imp543)にサイト全体でも同じ2日間だけ突出
      しており、他の日は3〜16表示程度**。平均掲載順位は期間を通じて一貫して2.0（変動なし）。
      **これは自サイトの掲載順位が変わったのではなく、「三重高校」という検索クエリ自体の検索需要
      （インプレッション母数）が2日間だけ外部要因で急増し、その後元の水準に戻った**ことを意味する
      （順位が動いていないのに表示数だけ数十倍に振れるのは、SERP側でなく検索ボリューム側の変動でしか
      説明できない）
- [x] 結論: **サイト側の問題ではない。** 「三重高校」は野球の強豪校として知られる私立高校で、
      8月中旬という時期（全国高校野球選手権大会の時期と重なる）を踏まえると、本サイトの内申点計算
      ページとは無関係な一般層（野球ファン等）による学校名検索が一時的に急増した可能性が高いが、
      検索需要急増そのものの外部要因はGSCデータからは断定できないため「可能性が高い」に留める。
      **確実に言えるのは「掲載順位の劣化ではなく、比較対象週(8/14-20)が一時的なスパイクを含んだ異常値
      だったために-78%という数字が出た」という点**。ページ側の修正・title/meta変更は不要と判断（施策は
      打たない）。git logでのコード変更確認は、掲載順位が一貫して2.0のまま動いていない時点で
      site側要因は考えにくいため省略した（`git log`はこの環境で稀にtimeoutする既知の罠があり、
      今回はGSCデータだけで原因を十分説明できたため不要と判断）

---

## G1-3 米国からの表示 3,990（28日）・CTR 0.10% を M2 の観測対象に追加

```
日本  227,641表示  5,614クリック  2.47%  平均位 7.4
米国    3,990表示      4クリック  0.10%  平均位 6.5
```

**表示全体の1.8%。順位は日本より良いのに CTR が25分の1。** 海外在住の日本語検索にしては多く、
CTR が低すぎる。断定はしない — これは観測項目であって、確定した bot 判定ではない。

### やること — ✅2026-08-30完了(loop・観測のみ・遮断なし)

- [x] `--dimensions country,date --days 90` を引いて、米国表示が**いつから**増えたかを特定する →
      **学校ページのインデックス開始(≒08-02)より前から存在する定常パターンと判明。**
      週次推移: 05-24週146→05-31週927→06-07週1,038→06-14週945→06-21週1,111→06-28週1,115→
      07-05週794→…→08-16週1,032→08-23週509。3ヶ月以上前から500〜1,100/週のレンジで推移しており、
      学校ページ解禁と時期が一致しない。**「学校ページ大量追加によるクローラ流入」という仮説は棄却**。
      CTRも通期0.00〜0.26%で一貫して低く、新しい現象ではない
- [x] `--dimensions country,page --days 28` で**どのページに集中しているか**を見る →
      50ページに分散しているが、兵庫県の学校ページ1件(`/pref/hyogo/school/D128210000157`)が
      28日累計312表示（page次元で可視化できる分）のうち42.0%を占め突出。ただしpage次元の合計は
      country,dateの週次合計よりかなり小さい（GSC匿名化ロングテールにより大半が可視化されない・
      T-G1本体前提の②校名クエリ天井の節と同型の制約）。集中と分散が両方観測され、単一の説明では
      片付かない
- [x] D1 の `affiliate_click` / `leads` に米国由来のものが無いか照合 →
      **実行不能と判明・正直に記録**。`clicks`・`leads`テーブルの`PRAGMA table_info`を実行して
      確認したところ、いずれのテーブルにも国/地域を示す列（country・geo等）が存在しない。
      `ip_hash`はハッシュ化済みで国情報を復元できない。この照合は現状のD1スキーマでは不可能
- [x] **GSCの表示はGoogle側の集計であって自サイトのアクセスログではない。** 遮断は提案しない →
      **結論: 遮断はしない。観測項目として`T-M2-bot-blocking.md`へ記録するに留める。**
      3ヶ月以上安定した低CTRパターン（急増していない）・D1との突合手段が無い、の2点から
      「異常の兆候」と断定する根拠が不足している。`clicks`にcountry列を追加する提案もここでは
      しない（D1 migrationはC7に近い判断でありGSC表示データだけを根拠に追加するのは過剰）

---

## G1-4 秋田・岩手の学校ページ CTR が突出して低い

表示300以上の県のうち最下位2県:

```
akita  1,317表示  9クリック  0.68%  平均位 8.9  (露出44枚)
iwate    973表示  7クリック  0.72%  平均位10.9  (露出59枚)
（全体平均 1.65%。同規模の nagano は 2.64%、miyagi 2.31%、kyoto 2.37%）
```

**平均掲載順位は akita 8.9 と悪くない**（全体平均 9.7 より良い）のに CTR が半分以下。
順位のせいではないので、タイトル・説明文・データの中身のどれかに原因がある可能性がある。

### やること — ✅2026-08-30完了(loop・仮説を実データで棄却・原因不明のまま止める)

- [x] akita / iwate の学校ページを実際に開いて（本番URL）、`overallRate` / `totalQuota` / `totalApplicants` が
      **ちゃんと値を持っているか**を確認する →
      **実装(`getPrefectureSchoolPageData`)を直接実行して確認**（本番HTMLのgrepではなく関数呼び出しで
      裏取り・niigata誤報の教訓を踏まえた手法）。**akita 44校・iwate 59校の全校で
      `overallRate===0`または`totalQuota===0`の学校は0件**。「倍率0倍」description仮説は**完全に棄却**
- [x] `src/data/competition-rates/akita.ts` `iwate.ts` の収録内容 → `quota: 0`のレコードも0件（実ファイル
      grep確認）。akita321レコード・iwate461レコード（複数年度分含む）
- [x] 同じ検査を全47県に広げ、**descriptionに0や空が入る学校が何校あるか**を集計 →
      **全国で28/3,132校（0.9%）が該当**。突出しているのは**福島県（50校中20校＝40%）**で、
      他は山形・長野・京都・兵庫・長崎・大分・鹿児島に各1〜2件ずつの散発。**akita/iwateは0件で
      該当なし**。なお福島のGSC CTRは1.63%（全体平均1.65%とほぼ同水準）で、40%が0値でも
      CTRが突出して低いわけではなく、**この仮説単体ではCTRの低さを説明しきれない**傾向が
      福島の実測からも裏付けられた
- [x] データが無い学校のdescription改善 → **今回は実施しない**（noindex化・削除はしない方針どおり、
      加えて上記の福島の実測がこの施策の効果を裏付けなかったため、実装コストに見合わない可能性が
      高いと判断。改善するなら👤判断＋別タスクとして起票する）
- [x] 上記が原因でなければ**「原因不明」と書いて止める** →
      **G1-4（akita/iwateのCTR低下）自体の原因は不明のまま。** description品質仮説は実データで
      完全に棄却された。掲載順位（akita8.9・iwate10.9）も全体平均9.7と大差なく順位起因説も薄い。
      これ以上の深掘り（SERP実見・競合比較）はloop単独では実行不能（Cowork案件候補）。
      **追加の施策は打たない。**

### 参考: 別件だが同時に見えたもの（着手不要・記録のみ）

- `chiba` は wave1 の index 解禁県なのに露出が **14枚**しかない（tokyo 122 / kanagawa 99 / saitama 82 に対し）。
  同じ波で解禁したのに1桁少ない。G1-4 の調査中に理由が分かれば書く。分からなければ書かない
- **福島県は50校中20校（40%）が`overallRate===0`または`totalQuota===0`**（G1-4の47県横断調査で判明・
  他県は最大でも1〜2件）。原因は未調査（データ収集側の欠測か学校側の実際の募集停止かは未確認）。
  ただしGSC CTRは1.63%（全体平均1.65%とほぼ同水準）でCTRへの明確な悪影響は観測されていないため、
  緊急対応は不要と判断。descriptionの書き方改善やデータ欠測の原因調査をするなら👤判断で新タスク化する

---

## 共通ルール

- ⚠️ **この文書の数値は対話セッションが集計したもの。loop が自分で引き直して確認してから動くこと**
- ⚠️ **`--limit` を必ず日数・URL数より大きく取る**（clicks 降順で切られて歪む）
- ⚠️ **推測・断定を書かない。** 分からなければ「不明」と書いて止め、SERP を見ないと分からない類は
  Cowork 案件として `loop-question-note` に上げる（起動条件＝loop が「不明/保留/見送り」と書いたもの）
- ⚠️ **本番の 3,089枚一括変更をしない**（G1-1 の判定日 2026-09-21 まで）
- ⚠️ **国別遮断・noindex 化・ページ削除は提案止まり。実行は👤ゲート**
- `tsc` は実 exit を確認（パイプ厳禁）／jest green ／低リスクは即 push
