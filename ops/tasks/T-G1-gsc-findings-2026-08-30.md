# T-G1 GSC 精査（2026-08-30）から出た4件

対話セッションが 2026-08-30 14:56 に GSC を全次元で引き直して出た宿題。
**⚠️ この文書の数値はすべて対話セッションが集計したもの。loop は自分で引き直して確認してから動くこと。**
（前科: 第2ラウンドで対話セッションが渡した「私立59.3%」が誤りで、loop の原典確認により 38.8% と訂正された）

再現コマンド:

```
$env:GSC_SITE_URL="sc-domain:my-naishin.com"
node --use-system-ca scripts/gsc.mjs query --dimensions page  --days 28 --limit 25000
node --use-system-ca scripts/gsc.mjs query --dimensions date  --days 120 --limit 500
node --use-system-ca scripts/gsc.mjs query --dimensions page,query --days 7 --limit 25000
```

**⚠️ `--limit` を日数以上にすること。** `--dimensions date --days 90 --limit 90` は
GSC が clicks 降順で返して 90 行で切るため、**低クリック日が落ちて日次カーブが歪む**。
実際これで対話セッションは「8月は加速中」と誤読しかけた（正しくは 7月344c/日 → 8月196c/日）。

---

## 前提として共有しておく発見（作業ではないが判断の土台）

### 断片URL（#付き）は「未回収の機会」ではなかった → バックログから削除済み

```
totals API（無次元）   表示  82,430   クリック 1,900
page 次元の合計        表示 101,057   クリック 1,910
差                    表示 +18,627   クリック   +10
断片URLの表示合計       表示  16,285  ← 差の 87% を説明
```

表示は23%ズレるのにクリックは0.5%しかズレない＝**断片は親の同じ掲載枠の二重計上**。
回収できる15,000表示は存在しない。今後この件を再着手しないこと。

### 校名クエリには 0.8% の天井がある（カニバリではない）

個別校名クエリ（一般語を除く厳格フィルタ・直近7日）:

| 受けているページ | クエリ数 | 表示 | クリック | CTR |
|---|---:|---:|---:|---:|
| ブログ記事など | 4,014 | 5,584 | 45 | 0.81% |
| 学校ページ本体 | 2,328 | 5,969 | 47 | 0.79% |

**どちらが出ても同じ。** 記事から学校ページへ入れ替えても1クリックも増えない。
2〜3位で0クリックの実例: 横浜清風高校 偏差値(25表示/位2.0)、横浜創英高校 偏差値(25/2.0)、
みなと総合高校 偏差値(11/2.0)、東京高校偏差値(19/2.1)、健大高崎 偏差値(20/2.6)、浦和東高校 偏差値(18/2.7)。

原因は Y-0憲法（学校別偏差値・ボーダー推定を永久に出さない）の構造的帰結。**直せない。直したら堀が消える。**

### ただし倍率だけが機能している

学校ページの検索意図別（直近7日・page×query の可視ぶん）:

| 意図 | クエリ数 | 表示 | クリック | CTR |
|---|---:|---:|---:|---:|
| 倍率 | 775 | 3,170 | 37 | **1.17%** |
| 偏差値 | 431 | 680 | 4 | 0.59% |
| 内申／ボーダー | 139 | 314 | 0 | **0.00%** |
| 校名のみ・その他 | 977 | 1,794 | 6 | 0.33% |

→ **T-N1-2（前年度47県の倍率収集・9/08-22予定）と Y-11（冬の倍率速報体制）が本命**であることの実測裏付け。

なお学校ページのクリックの89%・表示の75%は GSC が匿名化する超ロングテールから来ている
（page次元 23,907表示/423c に対し page×query は 5,969表示/47c しか見えない）。
3,089枚のロングテール資産として設計どおりの形。

---

## G1-1 学校ページ: 県別CTRベースラインの記録と、短縮形パイロットの判定日設定

### 状況

`src/app/pref/[code]/school/[schoolCode]/page.tsx` の title は既に倍率前面:

```
`${displayName}の入試倍率・募集人員 | My Naishin`
```

**「倍率をタイトルに出す」は実装済み。追加でやることは無い。**

同ファイルに `SHORT_FORM_PILOT_PREFECTURE_CODES = ['aichi']`（S3-3・165ff14・**08-24 23:11 デプロイ**）。
コード内コメントの根拠: 「短縮形（例:「一宮商業高校」）が正式名（「〜高等学校」）の23倍表示・76倍クリック」。

**判定はまだできない。** GSC 最新日は 08-27 ＝ デプロイ後わずか2〜3日ぶん。実測:

```
aichi        8/14-20 2,950表示 40c 1.36%  →  8/21-27 2,148表示 31c 1.44%   +0.09pt
aichi以外46県 8/14-20 26,527表示 461c 1.80% → 8/21-27 21,759表示 392c 1.74%  +0.06pt
```

差は誤差。コード内コメント自身が「4週間CTRを追跡してから展開県を広げる」と書いている。

### やること

- [ ] 下記の県別ベースライン（28日・2026-07-31〜08-27）を `ops/baselines/school-page-ctr-2026-08-30.md` に保存する
      — **4週間後の比較を後出しにしないため、先に確定させて置く**
- [ ] `ops/tasks/T-R1-recurring-checks.md` に **2026-09-21 に短縮形パイロットを判定する**項目を追加
- [ ] 判定ルールを**先に**書いておく（後から都合よく決めない）:
      `aichi の school ページCTR上昇 − aichi以外46県の同期間CTR上昇 ≧ +0.30pt` なら46県へ展開、
      それ未満なら **展開しない**（パイロットのまま据え置き。撤去はしない）
- [ ] **それまで 3,089枚の title/description を触らないこと。** 2,039/3,089枚が
      直近28日でインデックスに入り始めた最中で、全枚数の同時変更は評価をリセットする恐れがある

### ベースライン（28日 = 2026-07-31〜08-27 / 全体 69,775表示 1,150クリック CTR 1.65%）

| 県 | 露出URL | 表示 | クリック | CTR | 平均位 |
|---|---:|---:|---:|---:|---:|
| kanagawa | 99 | 6,622 | 76 | 1.15% | 11.9 |
| tokyo | 122 | 6,386 | 55 | 0.86% | 12.1 |
| aichi ★パイロット | 156 | 6,087 | 92 | 1.51% | 9.4 |
| fukuoka | 95 | 4,333 | 81 | 1.87% | 8.8 |
| saitama | 82 | 4,295 | 43 | 1.00% | 10.3 |
| hyogo | 75 | 3,445 | 54 | 1.57% | 9.0 |
| shizuoka | 87 | 2,638 | 36 | 1.36% | 9.6 |
| kyoto | 53 | 2,365 | 56 | 2.37% | 8.7 |
| niigata | 70 | 2,319 | 35 | 1.51% | 8.9 |
| akita | 44 | 1,317 | 9 | 0.68% | 8.9 |
| okinawa | 58 | 1,522 | 16 | 1.05% | 10.0 |
| iwate | 59 | 973 | 7 | 0.72% | 10.9 |
| oita | 38 | 739 | 8 | 1.08% | 8.9 |
| chiba | 14 | 710 | 6 | 0.85% | 10.4 |
| tokushima | 24 | 374 | 4 | 1.07% | 9.2 |

全県ぶんは上記コマンドで引き直して保存すること（この表は表示上位＋G1-4対象のみ抜粋）。

---

## G1-2 `/mie/naishin` が7日で -78%

```
8/14-20  1,931表示 14クリック
8/21-27    422表示  4クリック   ← -78%
```

**単独で落ちている。** 同型の県別ツール（kanagawa +581表示、hyogo +537表示）は伸びているので
サイト全体の変動ではない。

### やること

- [ ] `--dimensions page,query --days 28` で `/mie/naishin` のクエリを引き、**どのクエリを失ったか**を特定する
- [ ] 失ったクエリで**いま誰が出ているか**（自サイトの別ページに移っただけ＝カニバリなのか、外部に取られたのか）を
      同じ page×query データで確認する。自サイト内移動なら移動先を書く
- [ ] 三重の入試制度・ページ内容に 8/20 前後の変更が無かったか `git log --since=2026-08-10 -- src/app/mie src/data` で確認
- [ ] 原因が特定できなければ**「不明」と書いて止める。** 推測を結論として書かない
      （SERP を見ないと分からない類なら Cowork 案件として質問ノートに上げる）

---

## G1-3 米国からの表示 3,990（28日）・CTR 0.10% を M2 の観測対象に追加

```
日本  227,641表示  5,614クリック  2.47%  平均位 7.4
米国    3,990表示      4クリック  0.10%  平均位 6.5
```

**表示全体の1.8%。順位は日本より良いのに CTR が25分の1。** 海外在住の日本語検索にしては多く、
CTR が低すぎる。断定はしない — これは観測項目であって、確定した bot 判定ではない。

### やること

- [ ] `ops/tasks/T-M2-bot-blocking.md` に観測項目として追加（**遮断はしない**。まず観測）
- [ ] `--dimensions country,date --days 90` を引いて、米国表示が**いつから**増えたかを特定する。
      学校ページのインデックス開始（≒08-02）と一致するなら、単に新規大量ページへのクローラ/スクレイパ由来の可能性が高い
- [ ] `--dimensions country,page --days 28` で**どのページに集中しているか**を見る。
      1〜2ページに集中していればそのページ固有の事情、全面に薄く分散していれば地理的な実ユーザの可能性
- [ ] D1 の `affiliate_click` / `leads` に米国由来のものが無いか照合（trust_class は M2-2 で実装済み）
- [ ] **GSC の表示は Google 側の集計であって自サイトのアクセスログではない。** GSC の表示数を根拠に
      Cloudflare で国別遮断を提案しないこと（C7 かつ実害が確認されていない）

---

## G1-4 秋田・岩手の学校ページ CTR が突出して低い

表示300以上の県のうち最下位2県:

```
akita  1,317表示  9クリック  0.68%  平均位 8.9  (露出44枚)
iwate    973表示  7クリック  0.72%  平均位10.9  (露出59枚)
（全体平均 1.65%。同規模の nagano は 2.64%、miyagi 2.31%、kyoto 2.37%）
```

**平均掲載順位は akita 8.9 と悪くない**（全体平均 9.7 より良い）のに CTR が半分以下。
順位のせいではないので、タイトル・説明文・データの中身のどれかに原因がある可能性がある。

### やること

- [ ] akita / iwate の学校ページを実際に開いて（本番URL）、`overallRate` / `totalQuota` / `totalApplicants` が
      **ちゃんと値を持っているか**を確認する。0 や undefined が description に出ていれば
      「入試倍率0倍・募集人員0名」のような無価値なスニペットになっている
- [ ] `src/data/competition-rates/akita.ts` `iwate.ts` の収録内容と、`school-page-lookup` が返す
      学校数・欠損率を数える（**lib のコメントではなく実ファイルを数えること**）
- [ ] 同じ検査を全47県に広げ、**description に 0 や空が入る学校が何校あるか**を集計してレポートに書く。
      閾値を決めて、データが無い学校は description の書き方を変える（「倍率0倍」ではなく倍率に触れない形へ）
      — ただし **noindex 化や削除はしない**。判断は👤に上げる
- [ ] 上記が原因でなければ **「原因不明」と書いて止める**

### 参考: 別件だが同時に見えたもの（着手不要・記録のみ）

- `chiba` は wave1 の index 解禁県なのに露出が **14枚**しかない（tokyo 122 / kanagawa 99 / saitama 82 に対し）。
  同じ波で解禁したのに1桁少ない。G1-4 の調査中に理由が分かれば書く。分からなければ書かない

---

## 共通ルール

- ⚠️ **この文書の数値は対話セッションが集計したもの。loop が自分で引き直して確認してから動くこと**
- ⚠️ **`--limit` を必ず日数・URL数より大きく取る**（clicks 降順で切られて歪む）
- ⚠️ **推測・断定を書かない。** 分からなければ「不明」と書いて止め、SERP を見ないと分からない類は
  Cowork 案件として `loop-question-note` に上げる（起動条件＝loop が「不明/保留/見送り」と書いたもの）
- ⚠️ **本番の 3,089枚一括変更をしない**（G1-1 の判定日 2026-09-21 まで）
- ⚠️ **国別遮断・noindex 化・ページ削除は提案止まり。実行は👤ゲート**
- `tsc` は実 exit を確認（パイプ厳禁）／jest green ／低リスクは即 push
