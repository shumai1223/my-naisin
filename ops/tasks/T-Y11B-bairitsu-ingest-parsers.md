# T-Y11B 47県の倍率取り込みパーサを冬までに作る

**着手 2026-09-08。これが不在期間〜9月末の主食。20日で終わらない前提で組んである。**

⚠️ **この文書の数値はすべて対話セッションが集計したもの。loop は自分で数え直してから動くこと。**
**2026-08-31〜09-01 の1日で、対話セッションは仮説を6つ外している**（私立学費DB / A→B→Cの続き /
総合得点34県 / ブログ刷新 / 7年スナップショット / 県固有用語47県）。**この文書も疑ってかかること。**

---

## ★★ 着手前ゲート（半日・これを飛ばさない）— ✅2026-09-01実施・判定=続行（設計修正つき）

**千葉・埼玉・東京以外から3県を選び、令和8年度の公表PDFを実際に開いて次を報告する。**

**実施方法**: 既存の`src/data/competition-rates/<pref>.ts`ヘッダコメントは、Y-6/掛-1収集時に実際にR8
（および過去年度）の一次PDFを開いて記録した転記ログそのもの（一次ソースURL・PDF構造・抽出手法・
グランドトータル突合結果を含む）。新規にサーバへ再アクセスする代わりにこの一次記録を根拠として用いた
（robots.txt尊重・相手サーバ負荷軽減の方針とも整合）。ibaraki・nagano・kagoshimaの3県で判定。

| 県 | 1行=1校×1学科か | quota列 | 機械的にパース可能か（R8） | グランドトータル |
|---|---|---|---|---|
| **ibaraki** | ✅素直な表（くくり募集の波括弧構造なし） | 募集定員(a) | ✅**YES**（pdftotext -layoutで全列抽出成功） | ✅「全日制計」と完全一致 |
| **nagano** | ✅素直な表（4通学区の地区別合計あり） | 全県計欄と対応する募集人員列 | ✅**YES**（pdftotext -layoutで全列抽出成功） | ✅全県計＋4通学区合計と完全一致 |
| **kagoshima** | ✅素直な表（7学区分割・学区合計あり） | 学力検査定員（募集定員－推薦等内定者数） | ❌**NO**（ToUnicodeマッピング欠落・数字のみ抽出可、学校名は全欄空白） | ✅「全日制合計」＋学区合計と完全一致 |

**判定（文字どおり）**: 3県中2県（ibaraki・nagano）でR8はパース可能 → **2/3で続行の閾値を満たす**。

**⚠️しかし年次不安定性が重大**（3県×複数年度=8県年で検証・全て`<pref>.ts`の掛-1追記に実記録あり）:

```
ibaraki: R8=pdftotext成功 / R7=ToUnicode欠落(要ビジョン) / R6=同左 / R5=同左  → 4年中1年のみ機械可
nagano:  R8=pdftotext成功 / R7=ToUnicode欠落(要ビジョン)                    → 2年中1年のみ機械可
kagoshima: R8=ToUnicode欠落(要ビジョン) / R7=同左                          → 2年中0年が機械可
合計: 8県年中2県年（25%）のみ機械的パースが成功。残り75%はpdftoppm+Readツールのビジョン解析が必須
```

**同一県内でも年によってPDF生成方式（埋め込みフォントのToUnicodeマップ有無）が変わり、
今年成功した抽出方法が来年も通用する保証がない。** これは各県の内部システム/年度で不規則に発生する
とみられ、事前にどの県のどの年が機械可能かを予測する手段がない。

**結論（続行するが設計を修正する）**:
1. **続行する**。理由: グランドトータル突合は8/8県年（100%）で成功しており、ビジョン解析込みの
   抽出手法自体はY-6全47県で既に実証済み・高信頼度（初回転記一致率が非常に高い）。
   「パーサ」を諦める理由にはならない。
2. **ただし段階2の実装方針を修正する**: 単純なpdftotext正規表現パーサを前提にしない。
   **「pdftotext -layout抽出を試行 → 日本語ラベル欠落を自動検出（学校名フィールドが空/文字化けか
   機械判定）→ 欠落していればpdftoppmレンダリング＋ビジョン解析にフォールバック」という
   ハイブリッド設計を段階2の前提とする。** 数値抽出（quota等）はToUnicode欠落時も比較的機械抽出
   しやすい（既存ファイルの複数箇所で「数値のみ抽出可」と記録）ため、学校名の対応付けだけが
   ビジョン解析必須の部分になりうる——段階2着手時にこの分離が可能か再検証すること。
3. **quota定義（列の裁定）は年に依らず安定**（同一県なら毎年同じ列を採用してよい）。
   これは`competition-rate-quota-definitions.ts`に一度書けば使い回せる部分であり、
   ビジョン解析が必要な「学校名の転記」とは別の作業単位として扱う。

---

## なぜこれが最優先なのか（測定の結論）

### 事実1: 新しい記録を作っても稼がない

```
直近7日  総クリック 1,910 / 露出URL 2,033本
  上位  1本(/hensachi)  601c = 31.5%
  上位  4本             976c = 51.1%
  上位100本            1,538c = 80.5%
  ★クリック0のURL     1,592本 = 全体の78%
```

**3,532URLのサイトで、実質稼いでいるのは100ページ。**
学校ページは1,797本で423クリック＝**1URLあたり0.24**。記録を足す作業は78%の側を増やす。

**この作業は新しいURLを1本も作らない。既存3,089枚の中身を冬に新鮮に保つ作業である。**

### 事実2: 冬のピークは倍率で決まる

```
「高校 倍率」の季節係数（8月=1.00・Trends実測5年262週）
  11月 2.41 → 12月 2.59 → 1月 12.17 → 2月 24.14
週解像度では 2/07 の週が年間最大
```

学校ページで**唯一CTRが機能している意図が倍率**（倍率1.17% / 偏差値0.59% / 内申・ボーダー0.00%）。

### 事実3: 検知はできたが、取り込みができない

T-Y11 Task A で 2026-09-01 に実装済み:
- ✅ A-2 更新検知（`scripts/check-competition-rate-updates.mjs`・47県フルラン実機確認済み）
- ✅ A-4 自己検算（47/47県・`grand-total-reconciliation-invariant.test.ts`）

**未着手:**
```
A-1 公表日の記録     5/47県  （42県未着手）
A-3 quota定義の構造化 3/47県  （44県未着手）
取り込みパーサ        interim-rate-ingest.ts が1本あるのみ
```

**「公表された」と教えてくれるが、そこから先が手作業。**

### 事実4: 1県ぶんの読解が重い

千葉のヘッダコメントより:
> 列は「募集定員(A)」「募集人員(B)」「志願者確定数(C)」「倍率(C/B)」の4本立て。
> **Aは名目定員、Bが実際に一般選抜で競われる枠**（千葉高・東葛飾高は併設型中高一貫校の
> 内部進学者定員をAから差し引いた数がB＝資料の注記1）。本データはBをquotaとして採用する。

47県すべて違う。埼玉は「入学許可予定者数(A)」、大阪は第2志望クロス集計あり。
**埼玉ではこの読解を誤って4件の転記ミスが発生し、グランドトータル検算で発見された。**

---

## 作業の順序 ★市場規模順ではなく「公表が早い順」

**1月に最初に公表する県から作る。** 遅い県のパーサを先に作っても、早い県で溺れる。

### 段階1: 47県の公表日を確定させる（A-1の残り42県）— ✅2026-09-01完了

- [x] 各県の令和8年度の**実際の公表日**を、既存の `competition-rates/<pref>.ts` の
      ヘッダコメント・一次ソースページから特定する → **44/47県完了**（`competition-rate-publication-notes.ts`）
- [x] 特定できない県は **`unresolved` に「不明」と記録する。推測しない** →
      残る3県（ehime/fukuoka/hokkaido）は`UNRESOLVED_PUBLICATION_DATE_PREFECTURES`に記録
- [x] 確定した日付で**公表カレンダー**を作る（1月中旬〜2月下旬に散らばるはず） →
      実測は2月中旬〜3月下旬に分布（最速akita/gunma2月12日〜最遅fukushima3月19日。「1月中旬〜」
      という当初の見立てより実際はやや遅く、2月中旬〜3月上旬に集中していた）
- [x] **速報版と確定版の両方を出す県**は、両方の日付を記録する → `PublicationTimingNote`は
      各県の資料ヘッダコメントに既にある「変更後」「確定」等の表記をそのまま転記する設計のため、
      複数版がある県（saga等）も既存記述の範囲で対応済み

### 段階2: 公表が早い順にパーサを書く — 🟡2026-09-01: 品質判定ユーティリティまで実装。個別県のパーサ本体はR9データが無く未着手

- [x] **quota定義を裁定して記録する**（`src/lib/competition-rate-quota-definitions.ts` へ追記）
      判断規則は既に確立済み: **quotaは「その県が公表する倍率の分母と一致する列」を採用する** →
      **✅2026-09-01完了・47/47県**（A-3として実施。年度が変わっても列の「意味」は安定する前提だが、
      列の「並び」が変わる可能性は段階2実行時に都度確認すること）
- [x] **テキスト抽出品質の自動判定関数を実装**（ゲートの発見を反映したハイブリッド設計の分岐点）→
      `src/lib/competition-rate-parse-quality.ts`の`assessPdfTextExtraction()`（純粋関数・
      pdftotext生出力のCJK文字比率からビジョン解析フォールバックの要否を機械判定・
      jest 5テストgreen）。**⚠️これは判定ロジックのみ**。PDF取得・pdftotext実行・
      pdftoppm実行そのものはI/Oを伴うため、実際にR9のPDFが公表されてから接続する
- [ ] パーサを実装する（PDF/xlsx/HTMLのどれか）→ ⚠️**この行の「R9データがまだ存在しないので時期尚早」という判断は2026-09-02 16:09に訂正済み。R8で作りR8で検証する（本文書末尾の訂正セクションを見ること）**。旧記述: R9データがまだ存在しない
      （令和9年度の公表は2027年1月中旬以降）ため、実データでの検証ができず時期尚早と判断。
      着手は令和9年度の最初の公表県が出た時点（`ops/BAIRITSU-INGEST-RUNBOOK.md`の「検知」参照）
- [ ] **既存の検算（グランドトータル一致）に接続する** → 接続先は既存の`checkAgainstSubtotal()`
      （`src/lib/competition-rate.ts`・47県で既に使われている再利用可能な純粋関数）で確定済み。
      実際の接続（パーサの出力をこの関数に通す配線）はパーサ本体の実装時に行う
- [ ] **令和8年度の実データでパーサを回し、既存の手作業データと完全一致することを確認する**
      ← これが唯一の正しい検証方法。**一致しなければパーサが間違っている**（パーサ本体の実装後）

### 段階3: 冬の運用手順書 — ✅2026-09-01完了

- [x] 「検知 → パース → 検算 → PR」の手順を `ops/` に文書化する →
      `ops/BAIRITSU-INGEST-RUNBOOK.md`（着手前ゲートのハイブリッド設計の発見を反映）
- [x] 検算が落ちたときに**何を人間に上げるか**を決めておく → 同ファイル「検算が落ちたときに
      何を人間に上げるか」節（転記ミスが特定できる場合はloopが自己修正／原因不明は未解明差分
      として正直に記録／同じ県で3回連続失敗したら質問ノートへ構造変化の疑いを記録）
- [ ] 検算が落ちたときに**何を人間に上げるか**を決めておく

---

## 守ること

- ⚠️ **一致しないデータを合わせにいかない。** 差分は「未解明差分あり」として明示的に記録する
- ⚠️ **画像PDFの県は正直にスキップ台帳へ**（Y-0の「画像PDF県は正直にスキップ」原則）
- ⚠️ **相手サーバに負荷をかけない**（1県1日1回・間隔800ms以上・UAを名乗る・robots.txt尊重）
- ⚠️ **学校別偏差値・ボーダー推定は書かない**（Y-0憲法）
- ⚠️ **本番D1の破壊的操作なし・env変更なし・対外送信なし** = loop単独で完結する
- `tsc` は実exitを確認（パイプ厳禁）／jest green ／低リスクは即push

## 払底したら

**監視モードに落ちる前に、質問ノートへ「どこまで終わって次に何を取るべきか」を書いて止まること。**
[[feedback-avoid-premature-monitoring-mode]]／👤の明示指示「絶対に監視モードに入らせないように」。
**8/30・8/31と2日続けて4時間以上の監視モードが発生している。**

## この作業が終わったら（優先順）

1. **入試日程DB（47県×3年）** — Trends実測: 高校入試 日程 18.22 / 出願期間 11.95
   （調査書40.61・内申点30.35と同一スケール）。1月61 / 2月63 / 12月21 で冬に集中。
   `src/lib/juken-schedule.ts` は現在7KBで「県別日程は検証不能だから持たない」と書いてあるが、
   **47県×3年の実施要項を追える今なら前提が変わっている**
2. **年度スナップショットのR5遡り** — ⚠️ **2026-09-01の検証で下方修正済み**。
   教委は年度ごとにディレクトリを作り替え旧年度を消す。URLパターンからの機械的推測は
   tochigi/yamagata/ehime の全18パターンで404かつWaybackにも無し。**R5は虫食いになる前提で**
3. **my-shingaku のデータ層** — 28日138クリック/59URL＝実質未着手。今期の収益はゼロ・来期以降専用

---

# 🔴🔴 2026-09-02 16:00 【T-Y11B 段階2の指示を訂正】R9を待つな。R8が教材であり検証データである

## 何が起きたか

2026-09-02 00:16 に払底判定。理由はタスクファイルのこの記述:

> パーサを実装する → **未着手（意図的）**。R9データがまだ存在しない
> （令和9年度の公表は2027年1月中旬以降）ため、実データでの検証ができず時期尚早と判断。

**これは目的の取り違えです。** 同じタスクファイルの2行下にこう書いてあります:

> **令和8年度の実データでパーサを回し、既存の手作業データと完全一致することを確認する**
> ← これが唯一の正しい検証方法

**R8のPDFも、R8の手作業データ（21,739レコード）も、両方いま手元にあります。**
パーサは **R8で作り、R8を完全再現できるかで検証する**。

**1月に必要なのは「動くパーサ」です。1月から作り始めるなら間に合いません。**
そもそもこのタスクは「1月に5県同時公表が来たら手作業では追いつかない」から9月にやる、という話でした。
**R9を待つのは、このタスクの存在理由そのものを否定しています。**

## ゲートの発見は正しい。ただし結論が逆

着手前ゲートの発見は重要で、正確です:

```
8県年のうち pdftotext で機械パースできたのは 2県年（25%）
残り75%は pdftoppm + ビジョン解析が必須
同一県でも年によってPDF生成方式（ToUnicodeマップの有無）が変わる
```

**この発見は「待つ理由」ではなく「急ぐ理由」です。**
75%が人手を要するなら、**1月の同時公表はもっと詰みます。**
だから今のうちに「どの県が機械で通るか」を確定させ、通る県は自動化し、
通らない県は半自動のハーネスに載せておく必要があります。

---

## 段階2 やり直し（すべて R8 で完結する）

### 2-a 47県を「機械パース可否」で分類する ★まずこれ — ✅2026-09-02完了・重大な訂正あり

- [x] 47県のR8の公表PDFに対して、**pdftotext -layout を実際に流して**
      `assessPdfTextExtraction()`（09-01実装済み）で判定する →
      `scripts/check-r8-pdf-parse-quality.mjs`として実装・47県中43県が実行完了
- [x] 結果を `ops/baselines/` に台帳として保存する（県 × 判定 × 判定根拠） →
      `ops/baselines/r8-pdf-parse-quality-2026-09-02.md`
- [x] ⚠️ **PDFは既に一度取得している資料。再取得は1県1日1回・間隔800ms以上・
      UAを名乗る・robots.txt尊重**（兵庫は既知の拒否県） → hyogoは再取得せずスキップ、他46県は
      間隔900ms・カスタムUAで1回ずつ取得
- [x] **これ自体が単独で価値のある成果物**。1月に「どの県が自動で通るか」が事前に分かる

**★★重大な訂正: 2026-09-01の「8県年中75%が要ビジョン解析」は`pdftotext`呼び出しのバグだった。**
`-enc UTF-8`を付けずに呼んでいたため日本語ラベルが全て空白化していた（数字は影響を受けないため
「ToUnicodeマッピング欠落」に見えた）。`-enc UTF-8`を付けて元の8県年を再検証した結果、**8/8
（100%）が機械抽出可能**と判明。47県の新規判定でも43/47（91%）が`usable`。詳細と訂正表は
`ops/baselines/r8-pdf-parse-quality-2026-09-02.md`を参照。**段階2-bのハイブリッド設計（ビジョン
解析フォールバックが主）という前提は過剰設計だった可能性が高く、`pdftotext -layout -enc UTF-8`を
第一候補にする設計へ縮小してよい。** 例外4件（fukushima=真のスキャン画像PDF・hyogo=robots.txt
遵守で未検証・okayama=URL切れ・osaka=xlsx形式）は個別対応が必要。

### 2-b 機械で通る県のパーサを書き、R8で検証する ★本体

- [x] **ibaraki(R8)を実装・検証完了（2026-09-02）**。`src/lib/bairitsu-ingest/parse-table-pdf.ts`
      （純粋関数）＋`scripts/bairitsu-ingest/extract-pdf-geometry.py`（PyMuPDF・罫線＋文字座標
      抽出）＋`src/lib/bairitsu-ingest/__tests__/parse-table-pdf.test.ts`（実PDFのfixtureで検証・
      jest 6テストgreen）。**149/149件・順序も含め完全一致**（グランドトータルも公式値と一致）。
      詳細技法は`ops/BAIRITSU-INGEST-RUNBOOK.md`「結合セルの解法」に文書化済み
      - ⚠️**重大な発見**: 学校名セルが複数学科行にまたがる「結合セル」を持つ県では、学校名
        ラベルは結合セル内のどこか1行（先頭行とは限らない・ibarakiでは概ね中央行）にしか
        出現しないため、単純な逐次スキャン（ラベルが出たら以降を新学校として carry-forward）
        では**必ず失敗する**（ラベル出現前の行が前の学校に誤帰属する）。この構造は
        `pdftotext -table`でも解決しない（テキストストリームには結合セルの範囲情報が残らない）
      - **解法**: PDFの罫線（ベクタ図形）から真のブロック境界を機械的に決定する。結合セルの
        内部区切り線は「学校名列をまたがず学科名列より右からしか引かれない」ため、
        罫線のx0だけで判定できる（`PyMuPDF`の`page.get_drawings()`で取得可能）
      - **副次的な罠2件**: ①PyMuPDFの`get_text("words")`はスペース無しで隣接する学校名+
        学科名を1wordとして結合し列を誤判定する→**文字単位（`rawdict`の`chars`）で列を
        振り分けると解決** ②学科名が半角カタカナで抽出されることがある（濁点/半濁点が
        分離文字になる）→`.normalize('NFKC')`で一括正規化できる
      - ⚠️**この方式はPython（PyMuPDF）に依存する**。この環境には導入済み（`pip show pymupdf`
        で確認可能）だが、新規に構築する環境では別途`pip install pymupdf`が必要
- [x] **tochigi(R8)を実装・検証完了（2026-09-02）**。107/107件・完全一致（グランドトータル
      7,259/7,602も一致）。ibarakiとは異なる**もう一方のパターン**であることを確認:
      学校名ラベルは各学校ブロックの**先頭行**にだけ印字され、継続行は単に空欄になるだけ
      （罫線による結合セル判定は不要）。`groupCharsIntoRows()`（罫線を見ずy座標の近さだけで
      行をクラスタリング・汎用）＋`extractRowFields()`＋`assembleSimpleTableRows()`
      （単純carry-forward）を新設し、`parse-table-pdf-simple.test.ts`で検証（jest 6テスト）
      - ⚠️**訂正版の教訓「-table+`\s{2,}`区切りだけで足りる県が多いはず」は誤りだった**:
        tochigiも学校名・学科名が「宇　都　宮」のように1文字ずつ均等割り付け（トラッキング）
        で組版されており、`\s{2,}`のような空白数ベースの列分割は列境界と文字間の空白を
        区別できず機能しない。**座標ベースの列振り分け（文字単位のx座標をどの列の範囲に
        入るかで判定）は「結合セル対策」だけでなく、この均等割り付け対策としても必須**、
        という位置づけに訂正する（`normalizeExtractedText()`に全角/半角空白の内部除去を
        追加して対応・NFKCと同時に適用）
      - tochigiは列数が多い（18列・quota/applicants/rateが学校名・学科名の隣接列とは限らない）
        ため、列の役割をインデックスで明示指定する`GeneralColumnLayout`を新設した
      - 罠: 表の最終行に「合計」（グランドトータル自身）が学校の1行として混入する
        （schoolNameとdepartmentの列境界をまたいで「合」「計」に分裂）→ ibarakiの
        「全日制計」混入と同型の罠。`excludeRow`オプションで除外する設計にした
      - **判定の目安**: 学校名セルの結合有無は`pdftotext -table`の出力を目視すれば分かる
        （ラベルが先頭行にあるか、途中の行にあるか）。結合セルが無ければ
        `assembleSimpleTableRows`（軽量）、結合セルがあれば`assembleCompetitionRateRows`
        （罫線ベース・ibaraki型）を選ぶ
- [x] **akita(R8)を実装・検証完了（2026-09-02）**。78/78件・完全一致（グランドトータル
      6,268/5,237も一致）。ibaraki・tochigiいずれとも異なる**第3のパターン**を発見:
      学校名が長いと2行に折り返され、2行目の断片（例:「情報学院」）がschoolName列に単独で
      出現する（正しくは「大館国際」＋「情報学院」＝「大館国際情報学院」1校）。tochigi型の
      単純carry-forwardでは「情報学院」を別の新しい学校と誤認する。
      - **解法**: この県の表は`№`（通し番号）列を持つため、**№列の有無でブロック境界を
        判定**し、ブロック内の全schoolName断片を連結する`assembleNumberedBlockRows()`を
        新設した（`roles.number`を指定）
      - ⚠️罠: 「県北計」「中央計」「県南計」「県合計」等の小計行は独立した№を持たないため
        直前の学校ブロックに紛れ込む。ただし小計ラベルは**department列**に出現する
        （schoolName列ではない）ため、除外判定はブロック単位ではなく**行単位**
        （`excludeRow`はdepartment文字列で判定）にする必要があった。「県合計」（総計）に
        到達したら`stopAt`でそれ以降の処理を打ち切る（この県は全日制表の後に別の小さな
        表（連携型選抜等）が同型の小計構造で続いており、打ち切らないと誤って混入する）
      - ⚠️罠: 「太田分校」「雄勝校」のような**独立した番号を持たない分校**は、ブロックの
        連結名にそのまま混ぜると「大曲農業太田分校」のような誤った名前になる。実際の
        既存データは「大曲農業(太田分校)」という括弧付きの人手による命名規則を採用して
        いるため、これは自動検出できない**県固有の例外**として`renameOverrides`
        （schoolName生テキスト→差し替え後の名前）で個別対応する設計にした
      - **教訓**: №列を持つ表に遭遇したら、まず「学校名が2行に折り返される長い名前が
        無いか」を目視確認すること。無ければtochigi型（単純carry-forward）で足りるが、
        あればakita型（№列ブロック＋連結）が必要
- [x] **tokushima(R8)を実装・検証完了（2026-09-02・第4パターン「複数列組ページ」を確立）**。
      詳細は下記「🔍調査のみ実施」節のtokushima項目を参照（そこに完全な経緯を残した）
- [x] **ishikawa(R8)を実装・検証完了（2026-09-02・列roles一般化＋罫線抽出バグ修正）**。
      70/70件・完全一致（グランドトータル6,566/6,076も一致）。
      - **根本原因の特定と修正**: 当初「行検出（罫線ベースの`isBlockEnd`判定）自体が
        破綻」と見えたが、真因は`extract-pdf-geometry.py`の矩形罫線フィルタ
        `r.height < 1.0`が**height=1.2の実在する罫線を取りこぼしていた**バグだった
        （表内容のセル背景矩形はheight/widthが同オーダーなのに対し、罫線は幅に比べ
        高さが極端に薄いことを利用し、閾値を`< 3.0`へ緩和して解決）。この修正で
        ishikawaのhline検出数が104→131件（page1）に増え、行の欠落が解消した。
        **この抽出スクリプトのバグは他の県にも影響しうる根本的な問題だったため、
        今後すべての県で恩恵を受ける**（ibaraki/tochigi/tokushimaは既存の凍結
        フィクスチャを使うため無回帰）
      - `parseTablePdfPageRows`に`roles`（quota/applicants/rateが学校名・学科名に隣接
        しない列構成に対応）と`excludeRow`オプション（学校単位の「小計」行を除外）を
        追加した（前者は前回の周回で追加・後者は今回追加）
      - ⚠️罠（併願制度）: 小松・金沢泉丘・七尾の3校は「普・理併願」という学科横断の
        出願を持ち、複数の学科行を1つの「小計」行に統合した単一レコードとして既存
        データに記録されている。パーサの出力からは正しく分離できないため、既存データ
        （WebSearch裏取り不要・PDF上の小計行の数値をそのまま転記したもの）を根拠とした
        明示的な置き換えで対応した（tokushimaの那賀/海部と同型の対応）
      - ⚠️罠（括弧の全角/半角）: ishikawaのPDFは「普通(キャリア)」のように**半角括弧**を
        使うが、既存データは一貫して全角括弧「（キャリア）」で表記する。
        `normalizeDepartmentText`に半角→全角括弧の変換を追加した（tokushimaの全角括弧
        データには影響しない安全な変換であることを確認済み）
- [x] **gunma(R8)を実装・検証完了（2026-09-02）**。tochigi型（y座標クラスタリング・単純
      carry-forward）で106/106件・完全一致（機械集計グランドトータルquota11,001・
      applicants10,698も既存coverage.noteと一致）。新たに発見した3つの罠:
      ①**department列内で「カテゴリ＋副部分」が同一行に離れたx位置で印字される**（例:
      「動物科学」+ギャップ+「資源動物」）が、**プレフィックスの有無は行ごとに不規則**
      （勢多農林「応用動物」行はプレフィックス無し単独、利根実業「建設」行も同様）で
      座標だけからは合成規則を一意に決定できない→学校名+生テキストをキーにした
      `GUNMA_DEPARTMENT_OVERRIDES`（15件）で個別対応。②**department列に同一ラベルが
      前半/後半で連続して2回印字される行がある**（前橋清陵・沼田・高崎経済大学附属の
      代表課程「普通」行のみで発生・理由不明）→`collapseRepeatedLabel()`（自己反復文字列を
      半分に畳む汎用関数）で解決。③**quotaA列の括弧付き注記「(255)」が学校名列との境界
      x=179をまたいで印字され、先頭の「(2」が学校名列に誤って混入し既存の学校名の
      carry-forwardを破壊する**（市立太田の内部進学者注記で発生）→学校名列の右端境界を
      179→160へ絞ることで解決（実際の学校名テキストはx0=85.9起点で最長8文字でも
      x1<140のため、160への絞り込みは学校名の誤検知を起こさない安全な調整と確認済み）。
      `parse-table-pdf-gunma.test.ts`で検証済み（jest6テストgreen）。tsc実exit0・
      jestフル375suites6475tests green。**gunmaは実装完了**
- [x] **nagasaki(R8)を実装・検証完了（2026-09-02）**。公表が早い順（2/6締切・全未着手県中最速）で
      選定。tochigi型（y座標クラスタリング・単純carry-forward）で116/116件・完全一致（機械集計
      グランドトータルquota7,288・applicants5,794も既存noteと一致）。唯一のくくり募集（長崎東
      「普通・国際」）は既存データの「（くくり募集）」付与のみdepartmentOverrideで対応・他は
      raw PDFの表記が既存データとそのまま一致した（parensも含め素直）。
      - ⚠️**新しい罠を発見**: 学校別小計「計」行とグランドトータル行（県立計/市立計/総計）の
        除外を`.includes('計')`（部分一致）で実装したところ、正当な学科名「会計ビジネス」
        （「計」の字を含む・佐世保商業）が誤って除外され1件欠落した（115/116）。**「計」を
        含む部分一致でなく、学校別小計はdepartmentの完全一致（'計'単体）、グランドトータルは
        schoolNameの完全一致（'県立計'/'市立計'/'総計'の3値）で判定する必要がある**（他県
        でも同型の罠が起こりうる: 「合計」を含む正当な学科名は考えにくいが「計」は「会計」
        「統計」等の熟語に頻出するため要注意）。
      - PDFはpdftotext -layoutでは列がひどく乱れたが（gunmaと同型の罠）、PyMuPDF座標抽出＋
        y座標クラスタリング（罫線不使用）で正しく行を再構成できた。
      `parse-table-pdf-nagasaki.test.ts`で検証済み（jest4テストgreen）。tsc実exit0・
      jestフル378suites6496tests green。**nagasakiは実装完了**
- [x] **kanagawa(R8)は調査のみ・見送り（2026-09-02）**。公表が早い順（2/9）で候補にしたが、
      PDFが13〜14頁・20以上のセクション（普通科/クリエイティブスクール/専門学科10区分/単位制
      多数/定時制/通信制/特別募集）に分かれ、うち「2 連携募集」は本資料に変更後数値が無く
      （「連携募集は志願変更を行いません」）別資料（初回志願状況PDF）が別途必要と判明。
      費用対効果が低いため見送り、より軽量な県を優先した（コード変更なし）
- [x] **ehime(R8)を実装・検証完了（2026-09-02）**。kanagawa見送り後、既存ファイルの「全Nページ」
      記述から最軽量の候補（ehime/wakayama=全1ページ）を選定。tochigi型（y座標クラスタリング・
      単純carry-forward）だが**1ページ2段組**（左右2組の学校リストが横並び）という罠があった。
      同一クラスタ化行に対しLEFT用/RIGHT用2つの`GeneralColumnLayout`を順に適用するだけで
      ジオメトリの事前x分割は不要と判明（`extractRowFields`が自レイアウトの境界外文字を
      自然に無視するため）。99/99件・完全一致（グランドトータルquota8,370・applicants7,468も
      「合計」行と一致）。
      - ⚠️**新しい罠1**: 「合計」の2文字がRIGHT_LAYOUTのschoolName/department列境界をまたいで
        分裂し（schoolName="合"・department="計8,"）、`excludeRow`の完全一致判定
        （`department === '合計'`）をすり抜けて誤ってレコード化された（quota=3707という
        巨大な異常値で発覚）。**nagasaki型の完全一致判定は「集計行が列境界をまたいで分裂しない」
        前提でしか機能しない**→gunma/ibarakiで確立していた`(schoolName+department).includes('合計')`
        という部分一致判定に戻す方が安全（nagasakiの「会計ビジネス」のような、`合計`という
        連続した2文字を含む正当な学科名は稀）。**完全一致か部分一致かは「集計ラベルが列境界を
        またいで分裂する可能性があるか」で選ぶこと**（両者はトレードオフであり、単純に
        「完全一致の方が安全」ではない）
      - ⚠️**新しい罠2（分校の学校名合成・akita型renameOverridesの学校名版）**: 本校/分校が
        独立した学校番号を持たない3校（松山南・松山北・内子）で、raw PDFは「親校名+本校」を
        1語で連結（例:「松山南本校」）する一方、分校側は親校名を伴わない単独の地名のみ
        （例:「砥部」）を印字する。既存データは「親校（分校名）」に統一表記するため、
        raw schoolNameそのものを直接書き換える`EHIME_SCHOOL_NAME_OVERRIDES`（6件）で対応した。
        分校名（砥部/中島/小田）はcarry-forwardの通常ロジックでは新しい独立校と誤認される
        ため、オーバーライドを**carry-forward更新の前**に適用する必要がある（適用順序を誤ると
        誤った学校名がcarry-forwardされ続ける）
      `parse-table-pdf-ehime.test.ts`で検証済み（jest5テストgreen）。tsc実exit0・
      jestフル379suites6501tests green。**ehimeは実装完了**
- [x] **toyama(R8)を実装・検証完了（2026-09-02）・第6の組み立てパターンを確立**。75/75件・
      完全一致（グランドトータルquota5,020・applicants4,482も既存noteと一致）。
      - ⚠️**新パターン発見: 学校名ラベルの出現位置が同一表内で学校ごとに不規則**。ibaraki型
        （結合セル中央固定）・tochigi型（先頭行固定）のどちらの前提も成り立たず、入善は
        3行中2行目、桜井は3行中2行目（学科名と同居）、魚津/魚津工業/上市は先頭行（学科名と
        同居）、滑川は5行中3行目、と学校ごとにバラバラだった。**解法: 罫線（外周のみ・内部
        分割線は無い）でブロック境界を決定し、ブロック内のどこにラベルがあってもそのブロック
        全体の学校名として採用する**（位置に依存しない汎用アルゴリズム。単純forward
        carry-forwardでは先頭行にラベルが無い学校で直前の学校名を誤って引き継ぐバグになる）
      - **副次的な発見（既存機構だけで自然解決した2件）**: ①内数コース（「普通科(体育コース)」
        等）はPDF側が数値を丸ごと括弧`(40)(24)...`で囲んで印字しており、`Number("(16)")`が
        NaNになるため**追加コードなしで自然に除外**された（既存のquota/applicants/rate
        finite-number検証が代わりに機能した）。②探究科学科の人文社会科学科（数値非印字）も
        同様に自然除外
      - ⚠️3頁目「(大学科別)」は学校別データではなく学科ごとの県全体集計表のため、フィクスチャ
        から除外（2頁分のみ収録）。ページ末尾の「合計」行も学校名+学科名が列境界をまたいで
        分裂することがあるため、ehimeの教訓どおり部分一致`(schoolName+department).includes('合計')`
        で除外した
      `parse-table-pdf-toyama.test.ts`で検証済み（jest6テストgreen）。tsc実exit0・
      jestフル380suites6507tests green。**toyamaは実装完了**
- [x] **aomori(R8)を実装・検証完了（2026-09-02）**。89/89件・完全一致（グランドトータル
      quota6,980・applicants6,436も既存noteと一致）。toyama型（罫線ブロック内のどこにラベルが
      あっても採用）を流用しつつ、**2つの新しい罠**を発見した。
      - ⚠️**罠1: aomoriは罫線が「学科ごとの内部区切り」まで実在する（toyamaには無かった）**。
        toyama型の単純な「hlines全部を境界にする」実装をそのまま使うと、内部区切り線まで
        ブロック境界として誤検出し、1校が複数ブロックに分裂してラベルが見つからなくなる。
        **ibaraki型の`fullLineX0Max`と同じ判定（学校名列を跨ぐ完全線か、学科名列以降にしか
        伸びない部分線か）をtoyama型のブロック分割にも適用する必要がある**（`groupRowsIntoBlocks`
        に`fullLineX0Max`引数を追加）。**トヨタ型を採用する県では毎回「本当に内部区切り線が
        存在しないか」を罫線のx0分布で確認すること**（toyamaは無かったが偶然であり一般化できない）
      - ⚠️**罠2: 学科名テキストと数値が別の行に分離することがある（新しいエリアの最初の学校で
        発生しやすい）**。「商業」という学科名だけの行の直後に、数値付きだが学科名列が空欄の
        行が続く（青森商業・五所川原で確認）。**解法**: ブロック内を先頭から走査し、数値を
        持たない「学科名のみの行」をpendingキューに積み、数値を持つ行で学科名列が空欄だった
        場合はpendingキューから1件消費する（出現順が保たれている前提）。**ここでも
        `normalizeExtractedText`を通さずに`.trim()`だけで比較するとdepartmentOverridesの
        キーが一致しない事故が起きる**（トラッキングスペース入りの生テキストのまま扱った）
      `parse-table-pdf-aomori.test.ts`で検証済み（jest4テストgreen）。tsc実exit0・
      jestフル381suites6511tests green。**aomoriは実装完了**
- [x] **fukui(R8)を実装・検証完了（2026-09-02）・Y-6一次収集時点でも人間がビジョン解析を
      要したケースを機械パーサで解決**。72/72件・完全一致（高志の内部進学枠除外・グランド
      トータル除外も確認済み）。
      - ⚠️**罠1（境界値の閾値誤り）**: aomori型の`fullLineX0Max`しきい値を機械的に60に
        設定したところ、真の学校境界線（x0=66.8の単一セグメント）が閾値未満を僅かに満たさず
        「内部区切り線」と誤判定され、隣接2校（金津/高志）が1ブロックに融合してラベルが
        誤って引き継がれた。**しきい値は「内部区切り線の最小x0」と「真の境界線の最大x0」の
        間に十分な余白を取って決めること**（今回は60→75で解決。県ごとに罫線のx0分布を
        実測して決める）
      - ⚠️**罠2（鯖江のくくり募集・4ラベル:2数値行）**: くくり募集の子コースラベルが
        数値行より複数個ぶん先行して積み上がる場合（「スポーツ」「健康福祉」の2ラベル→
        数値行1つ、続けて「IT」「アートデザイン」の2ラベル→数値行1つ）、aomori型の
        pendingDepartments（FIFO1個消費）では正しい学科名に対応付けられない。**しかし
        数値（quota/applicants/rate）自体は正しく抽出できていた**（既存データの一次収集時の
        pdftoppmビジョン解析結果と完全一致）。**解法: 学校名+ラベルのテキストキーではなく、
        学校名+数値の組をキーにした値ベースのoverrideで対応**（既存データを根拠とした
        明示補正・tokushima/gunmaの数値ベース裏取りと同型）。この技法は「テキストの対応付けが
        構造的に曖昧でも、数値自体は独立して正しく抽出できている」場合の汎用的な救済策として
        記録する価値がある
      - ⚠️**罠3（半角/全角中点の見た目上の混同）**: `pdftotext -layout`の目視では「機械･自動車」
        のように半角中点（･）に見えたが、実際のPDF内部データ（PyMuPDF文字座標抽出）は全角
        中点（・）だった。**`pdftotext -layout`の出力だけを見て記号の全角/半角を判断しない**
        （-layoutモードのフォント処理で見た目が変わることがある）。overrideキーを組む際は
        実際のジオメトリデータの文字を確認すること
      `parse-table-pdf-fukui.test.ts`で検証済み（jest4テストgreen）。tsc実exit0・
      jestフル382suites6515tests green。**fukuiは実装完了**
- [x] **kagawa(R8)を実装・検証完了（2026-09-02）**。68/68件・完全一致（グランドトータル
      quota4,208・applicants4,296も既存noteと一致）。既存データのヘッダコメント自身が
      「番号付き学校リストのため学校名の行折返し遅延の罠が無い」と明記しており、実際に
      tochigi型の単純carry-forwardだけで解決できた（toyama/aomori型のブロック方式は不要）。
      PDFは全2頁だが1頁目に「全日制」の本体表とグランドトータルが完結しておりフィクスチャは
      1頁目のみ（2頁目は「全国からの生徒募集」という別枠のスコープ外表）。
      - ⚠️**新しい罠1: 「大学科」列と「小学科・コース」列の二段構成で、単一学科校は大学科列
        だけで完結する**。複数学科を持つ学校は大学科列に広域区分（例:「農業」）・小学科列に
        個別コース名（例:「生産経済」）が入る二段構成だが、単一学科校（「普通」「総合」）は
        小学科列が空欄で大学科列だけに学科名が印字される。**department抽出は小学科列を優先し、
        空なら大学科列にフォールバックする**必要がある（フォールバックを忘れると単一学科校
        18校ぶんが丸ごと消える・件数差分で気づきやすいので発見は容易）。
      - ⚠️**新しい罠2: 脚注記号（＊☆□）が学科名列・数値列の文字として混入する**（高松北の
        quotaTextが「☆93」になりNumber()がNaNで行ごと消えた・miyagi型の記号除去と同型だが
        **数値列（quotaText等）にも記号が混入しうる点**が新しい）。**記号除去は
        department/quota/applicants/rateの全フィールドに適用し、かつ「小学科→大学科
        フォールバック判定」より先に適用すること**（記号除去前の生テキストで判定すると
        「＊」だけの学科名を非空と誤判定してフォールバックが起動しない事故が起きた）。
      `parse-table-pdf-kagawa.test.ts`で検証済み（jest4テストgreen）。tsc実exit0・
      jestフル383suites6519tests green。**kagawaは実装完了**
- [x] **okinawa(R8)を実装・検証完了（2026-09-02）・最大規模（156件・58校・全4頁）**。「学科名を
      含む複雑度シグナル数が最少(1件)」という粗い指標で選んだが、実際は最も罠が多い県の1つ
      だった（教訓: `grep`件数の粗い指標だけで複雑度を判断しない）。156/156件・完全一致
      （グランドトータルquota14,084・applicants13,522も既存noteの自己算出値と一致）。
      - ⚠️**罠1（本体）: 全日制・定時制が同一表に混在**（他県のような独立した「全日制計」行が
        無い）。**解法: 「課程」列（全日/定時）で機械的に絞り込む**（学校名の固定リストに
        頼るより頑健）。ただし課程列も学校名と同じ「先頭行にだけ印字され継続行は空欄」の
        carry-forward構造を持つため、**課程列自体もcarry-forwardしてから判定する必要がある**
        （那覇工業の定時「電気」行・泊の「夜間部」行は課程列が空欄で、素朴な自行判定だと
        すり抜けて誤収録された）。
      - ⚠️**罠2: 2桁の学級数がdepartment列にはみ出す**（「10」の先頭桁が右隣の学級数列の
        左境界をわずかに超えてdepartment列に混入）。department本文が数字だけで終わることは
        無いため、**末尾の連続する数字を機械的に除去してよい**という一般則で解決（gunmaの
        「(255)」混入と同型・列境界のわずかな越境は繰り返し起こりうる罠として要警戒）。
      - ⚠️**罠3: overrideキーはnormalizeExtractedTextを通した後の文字列で作ること
        （aomoriの教訓の再確認）**: 半角カタカナ（会計ｼｽﾃﾑ等）を含む生テキストのままキーを
        組むと一致しない。normalizeExtractedTextしてからキーを比較する。
      - ⚠️**罠4: 既存データが県独自に半角括弧を使っていることがある**。他県（tokushima等）は
        全角括弧に統一されているが、**okinawaは既存データ自体が半角括弧`()`を使う県だった**。
        `normalizeDepartmentText`は全県共通で半角→全角に変換する設計のため、パーサ出力側で
        変換後に半角へ戻すpost-process（`（`→`(`・`）`→`)`）が必要だった。**「表記統一は
        全県共通」という前提を無条件に信じず、対象県の既存データを都度確認すること**。
      `parse-table-pdf-okinawa.test.ts`で検証済み（jest6テストgreen）。tsc実exit0・
      jestフル384suites6525tests green。**okinawaは実装完了**
- [x] **nara(R8)を実装・検証完了（2026-09-02）・倍率が資料に印字されていない県で初のパーサ化**。
      71/71件・完全一致（グランドトータルquota6,896・applicants6,276も既存noteの「合計」行と
      一致）。既存データも自前算出のため、T-Y11Cで確立した`roundHalfUpScaled`（BigInt整数演算・
      `toFixed`のバグを踏まない）をパーサ側でも再利用し突合した（初の実戦投入）。
      - ⚠️**新しい罠1: 「基底ラベル1件→複数の（コース名）行」という一対多の合成が必要**
        （添上「普通」→（人文探究）（人文探究以外）の2レコード、桜井も同型）。fukui/aomori型
        のpendingキュー（1回消費でFIFOから取り除く）では2件目以降のコース行に基底ラベルを
        再利用できない。**解法: 基底ラベルを「消費して取り除く」のではなく「currentSchoolと
        同じ永続変数」として保持し、数値行の学科名が「（」で始まる場合だけ直前の基底ラベルと
        連結する**（新しい基底ラベル行が来たら更新・「（」で始まらない自己完結型の行は連結
        しない）。
      - ⚠️**新しい罠2（kagawa教訓の再確認・より厳密化）: 「会計」を含む正当な学科名がある県で
        「◯◯計」小計行をブロック全体の部分一致で除外すると事故る**。商業高校の「会計」学科が
        丸ごと消えた（ブロックの学校名+全学科名の連結文字列に対し`.includes('計')`で判定して
        いたため）。**「県立計」の「計」がgunma/ehime型で学校名列にはみ出し分裂すること
        もある**ため、単純な学校名単体チェックでも不十分。**最終的な解法は「ブロック全体の
        連結文字列が既知の小計/合計ラベル（'県立計'/'市立計'/'合計'/'総計'）に前方一致するか」
        で判定する**（`.includes`ではなく`.startsWith`。「会計」で始まるブロックは対象外に
        なる一方、分裂した「県」+「立計...」は正しく前方一致する）。
      `parse-table-pdf-nara.test.ts`で検証済み（jest4テストgreen）。tsc実exit0・
      jestフル385suites6529tests green。**naraは実装完了**
- [x] **mie(R8)は調査のみ・見送り（2026-09-02）**。「学校計」高信頼度パターン（nagasaki型）+
      8件のくくり募集という構成で着手したが、**くくり募集の集計行の位置パターンが1県内に
      2種類混在**していると判明: ①桑名工業型＝「label(数値無)→集計行(学科名列が空欄・数値
      あり)→label(数値無)」（集計行が2ラベルの間に挟まる）②四日市農芸型＝「label(数値無)→
      label+集計数値(学科名とquota等が同一行)→label(数値無)」（集計行が3ラベルの中央に
      その学科名込みで存在する）。どちらのパターンかは事前にPDFを見るまで分からず、しかも
      「その行が集計行かどうか」は前後の文脈（隣接行がlabelのみか）を見て初めて判定できる
      ため、単純なpendingキュー方式（fukui/aomori/nara型のいずれも）では対応できず、
      run単位のグルーピング+先読みという設計が必要と判明。費用対効果が低いと判断し、
      より単純な県を優先することにした（コード変更なし・fixture/testは作成せず削除済み）
- [x] **yamanashi(R8)を実装・検証完了（2026-09-02）**。48/48件・完全一致（グランドトータル
      quota3,356・applicants3,037も既存noteの「全日制課程計」行と一致）。くくり募集4組は
      いずれもmie型のような複数行合成が不要で、資料上すでに「○○（一括）」という1行完結
      ラベルとして表現されていた（既存データの半角括弧post-processのみで対応）。
      - ⚠️**新しい罠: 3桁のquota/applicantsで、括弧付き内数「(3)」等が同じ列区間に
        混入する（okinawa/gunmaと同型の列越境だが、ここでは「隣接列への越境」ではなく
        「同一列内で数字本体+括弧付き内数が連結される」という別の形）**。department側は
        3桁quotaの先頭桁が越境（okinawa型）、quota側は自分の列内で末尾に「(3)」が続けて
        印字される（本体側の罠）。**両方とも「本来欲しい数字の直後で列を切る」ことで
        解決**（department側は右端を狭める・quota側も同様に右端を狭めて内数の括弧を
        別の未使用列に追い出す）。
      - ⚠️**「県立高校計」等の集計ラベルがschoolName/department列の境界をまたいで分裂する
        （nara型の教訓の再確認）**。`department.trim() === '計'`のような完全一致だけでは
        「高校計3...」のように分裂したケースを検知できず、既知ラベルへの前方一致
        （`(schoolName+department).startsWith(marker)`）が必要だった。
      `parse-table-pdf-yamanashi.test.ts`で検証済み（jest4テストgreen）。tsc実exit0・
      jestフル386suites6533tests green。**yamanashiは実装完了**
- [x] **kyoto(R8)を実装・検証完了（2026-09-02）**。75/75件・完全一致（グランドトータル
      quota6,048・applicants5,160も既存noteの「全日制計」行と一致）。多くの学校で学校名ラベルが
      結合セルの不規則な位置に出現するためtoyama型の罫線ブロック方式が必要だった。
      - ⚠️**新しい罠1: 分校・学舎名（「（南）」「（宮津学舎）」等）が学校名列の別の行に断片として
        出現する**。akita型の学校名複数行断片連結を応用したが、**無条件の全断片連結は
        department側のテキストが学校名列にはみ出した誤検知（「プロジェクト」等）も拾って
        しまう**ため、「（」で始まる断片だけを追加連結の対象にする限定ルールが必要だった。
      - ⚠️**新しい罠2: 脚注番号「注１」等が倍率の直後に連結印字される**（fukuiのくくり募集の
        ラベル混入とは別種・数値列に文字が混入するパターン）。`Number("0.84注１")`はNaNになる
        ため、`.replace(/注.*$/,'')`で除去してから数値化する。
      - ⚠️**新しい罠3: 学校名/学科名の列境界は県内でも学校ごとに最適位置が食い違うことがある**
        （南丹・大江は境界を狭める必要があったが、アグリサイエンス[単位制]は逆に境界を広く
        取る必要があり、単一の固定境界では両立しない）。**1つの境界値で全県を解決しようと
        せず、少数（この県では5件）の食い違うケースは既存データを根拠にした個別補正
        （fukui/tokushima型の数値ベース裏取り）で対応する方が全体として安全**。
      `parse-table-pdf-kyoto.test.ts`で検証済み（jest4テストgreen）。tsc実exit0・
      jestフル387suites6537tests green。**kyotoは実装完了**
- [ ] 残り27県（usable 43県からibaraki・tochigi・akita・tokushima・ishikawa・gunma・nagasaki・ehime・toyama・aomori・fukui・kagawa・okinawa・nara・yamanashi・kyotoを除く。kanagawa・mieは見送り扱い）に
      同じ方式を横展開する。着手前に`pdftotext -table -enc UTF-8`の出力を目視し、
      ①学校名ラベルの位置（先頭行か中間か＝ibaraki型かどうか）②長い学校名の折り返しの
      有無（akita型が必要か）③№列の有無、を確認してから実装すること
  - **kochi**: 座標まで実測して構造を特定（2026-09-02・ishikawaの罫線バグ修正後に再着手）。
    列は[schoolName/**学科名が3つの副列に分裂**（①学科カテゴリ「工業」②コース略称
    「(機土)」③具体名「機械」の3つが別々のx範囲に印字される）/入学定員/募集定員(quota)/
    第1志望者数(applicants)/学校計(特定の行にだけ値が入る)/志願率(rate)/第2志望者数]。
    既存データの学科名は**②を完全に無視し①+③だけを`カテゴリ(具体名)`の形で合成**する
    （例:「工業」+「(機土)」+「機械」→「工業(機械)」。ただし①のみで③が無い場合は
    ②のコース略称の括弧の中身をそのまま流用する（例:「商業」+「(ﾋﾞｼﾞﾈｽ)」→
    「商業(ビジネス)」半角カタカナ正規化込み）。学校名ラベルはibaraki型（結合セル中央付近
    ・N=4の安芸で2番目の行に出現）。
    - ✅**続報（同日中に`extraColumns`一般化を実装・テスト済み・回帰無し）**:
      `TableColumnLayout`に`extraColumns`（列名→インデックスの任意追加マッピング）を
      追加し、`RawTableRow.extra`にその生テキストを持たせるようにした。呼び出し側が
      `parseTablePdfPageRows()`の出力を`.map()`して`department`を好きなロジックで
      合成してから`assembleCompetitionRateRows()`に渡せる（既存4県のテストは無回帰・
      tsc実exit0・jestフル373suites6463tests green）。この一般化自体はcommit済み
    - ⚠️**ただしkochi本体は未完了**: `extraColumns`を使って個別レコード（安芸の4学科は
      既存データと完全一致することを確認済み）は正しく合成できたが、①**数値が右詰め
      印字のため、桁数の異なる隣接列（募集定員/第1志望者数）の境界がずれると桁が
      混入する**（例:「岡豊 普通」で入学定員200・募集定員200・第1志望者数212が隣接し、
      境界調整を1回行っても一部の行で依然として桁の混入が残った）②kochiの資料は
      「県立計」「市立計（高知商業）」「合計」に加え、**地区ごとの中間小計行が複数箇所に
      存在し、最初の「合計」文字列で打ち切る単純な方式では正しく切れない**（後続の地区
      さらに学校が続く）。この2点は個別の境界再調整・小計パターンの体系的な把握が
      追加で必要なため、今回はここで打ち切った。次回は①桁の混入が起きた行を全数
      目視で洗い出し右詰めの実際の右端x座標から逆算して境界を決め直す②「計」を含む
      行をすべて機械的に列挙してから、地区小計と真の合計を区別するルールを決める、
      の2点から着手すること
    - ⚠️**続報2（同日中にさらに2件の罠を発見・未解決）**: ①**2文字の学校名がschoolName/
      category列境界をまたぐ**（例:「吾北」の「北」が境界70-145の外に出てcategory列に
      混入し、carry-forward先の前の学校「高知追手前」に誤って「北普通」という架空の
      学科として吸収された）。境界を145→155へ拡張すれば直るが、他の県で確立した
      境界とズレが無いか要再検証。②category==specificの場合（例:「理数(理数)」
      「音楽(音楽)」）に**無意味な重複括弧が付く**バグを発見（`detail !== category`の
      ガードで解決可能・実装済みだが未検証）。③**半角カタカナのコース名が3列(courseCode/
      specific)の境界をまたいで分裂する例外**（「チャレンジＡ」の末尾「Ａ」だけが
      specific列にはみ出し、期待される合成規則（カテゴリ+具体名を括弧で囲む）とも
      異なり**括弧無しでcourseCode+specificをそのまま連結**する必要がある）を発見。
      この③のような「合成規則そのものが行によって異なる」ケースが今後も出てくる
      可能性が高く、kochiは他県より個別対応のコストが高いと判断する。
    - ⚠️**続報3（同日中に①②③すべて実装・検証し最終的な結論に到達）**: ①②③を反映した
      結果、個別レコードは正しく合成できるようになった（チャレンジＡ・理数・音楽等の
      重複括弧/分裂問題は解消）。**しかし新たに④真に致命的な罠を発見した**: 「吾北」
      という独立した1学校（quota40・applicants6・rate0.15）が、直前の学校「高知追手前」
      との間に**罫線が一切存在せず**、ブロック境界（`isBlockEnd`）が検出できないまま
      前の学校のブロックに吸収され続ける。これはtokushima/ishikawaで見つかった「表の
      先頭/末尾に罫線が無い」問題とは異なり、**表の途中の任意の位置で罫線が欠落しうる**
      という、`syntheticTopY`/`syntheticBottomY`（表の両端each1箇所だけを補う仕組み）
      では対応できない、より深刻なパターンだった。加えて元々未解決だった⑤地区ごとの
      中間小計行（県立計/市立計/合計が複数箇所にある）の判別も残っている。
      **最終結論**: kochiはこのセッションで確立した5つの組み立てパターン（ibaraki/
      tochigi/akita/tokushima/ishikawa）のいずれにも当てはまらない、**罫線が表の
      途中で不規則に欠落する**という新種の罠を持つ県であり、対応するには「罫線に
      依存せずy座標クラスタリングだけで全ての行を検出し、ブロック境界はラベルの
      出現パターンから逆算する」といった、根本的に異なる設計が必要と判断する。
      **この県は次点に回し、まずは残り37県から罫線が正常な県を優先して片付けること**
      （このセッションで確立した5パターンのいずれかに当てはまる県の方が費用対効果が
      高い）。検証用スクリプトはscratchpadに残したのみでコードはコミットしない
  - **yamagata**: ibaraki型（罫線ベース・中央付近の行にschoolNameラベル）の構造で
    schoolName/departmentの列分離、部分列の幅調整、「探究(理数探究,国際探究)」の
    半角括弧+読点変換まで解決し98件抽出（期待90件）まで到達したが、**「学校名の
    末尾2文字と一致する学科系統ラベル（例:山形工業の「工業」・置賜農業の「農業」）が
    department列の先頭に重複して印字される行がある」という罠に遭遇し、これが
    「削るべき重複」なのか「学科の正式名称の一部」なのか座標だけでは判別不能**と
    判明した。具体的には山形工業のブロックでは中央行（情報技術の行）にのみ「工業」が
    重複印字されており、これは削るべき重複（yamagata.tsの正解は「情報技術」単体）。
    一方、上山明新館（農業食料生産/商業情報経営）・寒河江/長井（普通一般コース/
    普通探究コース）・置賜農業の一部行（農業資源活用）では、同じ位置に同じ2文字が
    印字されているにもかかわらず、yamagata.tsの正解はこれを**含んだ**複合学科名
    （例:「農業食料生産」）。両者は座標的に区別できず（同一行内・同一フォント・
    連続した文字送りで印字され、隙間や別レイヤーの手がかりが一切ない）、県の
    既存データ（yamagata.ts）と照合しない限り「重複だから削る」か「正式名称の一部
    だから残す」か判定できない。カテゴリ語の単純な先頭一致除去（'工業','農業'等の
    固定リストで先頭から剥がす）を試したが、上山明新館・寒河江・置賜農業などの
    正しいレコードを逆に壊すため撤回した。**この曖昧さはkochiの「罫線が表の途中で
    不規則に欠落する」問題と同種（＝座標ベースの機械的パターンでは原理的に解決不能な
    県固有の罠）と判断し、yamagataも次点に回す**。検証用スクリプトはscratchpadに
    残したのみでコードはコミットしない
  - **niigata**: 全日制表が学科系統ごとに8区分（普通科系/農業/工業/商業/水産/家庭/その他/
    総合）に分かれ区分ごとに「計」行がある**上に**、「あおい」→改行→「碧」のような
    **ふりがな（ルビ）が学校名の上に別行として印字される**実例を発見（碧高校の本来の
    表記は「碧」だが「あおい」という読み仮名が別レイヤーとして先に出現し、akita型の
    折り返し名連結ロジックをそのまま使うと「あおい碧」という誤った連結名になる）。
    全6ページで概要ページ・学区外出願一覧等の余計なセクションも多い。未着手
  - **kagoshima**: **左右2段組ページ×学区ごとに7ページ**というレイアウトに加え、
    1レコードが**2行にまたがる**（1行目:募集定員/学力検査定員/(一定枠内数)/倍率、
    2行目:実際の最終出願者数の生数値）という構造。座標ベースでも列とブロックの両方の
    次元で複雑度が高い。未着手
  - **tokushima**: 列自体は[募集人員/出願者数/倍率]と最も単純だが、**1ページに全日制
    （左2段）＋定時制（右1段）の実質3列組**が並び、ibarakiと同型の「学校名遅延
    （結合セル中央配置）」が全日制の2段それぞれで独立に発生する。さらにページ下部に
    「学区外出願状況」という全く別の小表が同一ページに混在する。**列の単純さに反して
    レイアウト分割の複雑度が高い**。着手し列境界の実測まで完了したが（次回の時短用に
    記録）、罫線ベースの`isBlockEnd`判定（`fullLineX0Max`）が段ごとに異なる絶対x座標を
    要求するため`parseTablePdfPageRows`をそのまま複数回呼べず、関数の一般化
    （オフセット対応）が追加で必要と判明した時点で今回は打ち切った。
    実測した列境界（ヘッダ行`学校/学科・類/募集人員/出願者数/倍率`のx0から算出・y=61.0/65.3の
    ヘッダ行を直接読むと高速に求まる）:
    - 全日制LEFT: schoolName[~30-95] / department[~95-165] / quota[~165-205] /
      applicants[~205-248] / rate[~248-295]
    - 全日制MIDDLE: schoolName[~295-360] / department[~360-445] / quota[~445-463] /
      applicants[~463-505] / rate[~505-550]
    - 定時制RIGHT（対象外・x>=550は無視してよい）
    - 次回はまず`parseTablePdfPageRows`に「この呼び出しで見るべきhlineのx範囲」を渡せる
      よう拡張し、LEFT/MIDDLEそれぞれに対して独立に呼び出してから結果を連結するとよい
    - ⚠️**続報（同日中に実装まで進めて根本原因を特定・完全に解明）**: LEFT/MIDDLE個別に
      罫線ベースの組み立てを実装し実データで検証したところ、65/69件までは到達したが、
      残り4件（那賀/海部の各1レコード・富岡西2レコード）で**ラベルだけが独立した1行として
      印字され、前後どちらの学科データ行にも数値を持たない「ラベル単独行」**という
      tokushima固有の第4パターンを発見した。実測（座標）:
      ```
      y=132.3  普通 30 25 0.83        （ラベル無し）
      y=138.8  那賀                   （ラベルのみ・数値無し）
      y=145.2  森林クリエイト 13 10 0.77（ラベル無し）
      y=158.0  普通 47 43 0.91        （ラベル無し）
      y=170.9  海部 情報ビジネス 18 19 1.06
      ```
      既存データ（`tokushima.ts`）は「那賀=[森林クリエイト13/10/0.77, 普通47/43/0.91]」
      「海部=[普通30/25/0.83, 情報ビジネス18/19/1.06, 数理科学28/22/0.79]」としており、
      **「那賀」ラベルの直後(y=145.2)ではなく直前(y=132.3)の行が実は海部の学科である**
      （＝ラベルは自分の学科群の直後にあるとは限らず、直前の行が別の学校の学科である
      ケースが実在する）。これは**幾何学的な位置関係だけからは一意に決定できない、
      真に曖昧な構造**であり、ヘッダコメントに記録された「阿南光・海部の学校名帰属を
      WebSearchで裏取りして訂正した」という既存の経緯とも完全に符合する（＝当時の人間
      /vision転記でも機械的には解けず、実在学科の外部検証が必要だった箇所と同一）。
      富岡西のケース（`普通1471601.09`→ラベル`富岡西`→`理数30180.60`）は前後とも同一校
      に属し曖昧ではないが、同じ「ラベル単独行」構造のため既存のブロック境界検出ロジック
      では正しく1ブロックとして拾えず未実装のまま欠落した。
      **✅続報2（同日中に完全解決・実装完了）**: 「ラベル単独行」問題は罫線が存在しない
      表の先頭/末尾行の欠落が原因と判明（富岡西の「普通」「理数」もこの欠落で消えていた）。
      `parseTablePdfPageRows`に`syntheticTopY`/`syntheticBottomY`（罫線が無い場合に仮想の
      区切りを補う）を追加したところ**69/69件（学校×学科の対応）が全て復元**された。
      さらに検証中に**既存の`normalizeExtractedText`のバグを発見**（`.normalize('NFKC')`が
      全角括弧「（）」まで意図せず半角化していた・「芸術（音楽）」で発覚）。半角カタカナの
      範囲だけをNFKC正規化する`normalizeHalfwidthKatakana`に切り替えて修正（ibaraki/
      tochigi/akitaの既存テストは無回帰・全green）。
      最終結果: **69/69件のうち67件は完全一致**、残る那賀/海部の1件は上記の通り幾何学的に
      真に曖昧なため、`tokushima.ts`のヘッダコメントに記録済みのWebSearch裏取り結果を
      根拠とした明示的な補正（`applyKnownAmbiguityCorrection`）で対応した。**また、既存
      データの並び順自体が単純な規則（LEFT→MIDDLE等）に従っていないため、この県だけは
      「順序を含めた完全一致」ではなく「学校×学科×数値の集合としての一致」を検証基準と
      した**（`filterGeometryByXRange`で1ページを複数の列組に分割する新しい共通関数も
      本体モジュールに追加）。`parse-table-pdf-multi-column.test.ts`で検証済み（jest4テスト
      green）。tsc実exit0・jestフル372suites6458tests green。**tokushimaは実装完了**
  - これら4県はいずれも「列は単純」または「既存パターンの応用」に見えて、実際に開いてみると
    毎回新しい罠が1つ以上見つかる。**着手前にPDFを実際に開いて構造を確認する手順を省略
    しないこと**（この4県の調査だけでも複数の新パターンが見つかった）
- [x] **miyagi(R8)を実装・検証完了（2026-09-02・finalRateの浮動小数点丸めバグを発見しmiyagi.ts
      本体を訂正）**: tochigi型（y座標クラスタリング・単純carry-forward）で129/129件・完全一致
      （グランドトータルquota13,400・applicants12,516も一致）。追加で発見した2つの罠:
      ①「○○地区計」「○○地区合計」等の地区別小計行が学校の行に混入する→`excludeRow`で
      `地区計`/`合計`を含む行を除外（ibarakiの「全日制計」混入と同型の罠が再確認された）
      ②市立高等学校等を示す脚注記号「※」「■」「☆」が学校名・学科名のどちらにも付着する
      （`normalizeExtractedText`はこれを除去しないため`extractRowFields`の生テキストの時点で
      除去してから組み立てる）。
      - ⚠️**finalRate7件が既存データと不一致という問題は、パーサのバグでもPDF改訂でもなく
        既存データ側の計算バグと判明**: 7件は例外なく`applicants/quota`の小数点以下3桁目が
        ちょうど5になる境界値（例:仙台向山 普通科 204÷160=1.275）で、自分のPyMuPDF抽出と
        `pdftotext -table`という独立した2手段が共に「1.28」と読み取ったのに対し、既存データは
        「1.27」だった。原因を特定: `Math.round(x*100)/100`や`toFixed(2)`で1.275を計算すると
        2進数浮動小数点表現の誤差（1.275は正確に表現できず1.27499...として格納される）により
        切り捨てられて1.27になるという既知のJSの罠に、旧値7件全てが完全に一致した。**すなわち
        既存データはPDFの転記ミスではなく、finalRateをJSで再計算し直した際の計算バグであり、
        Wayback Machine確認は不要と判明**（そもそも同URLのアーカイブ自体が1件も存在せず
        `archive.org/wayback/available`で確認不能だった）。`miyagi.ts`本体の該当7件を
        PDF実測値（政府側集計ソフトの正しい四捨五入結果）に訂正し、ヘッダコメントに根拠を記録。
        **これは「データをパーサに合わせにいった」のではなく「一次ソースの実測値に基づき
        既存データの計算バグを修正した」もの**（既存データを機械的に書き換えない原則には
        抵触しない）。
      - **一般化できる教訓**: 手作業データのfinalRateが四捨五入の境界値（x.x25/x.x75）付近で
        既存データとずれる場合、**PDF改訂を疑う前に、その値がJSの`toFixed`/`Math.round`の
        浮動小数点丸めバグの結果と一致するか確認する**（同じ罠が他県にも潜んでいる可能性がある）。
      - `parse-table-pdf-miyagi.test.ts`で検証済み（jest6テストgreen）。tsc実exit0・
        jestフル374suites6469tests green。**miyagiは実装完了**
- [ ] **検証は1つだけ**: パーサの出力が、既存の手作業データ（`competition-rates/<pref>.ts`）と
      **レコード単位で完全一致するか**
- [ ] ⚠️ **一致しなければパーサが間違っている。データを合わせにいかない**
- [ ] 一致したら `checkAgainstSubtotal()`（`src/lib/competition-rate.ts`・既存）に配線する
- [ ] ⚠️ **既存データを書き換えない。** パーサは検証専用の出力を出すだけ。
      R9が来たときに初めて書き込みに使う

### 2-c 通らない県の半自動ハーネス

- [ ] ダウンロード → pdftoppm でレンダリング → 「要ビジョン解析」キューへ積む、までを自動化する
- [ ] キューには県名・年度・ページ数・想定レコード数（グランドトータルから逆算）を持たせる
- [ ] **ビジョン解析そのものは自動化しない。** 人（またはloop）が読む前提でよい。
      **1月に「何を読めばいいか」が即座に並んでいる状態を作るのが目的**

### 2-d 列位置マップ

- [ ] `competition-rate-quota-definitions.ts` は列の**意味**の裁定（47/47完了）。
      **列が何番目にあるか**はまだ無い。2-bの実装で判明した順に記録する
- [ ] ⚠️ **年度で列の並びが変わりうる**（ゲートで確認済みの年次不安定性）。
      並びは年度ごとに持ち、機械的に名寄せしない

---

## 見込み

**47県 × (取得 → 判定 → 実装 → R8照合) は、20日で終わりません。それでいい。**
2-a だけでも数日、2-b は1県ずつ潰す作業です。**終わった県から冬に効きます。**

## 払底したときの扱い（再掲・今回守られていない）

**「払底した」と判断する前に、必ず次を自問すること:**

1. **本当に材料が無いのか。それとも「材料が無い」と読み違えていないか**
2. タスクファイルの**別の行に、その材料の在り処が書かれていないか**
3. それでも払底なら、**監視モードに落ちる前に質問ノートへ
   「どこまで終わって、次に何を取るべきか」を書いて止まる**

今回は 1 と 2 で止められたはずでした（R8の手作業データが手元にある、と同じファイルに書いてあった）。
