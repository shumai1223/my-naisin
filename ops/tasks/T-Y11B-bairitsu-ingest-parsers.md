# T-Y11B 47県の倍率取り込みパーサを冬までに作る

**着手 2026-09-08。これが不在期間〜9月末の主食。20日で終わらない前提で組んである。**

⚠️ **この文書の数値はすべて対話セッションが集計したもの。loop は自分で数え直してから動くこと。**
**2026-08-31〜09-01 の1日で、対話セッションは仮説を6つ外している**（私立学費DB / A→B→Cの続き /
総合得点34県 / ブログ刷新 / 7年スナップショット / 県固有用語47県）。**この文書も疑ってかかること。**

---

## ★★ 着手前ゲート（半日・これを飛ばさない）— ✅2026-09-01実施・判定=続行（設計修正つき）

**千葉・埼玉・東京以外から3県を選び、令和8年度の公表PDFを実際に開いて次を報告する。**

**実施方法**: 既存の`src/data/competition-rates/<pref>.ts`ヘッダコメントは、Y-6/掛-1収集時に実際にR8
（および過去年度）の一次PDFを開いて記録した転記ログそのもの（一次ソースURL・PDF構造・抽出手法・
グランドトータル突合結果を含む）。新規にサーバへ再アクセスする代わりにこの一次記録を根拠として用いた
（robots.txt尊重・相手サーバ負荷軽減の方針とも整合）。ibaraki・nagano・kagoshimaの3県で判定。

| 県 | 1行=1校×1学科か | quota列 | 機械的にパース可能か（R8） | グランドトータル |
|---|---|---|---|---|
| **ibaraki** | ✅素直な表（くくり募集の波括弧構造なし） | 募集定員(a) | ✅**YES**（pdftotext -layoutで全列抽出成功） | ✅「全日制計」と完全一致 |
| **nagano** | ✅素直な表（4通学区の地区別合計あり） | 全県計欄と対応する募集人員列 | ✅**YES**（pdftotext -layoutで全列抽出成功） | ✅全県計＋4通学区合計と完全一致 |
| **kagoshima** | ✅素直な表（7学区分割・学区合計あり） | 学力検査定員（募集定員－推薦等内定者数） | ❌**NO**（ToUnicodeマッピング欠落・数字のみ抽出可、学校名は全欄空白） | ✅「全日制合計」＋学区合計と完全一致 |

**判定（文字どおり）**: 3県中2県（ibaraki・nagano）でR8はパース可能 → **2/3で続行の閾値を満たす**。

**⚠️しかし年次不安定性が重大**（3県×複数年度=8県年で検証・全て`<pref>.ts`の掛-1追記に実記録あり）:

```
ibaraki: R8=pdftotext成功 / R7=ToUnicode欠落(要ビジョン) / R6=同左 / R5=同左  → 4年中1年のみ機械可
nagano:  R8=pdftotext成功 / R7=ToUnicode欠落(要ビジョン)                    → 2年中1年のみ機械可
kagoshima: R8=ToUnicode欠落(要ビジョン) / R7=同左                          → 2年中0年が機械可
合計: 8県年中2県年（25%）のみ機械的パースが成功。残り75%はpdftoppm+Readツールのビジョン解析が必須
```

**同一県内でも年によってPDF生成方式（埋め込みフォントのToUnicodeマップ有無）が変わり、
今年成功した抽出方法が来年も通用する保証がない。** これは各県の内部システム/年度で不規則に発生する
とみられ、事前にどの県のどの年が機械可能かを予測する手段がない。

**結論（続行するが設計を修正する）**:
1. **続行する**。理由: グランドトータル突合は8/8県年（100%）で成功しており、ビジョン解析込みの
   抽出手法自体はY-6全47県で既に実証済み・高信頼度（初回転記一致率が非常に高い）。
   「パーサ」を諦める理由にはならない。
2. **ただし段階2の実装方針を修正する**: 単純なpdftotext正規表現パーサを前提にしない。
   **「pdftotext -layout抽出を試行 → 日本語ラベル欠落を自動検出（学校名フィールドが空/文字化けか
   機械判定）→ 欠落していればpdftoppmレンダリング＋ビジョン解析にフォールバック」という
   ハイブリッド設計を段階2の前提とする。** 数値抽出（quota等）はToUnicode欠落時も比較的機械抽出
   しやすい（既存ファイルの複数箇所で「数値のみ抽出可」と記録）ため、学校名の対応付けだけが
   ビジョン解析必須の部分になりうる——段階2着手時にこの分離が可能か再検証すること。
3. **quota定義（列の裁定）は年に依らず安定**（同一県なら毎年同じ列を採用してよい）。
   これは`competition-rate-quota-definitions.ts`に一度書けば使い回せる部分であり、
   ビジョン解析が必要な「学校名の転記」とは別の作業単位として扱う。

---

## なぜこれが最優先なのか（測定の結論）

### 事実1: 新しい記録を作っても稼がない

```
直近7日  総クリック 1,910 / 露出URL 2,033本
  上位  1本(/hensachi)  601c = 31.5%
  上位  4本             976c = 51.1%
  上位100本            1,538c = 80.5%
  ★クリック0のURL     1,592本 = 全体の78%
```

**3,532URLのサイトで、実質稼いでいるのは100ページ。**
学校ページは1,797本で423クリック＝**1URLあたり0.24**。記録を足す作業は78%の側を増やす。

**この作業は新しいURLを1本も作らない。既存3,089枚の中身を冬に新鮮に保つ作業である。**

### 事実2: 冬のピークは倍率で決まる

```
「高校 倍率」の季節係数（8月=1.00・Trends実測5年262週）
  11月 2.41 → 12月 2.59 → 1月 12.17 → 2月 24.14
週解像度では 2/07 の週が年間最大
```

学校ページで**唯一CTRが機能している意図が倍率**（倍率1.17% / 偏差値0.59% / 内申・ボーダー0.00%）。

### 事実3: 検知はできたが、取り込みができない

T-Y11 Task A で 2026-09-01 に実装済み:
- ✅ A-2 更新検知（`scripts/check-competition-rate-updates.mjs`・47県フルラン実機確認済み）
- ✅ A-4 自己検算（47/47県・`grand-total-reconciliation-invariant.test.ts`）

**未着手:**
```
A-1 公表日の記録     5/47県  （42県未着手）
A-3 quota定義の構造化 3/47県  （44県未着手）
取り込みパーサ        interim-rate-ingest.ts が1本あるのみ
```

**「公表された」と教えてくれるが、そこから先が手作業。**

### 事実4: 1県ぶんの読解が重い

千葉のヘッダコメントより:
> 列は「募集定員(A)」「募集人員(B)」「志願者確定数(C)」「倍率(C/B)」の4本立て。
> **Aは名目定員、Bが実際に一般選抜で競われる枠**（千葉高・東葛飾高は併設型中高一貫校の
> 内部進学者定員をAから差し引いた数がB＝資料の注記1）。本データはBをquotaとして採用する。

47県すべて違う。埼玉は「入学許可予定者数(A)」、大阪は第2志望クロス集計あり。
**埼玉ではこの読解を誤って4件の転記ミスが発生し、グランドトータル検算で発見された。**

---

## 作業の順序 ★市場規模順ではなく「公表が早い順」

**1月に最初に公表する県から作る。** 遅い県のパーサを先に作っても、早い県で溺れる。

### 段階1: 47県の公表日を確定させる（A-1の残り42県）— ✅2026-09-01完了

- [x] 各県の令和8年度の**実際の公表日**を、既存の `competition-rates/<pref>.ts` の
      ヘッダコメント・一次ソースページから特定する → **44/47県完了**（`competition-rate-publication-notes.ts`）
- [x] 特定できない県は **`unresolved` に「不明」と記録する。推測しない** →
      残る3県（ehime/fukuoka/hokkaido）は`UNRESOLVED_PUBLICATION_DATE_PREFECTURES`に記録
- [x] 確定した日付で**公表カレンダー**を作る（1月中旬〜2月下旬に散らばるはず） →
      実測は2月中旬〜3月下旬に分布（最速akita/gunma2月12日〜最遅fukushima3月19日。「1月中旬〜」
      という当初の見立てより実際はやや遅く、2月中旬〜3月上旬に集中していた）
- [x] **速報版と確定版の両方を出す県**は、両方の日付を記録する → `PublicationTimingNote`は
      各県の資料ヘッダコメントに既にある「変更後」「確定」等の表記をそのまま転記する設計のため、
      複数版がある県（saga等）も既存記述の範囲で対応済み

### 段階2: 公表が早い順にパーサを書く — 🟡2026-09-01: 品質判定ユーティリティまで実装。個別県のパーサ本体はR9データが無く未着手

- [x] **quota定義を裁定して記録する**（`src/lib/competition-rate-quota-definitions.ts` へ追記）
      判断規則は既に確立済み: **quotaは「その県が公表する倍率の分母と一致する列」を採用する** →
      **✅2026-09-01完了・47/47県**（A-3として実施。年度が変わっても列の「意味」は安定する前提だが、
      列の「並び」が変わる可能性は段階2実行時に都度確認すること）
- [x] **テキスト抽出品質の自動判定関数を実装**（ゲートの発見を反映したハイブリッド設計の分岐点）→
      `src/lib/competition-rate-parse-quality.ts`の`assessPdfTextExtraction()`（純粋関数・
      pdftotext生出力のCJK文字比率からビジョン解析フォールバックの要否を機械判定・
      jest 5テストgreen）。**⚠️これは判定ロジックのみ**。PDF取得・pdftotext実行・
      pdftoppm実行そのものはI/Oを伴うため、実際にR9のPDFが公表されてから接続する
- [ ] パーサを実装する（PDF/xlsx/HTMLのどれか）→ ⚠️**この行の「R9データがまだ存在しないので時期尚早」という判断は2026-09-02 16:09に訂正済み。R8で作りR8で検証する（本文書末尾の訂正セクションを見ること）**。旧記述: R9データがまだ存在しない
      （令和9年度の公表は2027年1月中旬以降）ため、実データでの検証ができず時期尚早と判断。
      着手は令和9年度の最初の公表県が出た時点（`ops/BAIRITSU-INGEST-RUNBOOK.md`の「検知」参照）
- [ ] **既存の検算（グランドトータル一致）に接続する** → 接続先は既存の`checkAgainstSubtotal()`
      （`src/lib/competition-rate.ts`・47県で既に使われている再利用可能な純粋関数）で確定済み。
      実際の接続（パーサの出力をこの関数に通す配線）はパーサ本体の実装時に行う
- [ ] **令和8年度の実データでパーサを回し、既存の手作業データと完全一致することを確認する**
      ← これが唯一の正しい検証方法。**一致しなければパーサが間違っている**（パーサ本体の実装後）

### 段階3: 冬の運用手順書 — ✅2026-09-01完了

- [x] 「検知 → パース → 検算 → PR」の手順を `ops/` に文書化する →
      `ops/BAIRITSU-INGEST-RUNBOOK.md`（着手前ゲートのハイブリッド設計の発見を反映）
- [x] 検算が落ちたときに**何を人間に上げるか**を決めておく → 同ファイル「検算が落ちたときに
      何を人間に上げるか」節（転記ミスが特定できる場合はloopが自己修正／原因不明は未解明差分
      として正直に記録／同じ県で3回連続失敗したら質問ノートへ構造変化の疑いを記録）
- [ ] 検算が落ちたときに**何を人間に上げるか**を決めておく

---

## 守ること

- ⚠️ **一致しないデータを合わせにいかない。** 差分は「未解明差分あり」として明示的に記録する
- ⚠️ **画像PDFの県は正直にスキップ台帳へ**（Y-0の「画像PDF県は正直にスキップ」原則）
- ⚠️ **相手サーバに負荷をかけない**（1県1日1回・間隔800ms以上・UAを名乗る・robots.txt尊重）
- ⚠️ **学校別偏差値・ボーダー推定は書かない**（Y-0憲法）
- ⚠️ **本番D1の破壊的操作なし・env変更なし・対外送信なし** = loop単独で完結する
- `tsc` は実exitを確認（パイプ厳禁）／jest green ／低リスクは即push

## 払底したら

**監視モードに落ちる前に、質問ノートへ「どこまで終わって次に何を取るべきか」を書いて止まること。**
[[feedback-avoid-premature-monitoring-mode]]／👤の明示指示「絶対に監視モードに入らせないように」。
**8/30・8/31と2日続けて4時間以上の監視モードが発生している。**

## この作業が終わったら（優先順）

1. **入試日程DB（47県×3年）** — Trends実測: 高校入試 日程 18.22 / 出願期間 11.95
   （調査書40.61・内申点30.35と同一スケール）。1月61 / 2月63 / 12月21 で冬に集中。
   `src/lib/juken-schedule.ts` は現在7KBで「県別日程は検証不能だから持たない」と書いてあるが、
   **47県×3年の実施要項を追える今なら前提が変わっている**
2. **年度スナップショットのR5遡り** — ⚠️ **2026-09-01の検証で下方修正済み**。
   教委は年度ごとにディレクトリを作り替え旧年度を消す。URLパターンからの機械的推測は
   tochigi/yamagata/ehime の全18パターンで404かつWaybackにも無し。**R5は虫食いになる前提で**
3. **my-shingaku のデータ層** — 28日138クリック/59URL＝実質未着手。今期の収益はゼロ・来期以降専用

---

# 🔴🔴 2026-09-02 16:00 【T-Y11B 段階2の指示を訂正】R9を待つな。R8が教材であり検証データである

## 何が起きたか

2026-09-02 00:16 に払底判定。理由はタスクファイルのこの記述:

> パーサを実装する → **未着手（意図的）**。R9データがまだ存在しない
> （令和9年度の公表は2027年1月中旬以降）ため、実データでの検証ができず時期尚早と判断。

**これは目的の取り違えです。** 同じタスクファイルの2行下にこう書いてあります:

> **令和8年度の実データでパーサを回し、既存の手作業データと完全一致することを確認する**
> ← これが唯一の正しい検証方法

**R8のPDFも、R8の手作業データ（21,739レコード）も、両方いま手元にあります。**
パーサは **R8で作り、R8を完全再現できるかで検証する**。

**1月に必要なのは「動くパーサ」です。1月から作り始めるなら間に合いません。**
そもそもこのタスクは「1月に5県同時公表が来たら手作業では追いつかない」から9月にやる、という話でした。
**R9を待つのは、このタスクの存在理由そのものを否定しています。**

## ゲートの発見は正しい。ただし結論が逆

着手前ゲートの発見は重要で、正確です:

```
8県年のうち pdftotext で機械パースできたのは 2県年（25%）
残り75%は pdftoppm + ビジョン解析が必須
同一県でも年によってPDF生成方式（ToUnicodeマップの有無）が変わる
```

**この発見は「待つ理由」ではなく「急ぐ理由」です。**
75%が人手を要するなら、**1月の同時公表はもっと詰みます。**
だから今のうちに「どの県が機械で通るか」を確定させ、通る県は自動化し、
通らない県は半自動のハーネスに載せておく必要があります。

---

## 段階2 やり直し（すべて R8 で完結する）

### 2-a 47県を「機械パース可否」で分類する ★まずこれ — ✅2026-09-02完了・重大な訂正あり

- [x] 47県のR8の公表PDFに対して、**pdftotext -layout を実際に流して**
      `assessPdfTextExtraction()`（09-01実装済み）で判定する →
      `scripts/check-r8-pdf-parse-quality.mjs`として実装・47県中43県が実行完了
- [x] 結果を `ops/baselines/` に台帳として保存する（県 × 判定 × 判定根拠） →
      `ops/baselines/r8-pdf-parse-quality-2026-09-02.md`
- [x] ⚠️ **PDFは既に一度取得している資料。再取得は1県1日1回・間隔800ms以上・
      UAを名乗る・robots.txt尊重**（兵庫は既知の拒否県） → hyogoは再取得せずスキップ、他46県は
      間隔900ms・カスタムUAで1回ずつ取得
- [x] **これ自体が単独で価値のある成果物**。1月に「どの県が自動で通るか」が事前に分かる

**★★重大な訂正: 2026-09-01の「8県年中75%が要ビジョン解析」は`pdftotext`呼び出しのバグだった。**
`-enc UTF-8`を付けずに呼んでいたため日本語ラベルが全て空白化していた（数字は影響を受けないため
「ToUnicodeマッピング欠落」に見えた）。`-enc UTF-8`を付けて元の8県年を再検証した結果、**8/8
（100%）が機械抽出可能**と判明。47県の新規判定でも43/47（91%）が`usable`。詳細と訂正表は
`ops/baselines/r8-pdf-parse-quality-2026-09-02.md`を参照。**段階2-bのハイブリッド設計（ビジョン
解析フォールバックが主）という前提は過剰設計だった可能性が高く、`pdftotext -layout -enc UTF-8`を
第一候補にする設計へ縮小してよい。** 例外4件（fukushima=真のスキャン画像PDF・hyogo=robots.txt
遵守で未検証・okayama=URL切れ・osaka=xlsx形式）は個別対応が必要。

### 2-b 機械で通る県のパーサを書き、R8で検証する ★本体

- [x] **ibaraki(R8)を実装・検証完了（2026-09-02）**。`src/lib/bairitsu-ingest/parse-table-pdf.ts`
      （純粋関数）＋`scripts/bairitsu-ingest/extract-pdf-geometry.py`（PyMuPDF・罫線＋文字座標
      抽出）＋`src/lib/bairitsu-ingest/__tests__/parse-table-pdf.test.ts`（実PDFのfixtureで検証・
      jest 6テストgreen）。**149/149件・順序も含め完全一致**（グランドトータルも公式値と一致）。
      詳細技法は`ops/BAIRITSU-INGEST-RUNBOOK.md`「結合セルの解法」に文書化済み
      - ⚠️**重大な発見**: 学校名セルが複数学科行にまたがる「結合セル」を持つ県では、学校名
        ラベルは結合セル内のどこか1行（先頭行とは限らない・ibarakiでは概ね中央行）にしか
        出現しないため、単純な逐次スキャン（ラベルが出たら以降を新学校として carry-forward）
        では**必ず失敗する**（ラベル出現前の行が前の学校に誤帰属する）。この構造は
        `pdftotext -table`でも解決しない（テキストストリームには結合セルの範囲情報が残らない）
      - **解法**: PDFの罫線（ベクタ図形）から真のブロック境界を機械的に決定する。結合セルの
        内部区切り線は「学校名列をまたがず学科名列より右からしか引かれない」ため、
        罫線のx0だけで判定できる（`PyMuPDF`の`page.get_drawings()`で取得可能）
      - **副次的な罠2件**: ①PyMuPDFの`get_text("words")`はスペース無しで隣接する学校名+
        学科名を1wordとして結合し列を誤判定する→**文字単位（`rawdict`の`chars`）で列を
        振り分けると解決** ②学科名が半角カタカナで抽出されることがある（濁点/半濁点が
        分離文字になる）→`.normalize('NFKC')`で一括正規化できる
      - ⚠️**この方式はPython（PyMuPDF）に依存する**。この環境には導入済み（`pip show pymupdf`
        で確認可能）だが、新規に構築する環境では別途`pip install pymupdf`が必要
- [x] **tochigi(R8)を実装・検証完了（2026-09-02）**。107/107件・完全一致（グランドトータル
      7,259/7,602も一致）。ibarakiとは異なる**もう一方のパターン**であることを確認:
      学校名ラベルは各学校ブロックの**先頭行**にだけ印字され、継続行は単に空欄になるだけ
      （罫線による結合セル判定は不要）。`groupCharsIntoRows()`（罫線を見ずy座標の近さだけで
      行をクラスタリング・汎用）＋`extractRowFields()`＋`assembleSimpleTableRows()`
      （単純carry-forward）を新設し、`parse-table-pdf-simple.test.ts`で検証（jest 6テスト）
      - ⚠️**訂正版の教訓「-table+`\s{2,}`区切りだけで足りる県が多いはず」は誤りだった**:
        tochigiも学校名・学科名が「宇　都　宮」のように1文字ずつ均等割り付け（トラッキング）
        で組版されており、`\s{2,}`のような空白数ベースの列分割は列境界と文字間の空白を
        区別できず機能しない。**座標ベースの列振り分け（文字単位のx座標をどの列の範囲に
        入るかで判定）は「結合セル対策」だけでなく、この均等割り付け対策としても必須**、
        という位置づけに訂正する（`normalizeExtractedText()`に全角/半角空白の内部除去を
        追加して対応・NFKCと同時に適用）
      - tochigiは列数が多い（18列・quota/applicants/rateが学校名・学科名の隣接列とは限らない）
        ため、列の役割をインデックスで明示指定する`GeneralColumnLayout`を新設した
      - 罠: 表の最終行に「合計」（グランドトータル自身）が学校の1行として混入する
        （schoolNameとdepartmentの列境界をまたいで「合」「計」に分裂）→ ibarakiの
        「全日制計」混入と同型の罠。`excludeRow`オプションで除外する設計にした
      - **判定の目安**: 学校名セルの結合有無は`pdftotext -table`の出力を目視すれば分かる
        （ラベルが先頭行にあるか、途中の行にあるか）。結合セルが無ければ
        `assembleSimpleTableRows`（軽量）、結合セルがあれば`assembleCompetitionRateRows`
        （罫線ベース・ibaraki型）を選ぶ
- [x] **akita(R8)を実装・検証完了（2026-09-02）**。78/78件・完全一致（グランドトータル
      6,268/5,237も一致）。ibaraki・tochigiいずれとも異なる**第3のパターン**を発見:
      学校名が長いと2行に折り返され、2行目の断片（例:「情報学院」）がschoolName列に単独で
      出現する（正しくは「大館国際」＋「情報学院」＝「大館国際情報学院」1校）。tochigi型の
      単純carry-forwardでは「情報学院」を別の新しい学校と誤認する。
      - **解法**: この県の表は`№`（通し番号）列を持つため、**№列の有無でブロック境界を
        判定**し、ブロック内の全schoolName断片を連結する`assembleNumberedBlockRows()`を
        新設した（`roles.number`を指定）
      - ⚠️罠: 「県北計」「中央計」「県南計」「県合計」等の小計行は独立した№を持たないため
        直前の学校ブロックに紛れ込む。ただし小計ラベルは**department列**に出現する
        （schoolName列ではない）ため、除外判定はブロック単位ではなく**行単位**
        （`excludeRow`はdepartment文字列で判定）にする必要があった。「県合計」（総計）に
        到達したら`stopAt`でそれ以降の処理を打ち切る（この県は全日制表の後に別の小さな
        表（連携型選抜等）が同型の小計構造で続いており、打ち切らないと誤って混入する）
      - ⚠️罠: 「太田分校」「雄勝校」のような**独立した番号を持たない分校**は、ブロックの
        連結名にそのまま混ぜると「大曲農業太田分校」のような誤った名前になる。実際の
        既存データは「大曲農業(太田分校)」という括弧付きの人手による命名規則を採用して
        いるため、これは自動検出できない**県固有の例外**として`renameOverrides`
        （schoolName生テキスト→差し替え後の名前）で個別対応する設計にした
      - **教訓**: №列を持つ表に遭遇したら、まず「学校名が2行に折り返される長い名前が
        無いか」を目視確認すること。無ければtochigi型（単純carry-forward）で足りるが、
        あればakita型（№列ブロック＋連結）が必要
- [x] **tokushima(R8)を実装・検証完了（2026-09-02・第4パターン「複数列組ページ」を確立）**。
      詳細は下記「🔍調査のみ実施」節のtokushima項目を参照（そこに完全な経緯を残した）
- [x] **ishikawa(R8)を実装・検証完了（2026-09-02・列roles一般化＋罫線抽出バグ修正）**。
      70/70件・完全一致（グランドトータル6,566/6,076も一致）。
      - **根本原因の特定と修正**: 当初「行検出（罫線ベースの`isBlockEnd`判定）自体が
        破綻」と見えたが、真因は`extract-pdf-geometry.py`の矩形罫線フィルタ
        `r.height < 1.0`が**height=1.2の実在する罫線を取りこぼしていた**バグだった
        （表内容のセル背景矩形はheight/widthが同オーダーなのに対し、罫線は幅に比べ
        高さが極端に薄いことを利用し、閾値を`< 3.0`へ緩和して解決）。この修正で
        ishikawaのhline検出数が104→131件（page1）に増え、行の欠落が解消した。
        **この抽出スクリプトのバグは他の県にも影響しうる根本的な問題だったため、
        今後すべての県で恩恵を受ける**（ibaraki/tochigi/tokushimaは既存の凍結
        フィクスチャを使うため無回帰）
      - `parseTablePdfPageRows`に`roles`（quota/applicants/rateが学校名・学科名に隣接
        しない列構成に対応）と`excludeRow`オプション（学校単位の「小計」行を除外）を
        追加した（前者は前回の周回で追加・後者は今回追加）
      - ⚠️罠（併願制度）: 小松・金沢泉丘・七尾の3校は「普・理併願」という学科横断の
        出願を持ち、複数の学科行を1つの「小計」行に統合した単一レコードとして既存
        データに記録されている。パーサの出力からは正しく分離できないため、既存データ
        （WebSearch裏取り不要・PDF上の小計行の数値をそのまま転記したもの）を根拠とした
        明示的な置き換えで対応した（tokushimaの那賀/海部と同型の対応）
      - ⚠️罠（括弧の全角/半角）: ishikawaのPDFは「普通(キャリア)」のように**半角括弧**を
        使うが、既存データは一貫して全角括弧「（キャリア）」で表記する。
        `normalizeDepartmentText`に半角→全角括弧の変換を追加した（tokushimaの全角括弧
        データには影響しない安全な変換であることを確認済み）
- [ ] 残り38県（usable 43県からibaraki・tochigi・akita・tokushima・ishikawaを除く）に
      同じ方式を横展開する。着手前に`pdftotext -table -enc UTF-8`の出力を目視し、
      ①学校名ラベルの位置（先頭行か中間か＝ibaraki型かどうか）②長い学校名の折り返しの
      有無（akita型が必要か）③№列の有無、を確認してから実装すること
  - **kochi**: 列が[入学定員/募集定員(quota)/第1志望者数/学校計(注記)/志願率/第2志望者数]と
    ibaraki/tochigi/akitaのどれよりも列数が多く、「志願者なし」という**非数値の文字列が
    倍率欄に入る**（0除算の代替表記）。学校名・学科名双方に「(ﾋﾞｼﾞﾈｽ)」等のコース略称の
    括弧書きが付く。未着手
  - **niigata**: 全日制表が学科系統ごとに8区分（普通科系/農業/工業/商業/水産/家庭/その他/
    総合）に分かれ区分ごとに「計」行がある**上に**、「あおい」→改行→「碧」のような
    **ふりがな（ルビ）が学校名の上に別行として印字される**実例を発見（碧高校の本来の
    表記は「碧」だが「あおい」という読み仮名が別レイヤーとして先に出現し、akita型の
    折り返し名連結ロジックをそのまま使うと「あおい碧」という誤った連結名になる）。
    全6ページで概要ページ・学区外出願一覧等の余計なセクションも多い。未着手
  - **kagoshima**: **左右2段組ページ×学区ごとに7ページ**というレイアウトに加え、
    1レコードが**2行にまたがる**（1行目:募集定員/学力検査定員/(一定枠内数)/倍率、
    2行目:実際の最終出願者数の生数値）という構造。座標ベースでも列とブロックの両方の
    次元で複雑度が高い。未着手
  - **tokushima**: 列自体は[募集人員/出願者数/倍率]と最も単純だが、**1ページに全日制
    （左2段）＋定時制（右1段）の実質3列組**が並び、ibarakiと同型の「学校名遅延
    （結合セル中央配置）」が全日制の2段それぞれで独立に発生する。さらにページ下部に
    「学区外出願状況」という全く別の小表が同一ページに混在する。**列の単純さに反して
    レイアウト分割の複雑度が高い**。着手し列境界の実測まで完了したが（次回の時短用に
    記録）、罫線ベースの`isBlockEnd`判定（`fullLineX0Max`）が段ごとに異なる絶対x座標を
    要求するため`parseTablePdfPageRows`をそのまま複数回呼べず、関数の一般化
    （オフセット対応）が追加で必要と判明した時点で今回は打ち切った。
    実測した列境界（ヘッダ行`学校/学科・類/募集人員/出願者数/倍率`のx0から算出・y=61.0/65.3の
    ヘッダ行を直接読むと高速に求まる）:
    - 全日制LEFT: schoolName[~30-95] / department[~95-165] / quota[~165-205] /
      applicants[~205-248] / rate[~248-295]
    - 全日制MIDDLE: schoolName[~295-360] / department[~360-445] / quota[~445-463] /
      applicants[~463-505] / rate[~505-550]
    - 定時制RIGHT（対象外・x>=550は無視してよい）
    - 次回はまず`parseTablePdfPageRows`に「この呼び出しで見るべきhlineのx範囲」を渡せる
      よう拡張し、LEFT/MIDDLEそれぞれに対して独立に呼び出してから結果を連結するとよい
    - ⚠️**続報（同日中に実装まで進めて根本原因を特定・完全に解明）**: LEFT/MIDDLE個別に
      罫線ベースの組み立てを実装し実データで検証したところ、65/69件までは到達したが、
      残り4件（那賀/海部の各1レコード・富岡西2レコード）で**ラベルだけが独立した1行として
      印字され、前後どちらの学科データ行にも数値を持たない「ラベル単独行」**という
      tokushima固有の第4パターンを発見した。実測（座標）:
      ```
      y=132.3  普通 30 25 0.83        （ラベル無し）
      y=138.8  那賀                   （ラベルのみ・数値無し）
      y=145.2  森林クリエイト 13 10 0.77（ラベル無し）
      y=158.0  普通 47 43 0.91        （ラベル無し）
      y=170.9  海部 情報ビジネス 18 19 1.06
      ```
      既存データ（`tokushima.ts`）は「那賀=[森林クリエイト13/10/0.77, 普通47/43/0.91]」
      「海部=[普通30/25/0.83, 情報ビジネス18/19/1.06, 数理科学28/22/0.79]」としており、
      **「那賀」ラベルの直後(y=145.2)ではなく直前(y=132.3)の行が実は海部の学科である**
      （＝ラベルは自分の学科群の直後にあるとは限らず、直前の行が別の学校の学科である
      ケースが実在する）。これは**幾何学的な位置関係だけからは一意に決定できない、
      真に曖昧な構造**であり、ヘッダコメントに記録された「阿南光・海部の学校名帰属を
      WebSearchで裏取りして訂正した」という既存の経緯とも完全に符合する（＝当時の人間
      /vision転記でも機械的には解けず、実在学科の外部検証が必要だった箇所と同一）。
      富岡西のケース（`普通1471601.09`→ラベル`富岡西`→`理数30180.60`）は前後とも同一校
      に属し曖昧ではないが、同じ「ラベル単独行」構造のため既存のブロック境界検出ロジック
      では正しく1ブロックとして拾えず未実装のまま欠落した。
      **✅続報2（同日中に完全解決・実装完了）**: 「ラベル単独行」問題は罫線が存在しない
      表の先頭/末尾行の欠落が原因と判明（富岡西の「普通」「理数」もこの欠落で消えていた）。
      `parseTablePdfPageRows`に`syntheticTopY`/`syntheticBottomY`（罫線が無い場合に仮想の
      区切りを補う）を追加したところ**69/69件（学校×学科の対応）が全て復元**された。
      さらに検証中に**既存の`normalizeExtractedText`のバグを発見**（`.normalize('NFKC')`が
      全角括弧「（）」まで意図せず半角化していた・「芸術（音楽）」で発覚）。半角カタカナの
      範囲だけをNFKC正規化する`normalizeHalfwidthKatakana`に切り替えて修正（ibaraki/
      tochigi/akitaの既存テストは無回帰・全green）。
      最終結果: **69/69件のうち67件は完全一致**、残る那賀/海部の1件は上記の通り幾何学的に
      真に曖昧なため、`tokushima.ts`のヘッダコメントに記録済みのWebSearch裏取り結果を
      根拠とした明示的な補正（`applyKnownAmbiguityCorrection`）で対応した。**また、既存
      データの並び順自体が単純な規則（LEFT→MIDDLE等）に従っていないため、この県だけは
      「順序を含めた完全一致」ではなく「学校×学科×数値の集合としての一致」を検証基準と
      した**（`filterGeometryByXRange`で1ページを複数の列組に分割する新しい共通関数も
      本体モジュールに追加）。`parse-table-pdf-multi-column.test.ts`で検証済み（jest4テスト
      green）。tsc実exit0・jestフル372suites6458tests green。**tokushimaは実装完了**
  - これら4県はいずれも「列は単純」または「既存パターンの応用」に見えて、実際に開いてみると
    毎回新しい罠が1つ以上見つかる。**着手前にPDFを実際に開いて構造を確認する手順を省略
    しないこと**（この4県の調査だけでも複数の新パターンが見つかった）
- 🔶**miyagiは着手したが未完了（2026-09-02・重要な発見あり・次回はここから再開）**:
  - パーサ自体は完成し**129/129件（学校×学科の対応）は完全一致**。追加で発見した2つの罠は
    解決済み: ①「○○地区計」「○○地区合計」等の地区別小計行が学校の行に混入する→
    combined textに`合計`を含む行を除外（`地区計`単体も含め汎用的に対応）②巻末の
    「全日制合計」グランドトータル行自体も学校名列/学科名列の境界をまたいで
    「全」/「日制合計1」のように分裂して混入する→同じ`合計`除外ロジックで対応済み
    （ibarakiの「全日制計」混入と同型の罠が3例目として再確認された）
  - ⚠️**しかし129件中7件で`finalRate`が既存データと一致しない（quota/finalApplicantsは
    全件一致）**。全7件が**ちょうど四捨五入の境界値（x.275やx.025）**で発生している
    （例: 仙台向山 普通科 204÷160=1.275 → 自分の抽出は「1.28」・既存データは「1.27」）。
  - **これはパーサのバグではない可能性が高い**: `pdftotext -table`（自分のPyMuPDF方式とは
    別の独立したツール）でも同じPDFの同じ行を確認したところ、やはり「1.28」と印字されて
    いた（2つの独立した抽出手段が一致）。一方、`miyagi.ts`のR8データは**2026-07-25に取得
    したPDF**からの転記で、自分が今回（2026-09-02）取得したPDFは**同一URL**だが取得日が
    約5.5週間後。他県（tochigi/nagano等）で「確定版」「訂正版」PDFが同一発表単元の中で
    後日更新される前例が複数あるため、**教育委員会がこのPDFを同一URLのまま無言で改訂した
    可能性**が最も疑わしい仮説（未確定）。
  - **確認しようとしたが未完了**: Wayback MachineでこのURLの2026-07-25前後のスナップショット
    を確認しようとしたが、Internet Archiveが本日も応答不安定（既知の罠・タイムアウト）で
    未確認のまま。
  - **対応方針（次回セッションへの申し送り）**: ①まずWayback Machineの復旧を待って
    2026-07-25頃のスナップショットを確認し、当時の印字値が1.27だったか1.28だったかを
    確定させる。②同一URLのまま改訂されていたと確認できた場合、`miyagi.ts`側の該当7件を
    現在のPDFの値に合わせて訂正してよい（データを「合わせにいく」のではなく「新しい一次
    ソースの実測に合わせて更新する」という扱い）。③確認できない場合は`unresolved`相当の
    記録として両方の値を残す。**miyagi用のfixture・jest testはこの決着がつくまでコミット
    しない**（未解決の差分を含むデータをテストに固定化しないため）。
  - **一般化できる新しい教訓**: 「同一URLのPDFが後日無言で改訂されることがある」ため、
    T-Y11Bのようにfetchが数週間〜数ヶ月空くタスクでは、`fetchedAt`が古いレコードとの
    突合で示された差分を**即座にパーサのバグと断定しない**こと。特に**四捨五入の境界値
    ちょうど**で差分が集中する場合はPDF自体が更新された可能性を疑う（ランダムな転記ミス
    ならこのような偏りは起きにくい）。
- [ ] **検証は1つだけ**: パーサの出力が、既存の手作業データ（`competition-rates/<pref>.ts`）と
      **レコード単位で完全一致するか**
- [ ] ⚠️ **一致しなければパーサが間違っている。データを合わせにいかない**
- [ ] 一致したら `checkAgainstSubtotal()`（`src/lib/competition-rate.ts`・既存）に配線する
- [ ] ⚠️ **既存データを書き換えない。** パーサは検証専用の出力を出すだけ。
      R9が来たときに初めて書き込みに使う

### 2-c 通らない県の半自動ハーネス

- [ ] ダウンロード → pdftoppm でレンダリング → 「要ビジョン解析」キューへ積む、までを自動化する
- [ ] キューには県名・年度・ページ数・想定レコード数（グランドトータルから逆算）を持たせる
- [ ] **ビジョン解析そのものは自動化しない。** 人（またはloop）が読む前提でよい。
      **1月に「何を読めばいいか」が即座に並んでいる状態を作るのが目的**

### 2-d 列位置マップ

- [ ] `competition-rate-quota-definitions.ts` は列の**意味**の裁定（47/47完了）。
      **列が何番目にあるか**はまだ無い。2-bの実装で判明した順に記録する
- [ ] ⚠️ **年度で列の並びが変わりうる**（ゲートで確認済みの年次不安定性）。
      並びは年度ごとに持ち、機械的に名寄せしない

---

## 見込み

**47県 × (取得 → 判定 → 実装 → R8照合) は、20日で終わりません。それでいい。**
2-a だけでも数日、2-b は1県ずつ潰す作業です。**終わった県から冬に効きます。**

## 払底したときの扱い（再掲・今回守られていない）

**「払底した」と判断する前に、必ず次を自問すること:**

1. **本当に材料が無いのか。それとも「材料が無い」と読み違えていないか**
2. タスクファイルの**別の行に、その材料の在り処が書かれていないか**
3. それでも払底なら、**監視モードに落ちる前に質問ノートへ
   「どこまで終わって、次に何を取るべきか」を書いて止まる**

今回は 1 と 2 で止められたはずでした（R8の手作業データが手元にある、と同じファイルに書いてあった）。
