# T-Y11C finalRate を「県ごとの公表流儀」まで含めて機械検証する

**着手: T-Y11B 段階2-b の次の県に進む前に、これを先にやる（半日）。**
**理由は下の「なぜ先か」。パーサを1県増やすより、47県ぶんの既存データを守るほうが先。**

⚠️ **この文書の数値は対話セッションが 2026-09-02 22:40 に集計したもの。loop は自分で数え直してから動くこと。**
集計スクリプトは対話セッション側の scratchpad にあり、リポジトリには入れていない（loop が自分で書き直す前提）。

---

## きっかけ

2026-09-02 22:07、miyagi のパーサが **既存データ側の計算バグ**を発見した。
`finalRate` 7件が四捨五入の境界値（204÷160=1.275 等）で、JS の `Number(x.toFixed(2))` が
浮動小数点の都合で **切り下げていた**（1.28 が 1.27 になっていた）。

**これは「miyagi の1県の問題」ではない。同じ実装で全県の finalRate が作られていれば、
同じ条件のレコードすべてに同じことが起きている。**

---

## 対話セッションの実測（loop はこれを再現してから進むこと）

47県の `src/data/competition-rates/*.ts` を**テキストとして**読み、
`quota` / `finalApplicants` / `finalRate` の3つ組を正規表現で抜き、
**整数演算だけで**（JS の浮動小数点を一切通さず）検算した。

```
検算できたレコード       21,875 件
四捨五入の境界値(x.xx5)   3,181 件 (14.5%)  ← バグが顕在化しうる母数
単純な四捨五入と食い違う  1,037 件 (4.74%)
```

食い違い 1,037 件の内訳（「データが正しい説明」を先に試した結果）:

| # | 説明 | 件数 | 判定 |
|---|---|---|---|
| ① | 県が**小数第1位**で公表している | 835 | データは正しい（yamaguchi 446 / nagasaki 378 が大半） |
| ② | 県が**切り捨て**で公表している **or** toFixed の丸めバグ | 175 | ⚠️**この2つは算術だけでは区別できない** |
| ③ | **どの説明にも当てはまらない** | 27 | ★要調査 |

### ⚠️ ②が最重要の論点

**切り捨て公表と、JS の浮動小数点バグは、境界値では同じ値を出す。**
だから「stored が切り捨てと一致する」ことは、データが正しい証拠にならない。

miyagi では **PDF を実際に読んだから**「PDFは1.28と印字している＝バグ」と確定できた。
残り 170 件は PDF を読むまで確定できない。**推測で書き換えないこと。**

### ③原因不明の27件（全件）

```
yamanashi 20件  ← 193件中20件（10%）。ここが最大
yamaguchi  5件
aichi      1件   744/300 = 2.4800 だが 2.49 が入っている
hokkaido   1件   163/200 = 0.8150 だが 0.815 が入っている（小数3桁＝明らかな異物）
```

**yamanashi の20件には規則性がある。** stored が一貫して計算値より小さい:

```
 80/ 80 = 1.0000  → 保存 0.99
 55/ 55 = 1.0000  → 保存 0.98
118/108 = 1.0926  → 保存 1.08
 11/ 21 = 0.5238  → 保存 0.48
 17/ 16 = 1.0625  → 保存 1
```

`quota × stored` を逆算すると **finalApplicants より1〜2小さい値**になる。
→ 仮説: **finalRate と finalApplicants が別の時点のスナップショット**
（倍率は志願変更前、志願者数は志願変更後、等）。
⚠️ **仮説のまま扱うこと。PDF で確かめるまで書き換えない。**

---

## なぜパーサ1県より先か

1. **範囲が違う。** パーサ1県 = 1県ぶんの自動化。この検証 = **47県21,875件すべての健全性**
2. **9/6 に外へ出る。** 提案書の第5章は「正確性をどう担保しているか」で 226行削除を挙げている。
   **買い手が最初にやる検算は `applicants ÷ quota` と `finalRate` の突合**。
   4.74%が合わないことを、こちらが先に説明できる状態にしておく必要がある
3. **これから作るパーサが全部楽になる。** 不変条件があれば、新しい県のパーサ出力を
   「県の公表流儀に合っているか」で自動判定できる
4. **fail-closed が効いた前科がある。** 2026-09-02 00:08、kagawa を台帳に足したとき
   `PUBLIC_LICENSE_SUMMARY` の5テストが落ちて漏れを検知した。同じ形をここにも作る

---

## Y11C-1 検算を純関数として実装する（`src/lib/` に置く）

- [ ] `finalRate` の妥当性を判定する純関数を作る。**整数演算のみ**（`toFixed`/`Math.round` を使わない）
      - `round2` / `round1` / `trunc2` の3つを整数で実装する
      - 入力 `(quota, applicants, storedRate)` → どの流儀に一致するかを返す
- [ ] ⚠️ **`Number(x.toFixed(2))` を検算側で使わない。** バグの実装で検算したら意味が無い
- [ ] 単体テストに **miyagi で実際に見つかった境界値**（204/160=1.275 等）を必ず入れる

## Y11C-2 県ごとの「公表流儀」を明示的な台帳にする

**いまは県ごとの丸め方がどこにも書かれていない。だから食い違いが誤りに見える。**

- [ ] `ops/` に県 → 流儀（`round2` / `round1` / `trunc2` / `mixed`）の台帳を作る
- [ ] **根拠を1県1行で書く。** 「PDFで確認した」のか「算術からの推定」のかを区別して書く
      ⚠️ 推定を確認済みと書かない（Y-0）
- [ ] 実装済み6県（ibaraki/tochigi/akita/tokushima/ishikawa/miyagi）は**PDFを読んだ実績があるので確定扱いにしてよい**
- [ ] 残り41県は**推定**と明記する

## Y11C-3 不変条件テストにする（fail-closed）

- [ ] 47県 × 全レコードを走査し、**台帳の流儀に一致しないレコードがあれば落ちる**テストを書く
- [ ] ③の27件は**既知の例外リスト**として明示的に列挙して通す
      （握りつぶさない。リストに理由を1件1行で書く）
- [ ] ⚠️ **例外リストが増えたら落ちるようにする。** 新しい県のデータを足したとき、
      黙って例外が増えることを防ぐ（件数を固定値でアサートする）

## Y11C-4 27件のうち確定できるものだけ直す

- [ ] `hokkaido` の `0.815`（小数3桁）は**PDFを見て確定させる**。1件だけなので確実に片付く
- [ ] `aichi` の 744/300=2.48 vs 2.49 も同様に1件
- [ ] ⚠️ **yamanashi 20件と yamaguchi 5件は PDF を読むまで触らない。**
      仮説（別スナップショット）をタスクファイルに書いて次に回してよい
- [ ] ⚠️ **②の175件は一切触らない。** 該当県のパーサができた時点で自動的に決着する

---

## DoD

- [ ] 21,875件を整数演算で検算するテストが CI で回っている
- [ ] 県ごとの丸め流儀が台帳にあり、**確定と推定が区別して書かれている**
- [ ] 既知の例外が件数固定でアサートされている（黙って増えない）
- [ ] `tsc` 実exit 0 / jest green
- [ ] `ops/` に結果サマリを残す（提案書の第5章に引用できる形で）

## 守ること

- ⚠️ **推測でデータを書き換えない。** 一次ソース（PDF）を読むまでは「不明」と書く（Y-0）
- ⚠️ **切り捨て公表と丸めバグを算術で区別できると思わない。** ここが最大の罠
- ⚠️ **検算に `toFixed` を使わない**
- ⚠️ 終わったら **T-Y11B 段階2-b（次の県のパーサ）に戻る**。ここに居座らない
