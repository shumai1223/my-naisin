// 岡山 R9 頁7のパッチ(林野・勝間田)。1置換ごとに期待件数ちょうどを検証する。
const fs = require('fs');
const f = 'C:/Users/E24054/my-naisin/src/data/school-selection-methods/okayama.ts';
let s = fs.readFileSync(f, 'utf8');
const rep = (from, to, expect = 1) => {
  const n = s.split(from).length - 1;
  if (n !== expect) throw new Error(`一致数 ${n} != ${expect}: ${from.slice(0, 70)}`);
  s = s.split(from).join(to);
};
// 林野 普通 特別: 募集人員6→10人程度、重視する実績の記載が3項目に
rep("募集人員50%。重視する実績を示した選抜の募集人員は6人程度。重視する実績:サッカー(男子・女子)又は野球(男子)。面接は集団", "募集人員50%。重視する実績を示した選抜の募集人員は10人程度。重視する実績:学校外の探究活動の発表会やコンテストの参加実績/サッカー又は野球/英語検定準2級以上合格(3項目が縦に並んで印字・対応は表から確定できないため区切って併記)。面接は集団");
// 勝間田(5系列共通): 特別入学者選抜の募集人員 50%→80%
rep("検査概要:口頭試問(討論、発表を含む)。与えられた課題について、自分の考えをまとめて表現する。募集人員50%。", "検査概要:口頭試問(討論、発表を含む)。与えられた課題について、自分の考えをまとめて表現する。募集人員80%。", 5);
// 勝間田(5系列共通): 一般入学者選抜の重視する事項
rep("重視する事項:生徒会活動、部活動、ボランティア活動の実績(5系列で共通)", "重視する事項:生徒会活動、部活動、校外におけるスポーツ・文化・科学・ボランティア活動の実績(5系列で共通)", 5);
fs.writeFileSync(f, s);
console.log('patched p7');
