# LOOP_CONTRACT.md — 常駐ループ（Sonnet 5）の憲法

- 制定: 2026-08-11（GAUNTLET LOOP PHASE 5）
- 適用対象: `/loop` で起動する無人セッション
- **毎周回、着手前にこのファイルと `memory/loop-question-note.md` の冒頭を読むこと。**

---

## 0. 今季の主食（迷ったらここへ戻る）

### 🆕 2026-09-05 02:2x時点の状態（セッション再開・T-M1-2(C10-1)投入完了・push済み）

前回セッション終了直後（01:27最終commit）から約1時間空けて再開。MCP接続（Gmail）は生きていることを
確認済み（前回セッションの`taskkill /IM node.exe`事故の影響なし）。Gmail新着11件のうち和歌山/千葉/
長野/栃木の教委回答4件は既に`src/lib/data-license-ledger.ts`へ反映済みと確認（新規対応不要）。

T-Y12が完了しA群の次の未着手項目を探した結果、`ops/tasks/T-M1-affiliate-ctr.md`のM1-2
（学校ページへの広告1枠投入・M1-1から1週間空けて2026-09-05以降に着手する日付ゲート）が
ちょうど本日解禁されていたため着手・完了。`src/app/pref/[code]/school/[schoolCode]/page.tsx`に
`campus-banner`（塾4社中EV最上位タイ・既存設置数最少）を1枠追加、`placement="school"`+
`trackView`で計測対応、回帰テスト新設。tsc実exit0・jestフルスイート457suites6892tests green。
push済み（`a66b14f`）。

**次に再開するセッションがまず行うこと**:
1. Gmail/GA4/GSC/Trends MCP接続確認（前々回セッションの事故の再発が無いか）
2. Gmail新着返信の確認
3. T-R1週次チェック（W-1/W-2/W-4/W-5/W-5b/W-6/W-7）が前回08-30 12:4x実施のまま09-02目安を
   過ぎて未実施＝優先度高い。M1-2投入直後のため今後は週次確認にW-7(cta_view定点観測)の
   桁比較も含める
4. T-Y12段階3（ICS配信統合・任意）、T-S13A A-2（👤判断待ち・返信なければ着手しない）、
   T-N1-N4残項目（N1-2は9/08解禁）等、§0配分規則（期限あり最低60%）を適用して次を選ぶ

### 🎉 2026-09-05 01:2x時点の状態（T-Y12段階1・段階2とも完了・47県の入試日程ページが公開済み）

T-Y12（入試日程DB）が段階1（47都道府県のデータファイル）・段階2（`/[prefecture]/nyuushi-nittei`
公開ページ）とも完了した。RUBRIC_MONEY_PAGE.md準拠（★項目実物確認・dev server+Playwrightで
モバイル390px実機スクショ確認）・sitemap登録・内部リンク・リッチリザルト監査すべてgreen
（詳細は`ops/tasks/T-Y12-nyuushi-nittei-db.md`本文末尾）。⚠️このセッション中に`taskkill /IM
node.exe`でGmail/GA4/GSC/Trends MCPを誤って切断する事故が発生した（`[[feedback-no-broad-taskkill]]`
に対策記録済み・次回セッションでMCP接続が生きているか要確認）。

**次に再開するセッションがまず行うこと**:
1. Gmail/GA4/GSC/Trends MCPサーバーの接続状態を確認する（前回セッションの事故で切断された
   可能性がある。生きていなければ再接続を試みるか、影響が無ければそのまま進めてよい）
2. Gmail新着返信の確認（`in:inbox newer_than:Nd -category:promotions`・特にT-S13A A-1関連の
   追加okがないか）
3. T-Y12は段階1・2とも完了・残るのは任意の段階3（ICS配信統合）のみ。次の主食を探す場合は
   T-N1-N4・S13-A・T-A1等（下記参照）でチェックボックスが残っていないか再走査する。
   **§0の配分規則（期限ありA群を最低60%）を必ず適用**すること

### 2026-09-04 04:0x時点の状態（T-Y12新設・ibarakiパイロット完了・次は残46県・現在は47/47で完了済み）

T-Y11Bの「この作業が終わったら（優先順）」§1（入試日程DB）に着手した。`ops/tasks/T-Y12-nyuushi-nittei-db.md`。
着手前ゲート（ibaraki/chiba/osakaで一次ソース実在確認）通過・ibarakiパイロット（令和8年度・12イベント）
実装完了（`src/lib/exam-schedule.ts`型定義＋`src/data/exam-schedules/ibaraki.ts`、二次情報源突合済み、
tsc実exit0・jestフル408suites6636tests green・commit f55fbce）。
**次に再開するセッションは、`ops/tasks/T-Y12-nyuushi-nittei-db.md`の段階1（残46県）から
2県目に着手すること**（WebSearchで「<県名> 令和8年度 公立高等学校入学者選抜 実施要項 日程」→
一次ソースをWebFetch→学力検査日・合格発表日は二次情報源と突合→`<pref>.ts`+`<pref>.test.ts`を
1県ずつ追加、の刻み方。T-Y11Bと同型）。一次ソースが見つからない県は`unresolved`として
タスクファイルに記録しスキップしてよい（Y-0捏造ゼロ優先）。

### 2026-09-04 03:0x時点の状態（T-SS1完了・T-Y11B完了・T-S13A A-1が9/10県まで進捗）

本日中に以下すべて完了・push済み:
- **T-SS1**（データライセンスを「買えるもの」に）: SS1-1実測（D1本番を`scripts/d1q.mjs`で直接調査・
  Business専用API`/api/schools/{pref}`への外部見込み客アクセスは実質ゼロと確認）・SS1-2
  （Businessティアのセルフ決済配線・checkout route/stripe.ts/UpgradeButtonの3層を配線）・SS1-3
  （`/developers`の件数を実測値に書き換え・回帰テスト新設）・SS1-4（OpenAPI仕様書のBusiness記載
  漏れを発見修正・GSC実測）。loop担当分は完了。残るのはStripeダッシュボードでのBusiness商品登録
  （👤専用・C7ゲート）のみ。
- **T-Y11B 段階2-a〜2-d**: 全て完了。2-b（usable候補35県のパーサ実装）は既に完了済みだったところに
  2-c（`scripts/bairitsu-ingest/queue-vision-review.mjs`・通らない5県の半自動ハーネスを実弾検証まで
  完了）・2-d（`ops/tasks/T-Y11B-column-position-map.md`・31県の列位置マップ）を本日追加完了。
- **T-S13A A-1（応募状況データ再配布許諾台帳）**: Gmail新着返信の確認からtochigi（栃木県）が
  ok化し8→9県（kill_criteria=10県まで残り1県）。**次にこのファイルへ戻るセッションは、まず
  Gmail `in:inbox newer_than:Nd -category:promotions`でkyoiku-*宛先からの新着返信を確認し、
  `src/lib/data-license-ledger.ts`へ反映することを最優先で行うこと**（1県で商品化の閾値に届く）。

**A群・B群とも、上記以外に自動着手可能な項目は無いことを確認済み**（DEADWIRE.mdの「今すぐ直すべき
順」上位項目はいずれも既に別セッションで修正済みと確認・T-C2は残チェックボックス無し・S1-4は
「自動で着手しない」と明記されているため見送り）。次のシグナルは
09-05(M1-2・学校ページ広告1週間ゲート)/09-08(N1-2)/09-09(T-A1 kill_criteria)/09-21(W-8)の
日付ゲート、またはGmail返信（特にT-S13A A-1の残り1県）・👤アクションのいずれか。
この時点以降に再開するセッションは、まず`memory/loop-question-note.md`末尾を確認し、次に
Gmail新着返信を確認してから、上記日付ゲートの到達有無をチェックすること。

> ⚠️ **2026-08-11 の GAUNTLET（8審問官 × 6候補 = 48判定）により改訂した。原典は `ops/VERDICT.md`。**
> 8審問すべてを通過した候補は **S13-A（データバンクのセルフサーブ商品化）1件のみ**。

### 🆕 2026-08-30 **👤が新主食 T-N1〜N4（¥300万の壁を破る4本）を直接起票**（`ops/tasks/T-N1-N4-revenue-ceiling.md`）

**位置づけ**: A群。T-C4〜C9（旺文社）と T-M1/M2（クリック率・bot）に次ぐ第3の主食。
**2026-08-30夜のセッションで実行順序表の1〜8を完走**（詳細はタスクファイル本体・以下は要約のみ）:

| 項目 | 状態 |
|---|---|
| N1-1 令和8年度スナップショット凍結 | ✅完了（`src/data/snapshots/2026-r8/`）。pdfHash収集のみ別イテレーション行き |
| N2-1 MCP公開レジストリ登録 | ✅loop側整備完了・**実際の`mcp-publisher publish`実行は👤専用（OAuth）** |
| N1-0 正確性設計 | ✅型契約実装済み（`exam-system-diff-types.ts`） |
| N1-4 倍率の年次変化レポート | ✅エンジン+見本1本完成 |
| N2-2 AIハルシネーション検証セット | ✅正解セット188問完成・**実測はAI APIキー提供待ち（👤アクション）** |
| N3-1 地域模試の買い手リスト | ✅47県中45県調査済み（島根・徳島のみ運営会社不明のまま） |
| N4-1 47県横断比較データ | ✅完了（研究者向けCSV済み） |
| N3-2/N4-2 文面 | 🔒返信を見てから・N4-2はT-C9返信待ちでブロック中 |
| N1-2 前年度47県の収集（最重量） | 🔒**9/08〜9/22（👤ユタ不在期間）に実行と👤承認済み**。それまで着手しない |

**次に払底検知したら**: まずこのT-N1-N4タスクファイルの残チェックボックス（N1-1のpdfHash等）を
確認し、それも尽きていれば本ファイル§0の他のA群（S13-A/T-A1等・下記）または
`ops/DEADWIRE.md`/`ops/PROPOSALS.md`へ進むこと。

### 🆕 2026-08-30夜 **T-N5（新規ページの本文薄さ対策）を👤が追加起票→完走**（`ops/tasks/T-N5-thin-content-thickening.md`）

T-N1-N4の1-8完走後、👤が「結構本気で作り込むようにloopに投げて」と直接commitで追加起票。
対象7ページ（`/hyotei-heikin/gakushu-seiseki`本体+chousasho/yokuaru-machigai/gaihyou/
tousei-han-i/kyoka-betsuの5サブページ+`/kakomon-jisaiten`）**全てに現行の文部科学省
実施要項（8文科高第318号・令和9年度）に基づく新規セクションを追加完了**。
**重要な教訓**: 着手時に見つけた3文科高第284号（令和4年度版）は4年前の失効通知で、
そのまま執筆していたら「学習成績概評Ⓐ」等の廃止済み規定を誤って追加するところだった。
一次ソースは必ず現行版であることをページ自身の出典表記等で確認してから使うこと。
**残タスク**: 本番デプロイ後の実際のレンダリング文字数計測（6,000〜10,000字目標との比較）のみ。

### 🆕 2026-08-30 18:10時点の状態（T-M2完了・T-N5第3ラウンド完了・T-G1完了・A群/T-C1とも払底）

本日中に以下すべて完了・push済み: **T-M2**（M2-4モバイル比率警告を日次ブリーフィングへ統合・DoD5項目達成）、
**T-N5第3ラウンド**（kakomon-jisaiten接続・文科省通知未使用4点追加・7ページtitle/description重複なし確認）、
**T-G1**（GSC精査4件=学校ページCTRベースライン保存/mie-78%の原因特定/米国表示の観測記録/秋田岩手仮説棄却）。
`ops/tasks/`全ファイル横断チェック＋`data/outreach-queue.json`のcandidate件数(0件)を確認し、
**A群・T-C1とも実行可能な項目が無いことを確認済み**。次のシグナルは
09-05(M1-2)/09-08(N1-2)/09-09(T-A1 kill_criteria)/09-21(W-8短縮形パイロット判定)の日付ゲート、
またはGmail返信・👤アクションのいずれか。この時点以降に再開するセッションは、まず
`memory/loop-question-note.md`末尾を確認してから、上記日付ゲートの到達有無をチェックすること。

### ⛔ 実行の規則（2026-08-12 改訂・**これを守らないと T-C1 から永久に出られない**）

**事故の実例**: 2026-08-11〜12 に loop は **T-C1 だけを 150コミット以上連続で実行**し、
S13-A・T-A1・T-R1（日次通知）に**1コミットも着手しなかった**。
loop は契約どおりに動いており、**原因は「T-C1 は∞常駐」「主食の本体は T-C1」「他に何も無ければ
T-C1 を進めれば手は空かない」と書いた契約側の設計ミス**。
∞タスクを最優先に置くと、期限のあるタスクが構造的に永久後回しになる。

```
【毎日の配分】1日の作業時間を、この比率で割り当てる
  ① 期限あり（下表の A 群）  … 最低 60%   ← 先に着手する
  ② T-C1（∞常駐）           … 残り       ← A群が全部ブロックされている日だけ100%

【A群＝期限のあるタスク。期限が近い順に上から食う】
| 期限 | ID | 内容 | 状態 |
|---|---|---|---|
| 9/07 | **T-R1 日次通知** | `daily-brief-health.ts` の npm script 登録＋スケジューラ。**envは設定済・疎通確認済（HTTP 204）。足りないのはスケジューラだけ** | ✅完了(`25de942`・毎朝7:30自動送信稼働) |
| 9/07 | **Discord 第3層ブリッジ** | 👤が全許可済。`discord.js@14.27.0` 導入済。設計は質問ノート | ✅完了(`a61559f`) |
| 8/31 | **S13-A** A-0〜A-6 | 8審問を全通過した唯一の候補。19〜28h | 🟡A-0(A-0-1〜A-0-5)完全完了・A-1のloop側作業(email20件+form10件=計30件下書き)も完了し返信待ちに移行(2026-08-12)。**ok県はakita/gifu/ibaraki/ishikawa/mie/okinawaの6県(2026-08-31 14:2x時点・石川県の応募状況データ掲載許諾で追加)・ok10県未達のためA-2以降は着手禁止**（`redistributableOkPrefectures()`のkill_criteria・残り4県）。本行は返信が届くたび更新すること（このメモの更新頻度が実態より遅れやすい＝過去に14日stale化した前科あり） |
| 8/31 | **T-A1** 多年度化 | 42/47県・8〜14h。手順1-4完了(`9da80cc`〜`e0f2bca`)・手順5は追加コード不要と判明 | 🟡ほぼ完了(kill_criteria判定は28日後・2026-09-09頃) |
| 随時 | **T-R1 週次/月次** | 定期確認。**「異常なし」も記録する** | ✅2026-08-12に日次/週次/月次とも初回実施済み(異常なし・W-1のみ評価不能扱い)。継続稼働中 |

【B群＝∞常駐】★2026-08-13 差し替え
  T-C2  送れる弾を作る（本文の無い candidate 280件に文面を用意）  ops/tasks/T-C2-outreach-bodies.md
  （T-C1 の候補収集は目標達成済み＝280件。**新規候補の追加はもう不要。**）
```

### 🔥 2026-08-13 **👤の決定: 今季は B2B に全部を賭ける**

> 「B2B（API・MCP・DBの交渉など）にすべてを捧げる。B2Bをやって損をすることはない。
> 　このサイトの"権威"にもつながる。一石二鳥だ。ただ負担は俺の商談をこなす能力の向上、それのみ」

**これに伴い、以前書いた「👤の商談枠は今季3〜5件・努力で増えない」は本人により撤回された。**
律速は商談枠ではなく、**送れる弾の数と買い手の母数**に戻っている。

**A群に T-C3（商談の型）を追加する。** 弾が届いても、返ってきた時に型が無ければ落とす。

| 期限 | ID | 内容 |
|---|---|---|
| 随時・最優先 | **T-C3** | 商談の型（振り返り・想定問答10問・meeting-copilot のB2B化・稟議1枚）`ops/tasks/T-C3-meeting-playbook.md` |

⚠️ **「数を撃つ」と「雑に撃つ」は違う。** 模試会社59社は業界内に横のつながりがあり、
雑な営業の評判は狭い業界を回る。**速度は上げる。粗さは上げない。**
👤が積んでいる「権威」は、そこで毀損しうる唯一の資産である。

### 💸 2026-08-14 **push の方針を変える（Cloudflare 請求 $104.76 の86%が Build Minutes だった）**

**実測（Invoice IN-74884235・👤がダッシュボードで確認）**:

| 項目 | 数量 | 金額 |
|---|---|---|
| Workers CPU ms（＝実ユーザーのアクセス） | 17,620,296 | **$0.36** |
| **Worker Build Minutes**（月6,000分込み） | **17,974分** | **$89.87** |
| Workers Paid | 1 | $5.00 |

**サイトの運用コストはほぼゼロ（$0.36）。費用は全部「push 1回 = Workers Build 1回」から出ている。**

- 過去30日のコミット **2,667件**（89件/日）／1ビルド約 **6.7分**
- 無料枠 6,000分 ＝ 約895ビルド ＝ **30ビルド/日**。**現状は3倍**
- **コミットの46.4%は `src/` `public/` 設定ファイルに触れていない**（＝デプロイ不要）

**⚠️ 原因は `memory/fable5-loop-protocol` の「2026-07-11 Workers Paid化によりpushバッチ制限を廃止・
green毎回即push」という判断。ビルド課金を計算に入れていなかった。**

#### 新しい push 方針

| 変更内容 | push |
|---|---|
| `src/` `public/` `package.json` `wrangler.jsonc` `next.config` `open-next.config` を含む | **従来どおり即 push**（デプロイが要る） |
| **`docs/` `ops/` `data/` のみ** | **溜めて 1日3〜4回にまとめて push** |

**commit は従来どおり毎回してよい。変えるのは push のタイミングだけ。**
worklog には「今回は docs のみのため push を保留（未pushN件）」と書く。
**ただし、1日の終わりと `!w stop` 前には必ず push すること**（未push のまま停止しない）。

⚠️ **👤側の対策（Build watch paths の設定）と併用して初めて無料枠に収まる。** `ops/INFRA-COST.md` を参照。
⚠️ **T-R1 月次に「Cloudflare の請求額と Build Minutes を確認する」を追加すること**（M-5）。
   今回は誰も見ていなかったため3か月目で初めて表面化した。

### 📌 2026-08-13 **👤の負担はこの4つだけ。これを超える作業を👤に振らない**

1. **フォーム送信**（Coworkで直前まで自動化し、**押すだけ**の状態にして渡す）
2. **商談**
3. **数日に一度の VS Code 上での対話**
4. **Discord での監視**

**これ以外は loop と対話セッションが持つ。** 調査・文面・資料・見積・実装・検証は全部こちら側。
「👤に確認してください」で投げる前に、**自分で確認できないかを必ず先に試すこと**
（実例: 2026-08-12に「Gmail下書き23通を送ってください」と依頼したが、
`in:sent` を引けば20通が既送信先だと分かった。確認せずに人を動かした）。

### 📌 なぜ収益予測を書いてはいけないのか（理由を記録する）

👤の証言:

> 「5,6月時点ではアフィリ・AdSenseだけで今期200万出るという甘い世界を見せられ続けた。
> 　収益モデルを立てろと指示するたびに、0円をごまかすために『2週間後あたりから収益が発生する』
> 　というモデルに騙され続けた。だからメモリに収益モデルの数値を残すなと命令した」

**これは歴代セッションがやったこと。** ¥0という実測を、未来の数字で塗り潰した。
→ **金額を書くのは、👤が明示的に依頼した成果物の中だけ**（`ops/PRICING_OPTIONS.md` 等）。
→ **書く時は必ず「これは提案であって売れる保証ではない」と根拠の計算過程を添える。**
→ **実測ゼロの数字を、達成見込みとして書かない。**

### ⛔ 2026-08-13 改訂: B群を T-C1 から T-C2 に差し替えた理由（実測）

`data/outreach-queue.json` を数えたところ **candidate 280件のうち本文があるのは0件**だった
（全339件で「本文あり54／本文なし285」）。**「候補273件」は「宛名リストが273件」の意味でしかなく、
👤が実際に送れる弾は1ヶ月間54件のまま動いていない。**

**loop が候補を274件目・275件目と増やしても、送信できる数は1件も増えない。**
今季の律速は候補の数ではなく、**送信できる状態になっている弾の数**である。

→ **B群で食うのは T-C2（本文を作る）。T-C1 への新規候補追加は停止。**
→ **進捗として報告する数字を「candidate件数」から「本文あり件数」に変える。**
   274件目を報告しても、それは前進ではない。

### ⛔⛔ 2026-08-13 **監視モードは禁止する（👤指示）**

> 「あと絶対に監視モードに入らせないようにして。**もうすることがないじゃなくて、
> 　もっと打診先を考えるようにしなきゃ論外**」

**監視モード（1800s待機）への移行を禁止する。** 「やることが無い」という状態は存在しない。
**手が空いたら、次の打診先を探す。** 日本の教育関連事業者は数万社あり、**払底しない。**

#### 手が空いたときに降りていく梯子（上から順に。**必ずどれかに当たる**）

| # | やること | 残量（2026-08-13 11:4x時点の実測・訂正版） |
|---|---|---|
| 1 | **C2-0**: T-C1で数え済みの買い手を queue に運ぶ | **✅moshi59社リストは調査完了(創育/全中模試事務局のみ実体不明で保留)。教育出版/EdTech/塾本部も完了または目標超過**（下記「2026-08-13訂正」参照）。**このレーンは事実上払底＝梯子#2以降に進む** |
| 2 | **未計測レーンを新規発掘する** | ⚠️2026-08-16実測: 家庭教師会社(5件検証済・全excluded)/通信教育(4件検証済・全excluded)/塾FC本部(目標超過済み)は事実上閉鎖。詳細は`ops/tasks/T-C1-b2b-target-list.md`末尾の`#lane-tsushin`追記を参照。**このレーンは以後スキップし#3へ進む（#5も下記の通り既に調査済みなので飛ばさない）** |
| 3 | **既存の本文の質を上げる** | 本文あり98件。**返信率を上げる方が社数を増やすより効く**（250社×15% > 400社×7%） |
| 4 | **返信が来た相手への追撃** | `computeFollowupCandidates()` で43件が候補（最古29日） |
| 5 | **買い手母数そのものを広げる調査** | ⚠️2026-08-15に3項目とも軽量調査済み・**新規発掘レーンではなくなった**（詳細は`ops/tasks/T-C1-b2b-target-list.md` 430-438行目）: 私立高校1,320校=個別校試掘1件のみでcold outreach自体は未確立、母集団設計にQ1 2027仕込みが必要で今すぐの主食ではない／塾教室単位5万教室=1件ずつのcold outreach方針自体を不採用と結論済み／大学入試版=自社・my-shingakuとも売る製品が存在せず構造的に対象外。**この3項目に戻って再調査しない** |
| 6 | **`ops/DEADWIRE.md`/`ops/PROPOSALS.md`（2026-08-10監査）の未対応項目** | 🆕2026-08-23追加。B2B転換の翌日(08-11)から13日間誰も追跡していなかった2つの監査文書を再点検し、`DEADWIRE.md`のDW-3/4/5,6/7/8と`PROPOSALS.md`のC8是正3案(S10-3/S10-4)+EV案2件(S4-2/S3-2)を実装・commit済み（詳細はdocs/worklog/2026-08-23.md 12:43〜14:28）。**DEADWIRE.mdは全8項目を出し尽くした**（残るDW-3の#1/#3は影響範囲・誤検知リスクを理由に意図的見送り）。**2026-08-24に大量消化**: S1-1+S11-5・S4-3に続き S11-1（ResultSection低EVバナー13箇所置換）・S9-2（placement表記ゆれ二重計上・DW-5書込み側は既済で読取り側を追加）・S2-3（ParentWindowBridge /mendanリンク計測）・S5-2（学校ページ露出の週次定点観測+GSCカレンダー）・S1-2（moshimo-manecafeを第3アームに追加）・S4-1≡S2-4（/hogosha到達距離3→2・4面）を対応完了、S12-1（HyoteiUniversityBridgeのD1一次計測）はコード完了・**本番D1へのmigration適用のみ👤待ち**（`loop-question-note`に手順記載済み・commit 3736961）。また調査の過程でS1-3（affiliateリンク生存確認スクリプト）が既に2026-08-17完了済みだったのに本書未更新だったと判明し記載を訂正した。**円が出る7案(§2-A)・測定基盤の主要項目・S1系はほぼ出し尽くした**。PROPOSALS.mdの残りは👤裁量枠(S9-1 Cowork発注)・👤判断待ち(S10-2/S10-6)・9月以降ラベル済み(S9-3/S9-4/S9-5等)・NEXT_SEASON扱いの案が中心。**払底検知のたびにこの2文書へ戻って未対応行を探すこと**（各行に✅注記があるかで対応済みか判別できる。ただし✅注記が無くても実は別の場所で既済のことがある＝S1-3の教訓。疑わしい項目は実ファイルの存在確認を先にすること） |

⚠️ **「新規候補の追加はもう不要」（T-C1停止）と、この梯子の#2は矛盾しない。**
T-C1 で止めたのは **npo/media レーンへの追加**。**買い手レーンの発掘は止めていない。**

#### 🔴 2026-08-13訂正: 上記表の「26/170」という実測は診断コマンドのバグだった

`node -e`診断が `lane==='moshi'|'edtech'|'juku'` という**リテラル値**で数えていたが、実際の
データは大半が `lane:'b2b-saas'` ＋ `id`プレフィックス（`moshi-*`/`edtech-*`/`juku-*`/`gakusan-*`）で
管理されていた。**さらに26件は`OutreachLane`型に存在しない無効なlane値（'moshi'等）でタグ付けされており、
`queueReviewTierOf()`がundefinedを返しレビュー画面から不可視になっていた実害あるバグでもあった**（`4799937`で修正）。

**id プレフィックスで数え直した正しい残量**（2026-08-13 14:0x・再修正）:

| プレフィックス | candidate | excluded | 対象社数 | 真の残り |
|---|---|---|---|---|
| `moshi-`（模試） | 24 | 19 | 59社 | **59社リストは調査完了**（創育[到達不能5回]・全中模試事務局[実体不明]は保留。フクト新規追加・sinro-hyogo等目的不一致3件をexcludedへ訂正） |
| `gakusan-`/`lane2-`/`textbook-`（教育出版・学参） | 41 | 9 | 50社 | **✅完了（41+9=50でtargetと完全一致・オーム社を目的不一致でexcludedへ訂正）** |
| `edtech-`（EdTech） | 52 | 0 | 38社目安 | **目標超過**（`71`という前回記載はsourceDoc `#lane4`タグ集計との混同だった可能性が高い・`edtech-`プレフィックスの実数は52。通信制高校サポート校から角川ドワンゴ学園/ブロードメディアを新規追加） |
| `juku-`/`lane3-`（塾チェーン本部） | 20 | 11 | 23社 | **✅ほぼ完了（31/23で目標超過）** |

⚠️ **プレフィックスによる集計値はセッションを跨いで変動しうる**（新規発見・見送り訂正・目的不一致の再分類等）。
数字そのものより「このカテゴリはまだ動きがあるか」を見ること。上表は毎回の周回で機械的に再集計している。

⚠️ **2026-08-13 12:3x再訂正**: 上表の直前バージョンは`gakusan-`/`juku-`単一プレフィックスのみで数えており、
**教育出版・学参レーンは初期に`lane2-`/`textbook-`プレフィックスで登録された19件を見落として過小報告していた**
（gakusan-のみでは30+11=41だが、sourceDocの`#lane2`タグで正しく集計すると42+8=50=target丁度で完全一致・
実は既に完了していたタスクを「9社残」と誤認していた）。**教訓: id接頭辞のリネーム規約は途中で変わることがあるため、
プレフィックスだけでなく`sourceDoc`の`#lane番号`タグでも定期的にクロスチェックすること。**

**正しい診断コマンド（プレフィックス版・以後はこちらを使う）**:
```
node -e "const d=require('./data/outreach-queue.json');
const groups={moshi:['moshi-'],gakusan:['gakusan-','lane2-','textbook-'],edtech:['edtech-'],juku:['juku-','lane3-']};
for(const [name,prefixes] of Object.entries(groups)){
  let c=0,ex=0;for(const e of d.entries){if(prefixes.some(p=>e.id.startsWith(p))){if(e.status==='candidate')c++;else if(e.status==='excluded')ex++;}}
  console.log(name,'candidate:',c,'excluded:',ex);
}
let b=0;for(const e of d.entries){if(e.body&&e.body.length>50)b++;}
console.log('本文あり:',b,'/',d.entries.length);"
```
`lane`フィールド単体でグループ化する旧コマンドは**id命名規則を考慮しないため過小報告する**。使わないこと。
単一プレフィックスのみのチェックも同様に過小報告しうる（教育出版・学参レーンで実証済み）。迷ったら
`sourceDoc`の`#lane番号`タグで集計し直すのが最も確実（本ファイルの直上の訂正を参照）。

#### 定期確認だけは続ける（ただし「モード」にはしない）

D1件数・Gmail新着・tsc/jest は**作業の合間に挟む**。それ単体を1周回の成果にしない。
**「監視モード定期確認（変化なし）」だけのコミットを作らない。**

### ⛔ 「ブロック中」と書く前の必須手順（3回目の事故を受けて）

**「A群B群とも真にブロック中」と書く前に、必ず数えてから書くこと。印象で判断しない。**

1. 下のコマンドを実行し、**出力を worklog にそのまま貼る**

```
node -e "const d=require('./data/outreach-queue.json');const e=d.entries;
const l={};for(const x of e){const k=x.lane+'/'+x.status;l[k]=(l[k]||0)+1;}
console.log(l);let b=0;for(const x of e){if(x.body&&x.body.length>50)b++;}
console.log('本文あり:',b,'/',e.length);"
```

2. **各タスクファイルの DoD を1行ずつ引用し、達成/未達を明記する**
3. **「👤の手を必要としない未達」が1つでもあれば、監視モードに入ってはならない**

**実例（2026-08-13 08:25 の事故）**: 「A群B群とも真にブロック中」と判断して監視モード(1800s)へ
移行したが、**C2-0（買い手144社の搬入）は 26/170 で止まっており、これは👤の送信を1件も必要としない**。
模試会社は T-C1 本文に59社の社名の表があるのに8社しか運ばれておらず、教育出版50社はレーンすら
無かった。**ブロックされていたのは「送信」だけで、「搬入」はブロックされていなかった。**

⚠️ **これは3回目**（`feedback-avoid-premature-monitoring-mode` 2026-07-23 / 08-13 03:34 / 08-13 08:25）。
**送信待ちのタスクと、送信を必要としないタスクを、必ず分けて評価すること。**

**⚠️ A群が1つでも未着手のまま、T-C1 だけを1日中実行してはならない。**
**⚠️ 「T-C1 で手が空かないから」は A群を後回しにする理由にならない**（それが今回の事故の原因）。
**⚠️ A群が全部「👤待ち」で完全にブロックされた場合のみ、T-C1 に100%を充てる。**
　　その場合は worklog に「A群は全てブロック中」と理由を明記すること。

**今季の収益は「権威」と「B2B/API」の2変数でほぼ決まる**（他の経路は実測で閉まっていることが確認済み）。
T-C1 はその両方に同時に効くが、**∞であるがゆえに他を食い潰す**。上の配分規則で必ず抑えること。

**降格・保留（`ops/GRAVEYARD.md` G-21 / `ops/VERDICT.md` §4）**
- **T-B1**（学校ページ層の換金受け皿）＝「**冬の床を取りに行く施策**」としては降格。
  床の0.25%しか埋めず（G2）、CTAを足しても競合との情報量差は動かない（G5）。
  **救済策は換金コードの追加ではなく T-A1（多年度化）。** 復活条件は G-21。
  ※配線作業そのものの安全性は否定されていない（6審問が条件付きPASS・C7項目なし）。
- **T-D1**（私立47県の出口）＝**決定済み（2026-08-12・この行の記述は古い）**。出口C（データ商品に合流）を採用。
  育伸社言及23ファイルを除外しても`private-school-detail`23県+`schools-private`47県+`private-school-tuition`8県が
  クリーンで残存（`ops/tasks/T-D1-kake2-exit.md`参照）。follow-up（私立23県分をライセンス台帳へ追加するか検討）は
  **S13-A A-1〜A-2完了後**に着手する起票のみ済み。A-1が9県のok待ちで足止め中のため現状はブロック中で正しい
  （「保留」ではなく「決定済みだがA-1待ち」が正確な状態）。

**✅ E-0 は解消済み（2026-08-13・loopが実装状況を実測して確認）。** 上の段落は選択肢A（全削除）が
実装される前の記述で、現状とは一致しない。`<HighSchoolBorderlineTable`の実レンダーはコードベース全体で
0箇所、`buildTargetSchools()`は削除済み、`/api/naishin`等からの`targetSchools`配信も停止済みで、
`dataset-contract.test.ts`が回帰防止テストとして固定している。詳細は`ops/tasks/T-E0-borderline-y0-violation.md`。

---

## 1. ⛔ 停止禁止条項

**監視モードに入ってよいのは、バックログの主食が空になった時だけ。**
以下はすべて停止の理由にならない:

- 日付待ち（「11/15の窓が開くまで待つ」等）
- ゲート待ち（👤の承認待ちの項目がある）
- 👤の返信待ち
- 収穫逓減（「これ以上やっても効果が小さい」）
- 判断の迷い（「どちらが正しいか分からない」→ **質問ノートに書いて別のタスクへ進む**）
- 「区切りが良いので」

⚠️ 2026-08-02〜09 に、上記6パターン**すべて**を理由に実際に停止した前科がある。

**ただし「止まらない」と「同じタスクを回し続ける」は別物である。**
2026-08-11〜12 に、loop は T-C1 だけを150コミット以上連続で実行し、期限のある
S13-A・T-A1・T-R1 に1コミットも着手しなかった。**止まってはいないが、進んでもいなかった。**
→ **§0 の配分規則（期限ありに最低60%）を必ず適用すること。**
→ T-C1 は「手が空かないための保険」であって、**「他を後回しにしてよい理由」ではない。**

---

## 1.5 ⛔ リンクの向き（2026-08-28 👤裁定）

**「相手にリンクを貼ってもらう依頼」は閉鎖のまま。「こちらが相手へリンクを張る許諾を得る」は継続してよい。**

loopが2026-08-28にT-C9で「閉鎖済みのリンク掲載依頼と文面が重複するのでは」と保留した。
慎重な判断として正しいが、**方向が逆なので別物**である。

| | 内容 | 可否 |
|---|---|---|
| 被リンク乞い | 「**当サイトへリンクを貼ってください**」と相手に依頼する | ❌ **閉鎖のまま** |
| 許諾の取得 | 「貴課の公表資料を出典明記のうえ**当サイトに掲載してよいか**」「**当方から**貴課サイトへリンクを張ってよいか」 | ✅ **継続してよい** |

根拠となる実例（沖縄県教育委員会・2026-08-24受領）:
> 「公表データの転載および**当課ウェブサイトへのリンク設定**につきましては、差し支えございません」

＝ **My Naishin が教委へリンクを張る**ことの許諾であって、逆ではない。

**超えたら止まって👤に聞く境界:**
- ❌ 「貴課のサイトに当サイトを掲載していただけないか」
- ❌ 「リンク集に載せてほしい」
- ❌ 相互リンクの提案

---

## 2. ⛔ 人間ゲート（C7・自動実行は絶対禁止）

| ゲート | loopがやってよい範囲 |
|---|---|
| **対外メールの送信** | 下書き設置まで（Gmail MCPは`compose`のみ・`send`スコープは意図的に持っていない） |
| **フォーム送信** | 送信直前まで（Coworkで自動化してよい）。**送信ボタンは人間** |
| **既存フラグの点火（off→on）／env変更** | 一切不可。毎回👤に戻す |
| **本番D1の破壊的操作**（DROP・列削除・データ書換） | 不可。**非破壊migration（テーブル/カラム追加）は単独可・適用前バックアップ必須** |
| **Stripe点火・価格の数字** | 不可 |
| **本番反映** | ステージングはloop全権。本番は👤 |
| **PIIの記録** | 不可（個人名・個人のメールアドレス。法人の代表窓口アドレスは可） |

### ⛔ 追撃メールに `gmail_create_reply_draft` を使ってはいけない（2回発生した再発事故）

**`gmail_create_reply_draft` は「そのメッセージの差出人」に返信する。**
追撃したい相手のスレッドで一番新しいメッセージは、たいてい **自分が送った送信済みメール**である。
そこに `create_reply_draft` を使うと、差出人＝自分 → **宛先＝自分の下書き**ができる。
本文は完璧なのに宛先だけが壊れるため、`in:draft` を眺めても気づきにくい。

| 状況 | 使うツール |
|---|---|
| **相手から届いたメールに返信する** | `gmail_create_reply_draft`（正しい用途） |
| **自分が送ったメールを追撃する** | **`gmail_create_draft`** で `to` を明示する |

**判定手順（下書きを作る前に必ず）**: 返信対象メッセージの `from` を見る。
**`from` が `naishin.dev@gmail.com` なら `create_reply_draft` を使ってはいけない。**
宛先は同じスレッドの**一番古い送信済みメールの `To`** から取る（＝最初にその相手へ送ったときの宛先）。

**発生記録**: 2026-08-14に7件・2026-08-23に13件。どちらも👤が目視で発見。
**復旧**: `node --use-system-ca scripts/gmail-fix-self-drafts.mjs`（確認）→ `--apply`（修正）。
下書きは消さず `drafts.update` でToヘッダだけ差し替えるので、本文・スレッドは保たれる。
**T-R1の日次チェックに「自分宛て下書きが0件か」を入れた。**

---

## 3. ★★ 2026-08-10〜11 に実際に起きた事故から得た教訓（最重要）

### 3-1. 「commit」「push（デプロイ）」「D1 migration適用」は**3つ別の事象**
1つ確認しただけで「本番反映済み」と書いてはならない。

**実際に起きたこと**: `trusted` 列を参照するコードがpushされたが、本番D1に migration 0019 が未適用だった。
この状態でデプロイが走ると、集計は空にフォールバックする（安全）一方で、
**`insertStatsSubmission` が毎回失敗し新規投稿が黙って捨てられる**（サイレントな計測停止）。

→ **反映確認は必ず「スキーマ」と「デプロイ済みコードの版が判別できる応答」の両方で行う。**
→ APIのレスポンス文字列が2つの版で同一になりうる場合、それは反映の証拠にならない。

### 3-2. 判定は必ず**内部referer付き**で数える
D1 `clicks` の **88%はbot**。全期間629件のうち内部パス付きrefererは**41件**しかない。

```sql
-- 人間のクリックを数える唯一の正しい方法
WHERE referer LIKE 'https://my-naishin.com/_%'
```
⚠️ `LIKE 'https://my-naishin.com%'`（アンダースコア無し）は **root_only（パス無し）まで拾う**ので使わない。
⚠️ bot込みの数で面別の勝ち負けを判定すると、**全部の結論が狂う**（実例: `fp-soudan` は記録上131クリックで
全期間2位だが、内部referer付きは**0件**）。

### 3-3. referer のパスと placement が矛盾する行は bot（内部整合性の検査）
root_only 33件のうち **21件（64%）が「そのページに存在しない placement」を名乗っていた**
（トップページから来たと主張しつつ `parent-lp` を名乗る等）。
ヒューリスティックではなく論理的な矛盾なので、**確実にbotと言える**。

→ `<AffiliateAd>` に **placement を必ず渡す**こと（現状96箇所中89箇所が未付与）。
   付けるほどこの判別軸が効く範囲が広がる。**順序に意味がある**——placement是正が先。

### 3-3b. ★ GSCの `query` / `page` 次元の合計を、サイト総計として使ってはならない

**GSCは匿名化のため、しきい値未満のクエリを次元別レスポンスから落とす。小規模サイトほど落ちる割合が大きい。**

**実際に起きたこと（2026-08-11）**: my-shingaku の28日クリックを
`query` 次元の合計から **5件**と報告したが、`gsc_totals`（次元なし）の正しい値は **52件**。
**10倍の過小報告**。しかもその誤った数字を根拠に「PLAYBOOK横展開は前提未成立」として
`ops/GRAVEYARD.md` G-5 に不採用として葬っていた（同日に判定を撤回）。

```
✅ サイト全体の規模を知りたい → mcp__gsc__gsc_totals（次元なし）
✅ 内訳・構成比を知りたい     → gsc_query の次元別（合計は総計と一致しないことを前提にする）
❌ 次元別レスポンスの合計を「サイト総計」として引用する
```
⚠️ my-naishin でも同じ乖離がある: **query次元はサイト総クリックの57%・表示の43%しか映していない**
（`ops/raw/BRIEF.md` §I）。**次元別の数字を出すときは必ず「見えている範囲の話」と明記すること。**

### 3-3c. 新規ページ・新規ドメインの順位を、現在値で評価しない

**自社実測（my-shingaku・被リンクほぼゼロの新規ドメイン）**:
```
7/13 表示   7  順位 61.3
7/22 表示 146  順位 23.6
7/31 表示 215  順位 14.5
8/08 表示 301  順位 13.9      ← 4週間で 61.3 → 13.9
```
→ **順位は固定値ではなく、露出とともに改善する。**
→ 学校ページ（公開2週間・加重9.76位）を「9.76位だからCTR1.06%」と定常状態のように語ってはいけない。
   **my-shingaku は4週間かけて14位。学校ページは既にそれより速い。**
→ **2週間のスナップショットで施策の成否を判定しない。** kill_criteria の窓は最低28日。

### 3-4. 数値を持つ型を作ったら、不変条件テストを**同時に**書く
- 2026-08-08: 満点135点の宮崎県で `avgNaishin: 425`（満点の3倍）。**3,826件のテストが全green**のまま素通り
- 2026-08-10: 偏差値の全国平均を **63.16** として本番配信（偏差値は定義上、母集団平均が50）
- 2026-08-09: 複数年度のレコードが合算され `totalQuota` が無意味な値に

**共通の原因**: ロジックのテストはあっても「その値が取りうる範囲」のテストが無かった。
例: `avgNaishin <= maxScore` / `合計 = 内訳の和` / `年額×3 = 総額` / `倍率 = 出願者数 ÷ 定員` /
`偏差値の標本平均は50±5`。

### 3-5. 公開されている記述が事実と違わないか、定期的に疑う
2026-08-10に2件見つかった: 統計API（偏差値平均63.16）と**プライバシーポリシー**（「現在、広告は掲載しておりません」と
書きながら live 34本が稼働）。**このサイトの拠り所は「公表値のみ・捏造ゼロ」なので、ここが崩れるのが最悪の事故。**

→ 機能を止めたり方針を変えたりしたら、**それを説明している公開ページも同時に直す**。

### 3-6. 「都道府県教委の入試情報ページへのリンク掲載依頼」は打ち切る（★2026-08-11 に範囲を訂正）

**実測**: 三重・石川・岐阜・和歌山の4県が「引用はOK、リンクは内規で不可」と回答（2026-08・Gmail。和歌山は2026-08-12 16:49着信、原文「公的団体のページ以外へのリンクは原則として行っておらず...個人が運営されているホームページ等へのリンクを挿入することは、公平性や中立性の観点から困難」）。
4件とも理由が組織方針（「課内で協議して不可」「公平性・中立性の観点」等）なので、**この型は4/4で閉まっていると判断してよい。**

⚠️ **ただし「.jp/.go.jp からの被リンクは無理」と一般化してはならない。** 当初この憲法にそう書いたのは
**過度な一般化だった**（👤が2026-08-11に指摘）。実測は「**都道府県教委の入試情報ページ**に
**リンクを貼ってもらう**」という**単一の型**に対する3件にすぎない。

**未検証のまま残っている主体（＝まだ閉まっていない）**:
- **大学・研究室（`.ac.jp`）** — 教育学研究で内申点・倍率データを使う需要は実在しうる。DRも高い。
  ⚠️2026-08-13試掘: 適合度の高い候補（東大・都道府県別入試選抜制度比較研究）を発見したが、
  窓口が教員個人メールのみでC7（個人メール記録禁止）に抵触しqueueに追加不可と判明。
  研究科代表窓口（総務等）経由の間接打診に設計変更するまで「⏸要設計」（詳細は`ops/tasks/T-C1-b2b-target-list.md`末尾）
- **市区町村の教育委員会** — 都道府県教委とは**別の判断主体**。同じ内規とは限らない。
  ⚠️2026-08-14: 名古屋市教育委員会から初の回答（1/1）＝リンク掲載を拒否（理由説明なし・簡潔な定型文）。
  都道府県教委と同じ結果だが**サンプル数1では「閉まった」と判定しない**（横浜市教委等が引き続きawaiting）。
  複数の政令市で同じ結果が揃うまでは未検証のまま扱う。
- 国立教育政策研究所・教職員支援機構 等の独立行政法人
- **公共図書館のリンク集**（伝統的にリンク集を持つ文化がある）
- 高校（`.ed.jp`）の進路指導ページ

**未検証のまま残っている「依頼の型」**:
- リンク掲載依頼以外に、**資料提供→引用される / データ提供→出典として記載される / 研究協力→論文で被引用**がある
- 三重県は**引用そのものは許諾している**（「出典を明記のうえ紹介いただくことは差し支えございません」）。
  **リンクは無理でも言及はありうる。**

**判断基準**: 被リンクは「1本の価値が桁違い × 試行コストが低い（loopが生成・👤が送信）」ため、
**成功率が低くても期待値は正**になりうる。「3件断られたから全部やめる」は誤り。
**閉まったと言ってよいのは、同じ主体・同じ型に対して複数回試して全滅した場合だけ。**

---

## 4. 公開の作法（C8 fail-closed）

- **学校ページ等の新規公開は 1波5県・2〜3日おき**（👤合意済の必須条件）。**波を急がない**
- **既存ページの改善は「新規公開」ではない**ので波の制約を受けない（ただし一度に47県まとめてpushしない）
- **RUBRIC で 20/25 未満なら公開せず差し戻す**（`ops/RUBRIC_MONEY_PAGE.md`）
- ドメインは実質6か月（GSC起点2026-02下旬）。**1年未満で手動対策を受けたら今季は消える。**
  scaled content abuse の疑いが1つでもあれば、EVがいくら高くても不採用（**C8 > C11**）

## 5. ピークフリーズ

**2026-11-15 〜 2027-02-28** は構造・URL・大規模リライトを凍結。
この期間に触ってよいのは **導線・CTA・比較表・価格情報＝CVR系のみ**。
→ **フリーズ前に終わらせる必要があるものを逆算して優先する。**

---

## 6. 異常時の停止条件（これらは止まってよい・むしろ止まるべき）

| 条件 | 対応 |
|---|---|
| ビルド失敗 / `tsc` exit != 0 / `jest` 失敗 | **push しない。** 直すまで次へ進まない |
| GSCに手動対策・セキュリティの問題の通知 | **即停止して質問ノートの冒頭に書く。**👤の判断を待つ |
| 順位の急落（主要面の平均順位が7日で2位以上悪化） | 直前の変更をロールバック候補として質問ノートへ |
| 本番D1のデータ件数が減っている | **即停止。** 書き込み経路の事故を疑う |
| 公開ページの記述が事実と違うことに気づいた | **即停止して直す**（3-5参照。優先度は他の全タスクより上） |

---

## 7. 環境の罠（毎回ここで詰まる）

| 項目 | 正 |
|---|---|
| リポジトリの実パス | `c:\Users\E24054\my-naisin`（**h が無い**） |
| shell | **PowerShell**。`&&` 不可・heredoc不可・`;` で連結。`2>&1` を native exe に使わない |
| `tsc` / `jest` | **パイプ厳禁**（`tsc \| tail` は tail の終了コードを返す）。ファイルに落として**実exitを見る**。`NODE_OPTIONS=--max-old-space-size=6144` |
| python | `$env:PYTHONIOENCODING='utf-8'`（cp932 で落ちる） |
| 本番D1（読み取り） | `node scripts/d1q.mjs "SELECT ..."` |
| GSC取得 | `node --use-system-ca scripts/gsc-pull.mjs --siteUrl "sc-domain:my-naishin.com" --start ... --end ... --dimensions query --limit 5000 --out ops/raw/xxx.json` |
| curl | `-k --max-time 45` ＋ **iPhone UA必須**（計測APIは curl 既定UAをボット判定で204にする） |
| scratchファイル | **repoルートに `scratch-*.ts` を残さない**（`tsc` が壊れる） |
| 巨大JSON | Read せず python で集計する |
| ワークフロー同時実行 | **このマシンは4コア＝上限2**。11体並べると2.5時間かかる |

## 8. 並行実行の禁止

**同じリポジトリで2つのセッションを同時に走らせない。**
2026-08-10に実際に事故が起きた: 並行セッションの未コミット変更を別セッションが差し戻し、
「未pushのつもり」のコミットが相手のpushに巻き込まれて本番へ出た。

---

## 9. 数値の正準ソース

| 知りたいこと | 見るファイル |
|---|---|
| 係数（人間クリック数・遷移率・CTR曲線・表示の分母） | **`ops/COEFFICIENTS.md`**（ここからだけ引用する） |
| 実測値全般 | `ops/raw/BRIEF.md` |
| PHASE 0 の数値誤りの訂正 | `ops/CORRECTIONS.md`（**MONEY/DISTANCE/BAR/STATEの数値をそのまま信用しない**） |
| 時計 | `ops/CLOCK.md` |
| 脅威と早期警戒指標 | `ops/THREATS.md` |
| 不採用にした案と死因 | `ops/GRAVEYARD.md`（**復活条件を満たさない限り再提案しない**） |

⚠️ **転換の真値は D1、規模は GSC、GA4 は比率のみ。GA4のゼロをアラート条件に使わない**
（過去に6日間オオカミ少年になった）。
