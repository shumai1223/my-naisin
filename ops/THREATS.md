# THREATS.md — プレモーテム12通り（PHASE 0 完全性critic §5-B の空白を埋める）

- 作成: 2026-08-10 / 担当: PHASE 1（THREATS）
- 元になった欠陥: `ops/PHASE0_FINDINGS.md` §5-B「PHASE 0 の5成果物はどれも『失う可能性』を1行も扱っていない」
- **本書のルール**:
  1. 係数（人間クリック数・遷移率・CTR曲線・EV/click・表示分母・学校ページ枚数・窓の定義）は**すべて `ops/COEFFICIENTS.md` から引用**し、再導出しない。
  2. 円換算は COEFFICIENTS の幅規約（1つに決め切れない量は幅で書き、下流は必ず下限を使う）に従う。**円で語れないものは無理に円にせず、出せない理由を書く。**
  3. 本書は調査のみ。`src/` は1行も変更していない。本番への書き込み・デプロイ・送信・env変更・フラグ点火は一切していない。
  4. C0（Y-0憲法・学校別偏差値/ボーダーの独自推定禁止）に抵触する脅威対応は提案していない。

---

## 読み方

各脅威は以下の7項目を持つ2列表で記述する。

| 項目 | 意味 |
|---|---|
| 症状 | 何が起きるか（起きた場合／起きている場合） |
| **早期警戒指標** | 何を見れば点灯が分かるか（具体的なクエリ・コマンド・閾値）★本ファイルの本体 |
| 発生確率 | 高/中/低 + 判断根拠（実測 or 「未検証」を明示） |
| 影響 | 円で出せる部分は出す／出せない部分は理由を書く |
| 予防 | 事前に打てる手 |
| 回復 | 起きた後の手順（ロールバック手順の有無を明記） |
| 人間ゲート(C7) | 👤必須か・不要か |

---

## 脅威1（TH-9）: 保護者到達率が動かない（最大の単一障害点）

北極星（memory `[[session-2026-07-04-oneshot-revenue-architecture]]`）は「唯一の桁レバーは C_p（保護者起点クリック）」と定義している。この経路が実質ゼロのまま推移することが、収益化全体の最大の単一障害点。

| 項目 | 内容 |
|---|---|
| **症状** | 生徒は結果画面（GapToTarget等）に到達するが、保護者共有ボタンを押しても、その先（保護者側のランディング・申込）まで到達する経路がほぼ発火しない |
| **早期警戒指標** | ①GA4 `share_to_parent` の日次件数（本書で実測・`mcp__ga4__ga4_run_report` dimensions=[date], eventName=share_to_parent, 2026-07-13〜08-10）: **2026-07-18に1件・2026-07-22に1件のみ。それ以降08-10まで19日間ゼロ**。②D1 `parent_funnel_events` の全件数（`node scripts/d1q.mjs "SELECT COUNT(*) FROM parent_funnel_events"`）: **全期間0件**（`ops/raw/BRIEF.md:98`）。**0→1への遷移そのものが最重要のシグナル**。③`activeParentWindow()`（`src/lib/parent-window.ts:25-31`）の窓は7/1〜7/25（mendan-july）と11/15〜12/25（winter）のみ点灯し、**7/26〜11/14の112日間（両端を含む）は保護者向け訴求モジュール自体が非表示になる設計**。この期間中に指標が動かないのは「未実装」ではなく「未点灯（意図的な季節限定）」の可能性があるため、11/15の窓オープン後も同様にゼロが続くかどうかで「機能不全」か「季節性」かを切り分けられる |
| **発生確率** | **高**（実測。将来のリスクでなく現在進行中の状態そのもの） |
| **影響** | **円は出せない**。理由: 保護者経路の `convRateLow`／CPAは全て未実測の仮置き（`ops/COEFFICIENTS.md` §4）で、実測イベントが0件のため掛け算する分子が無い。数えられる唯一の値: 到達イベント発生回数=0（全期間） |
| **予防** | 11/15の冬窓オープン時に、まず「ParentWindowBridge自体が表示（view相当）されているか」を確認してから「到達率が低い」と結論する（未点灯かどうかを機能不全より先に切り分ける） |
| **回復** | Cowork（実ブラウザ）で `/hyotei-heikin` 結果画面→共有ボタン押下→DevTools Networkで `POST /api/parent-funnel` の実応答（204でなく2xx）を確認し、直後にD1へ1件入るかを見る（`ops/STATE.json` 記載の verificationTask。memory `[[cowork-division-of-labor]]` の起動条件「loopが『不明/保留』と書いたもの」に該当） |
| **人間ゲート(C7)** | 検証タスクの発注文面作成まではloop可。実行はCowork（👤の裁量枠） |

---

## 脅威2（TH-10）: 無料相談のCV率が想定より低い（未検証の最大の不確実性）

| 項目 | 内容 |
|---|---|
| **症状** | アフィリクリックが発生しても、その先（ASP側の申込完了・確定）への転換率が `src/lib/affiliate-economics.ts` の仮置き `convRateLow`（0.005〜0.06、案件により異なる）より実際には低い可能性がある |
| **早期警戒指標** | `data/affiliate-actuals.json` の存在チェック（現状は**存在しない**＝`ops/PHASE0_FINDINGS.md` P-1）。3ASP（A8／もしも／アクセストレード）管理画面の**発生件数**（金額は後回しでよい）を👤が転記した瞬間に、この係数が仮置きから実測に置き換わる。指標としては「ファイルが存在するか」「発生件数>0か」の2値で十分（発生0件でも「convRateLowの上限が機械的に決まる」という情報価値がある） |
| **発生確率** | **未検証**（校正点が1件も存在しないため、確率自体を推定できないこと自体が問題の本体） |
| **影響** | **円は出せない**。理由: 校正点（P-1）が無いため、`convRateLow` を何%と仮定しても反証不能。MONEY/DISTANCE/BAR/DEADWIREの4文書が出した ¥7,199 / ¥4,440 / ¥3,300 / ¥2,943（月換算）は全てこの仮置き係数の産物で、実測1件で全部書き換わる（`ops/PHASE0_FINDINGS.md` 出血5） |
| **予防** | 受け皿スクリプト（`scripts/reconcile-clicks.ts`・`npm run ev:reconcile`）を使う準備をしておく（`ops/PHASE0_FINDINGS.md` E-5/E-6・未実行） |
| **回復** | 実測が入り次第、`ops/COEFFICIENTS.md` §4の仮置き値を置き換え、同ファイル末尾の更新チェックリストに従い下流5文書を見直す |
| **人間ゲート(C7)** | **必須**（ASP管理画面は👤のみ閲覧可） |

---

## 脅威3（TH-1）: 単一記事依存

| 項目 | 内容 |
|---|---|
| **症状** | `/blog/all-3-high-school-options-2026-update`（本体＋断片URL込み）がサイト全表示の約31%（95,817／28日窓の断片込み合計317,005・`ops/raw/BRIEF.md`§2）を占める。この1本の順位が落ちる、または評価が変わって圏外に出ると、表示ボリュームが構造的に痩せる |
| **早期警戒指標** | 該当URLの週次 position（`gsc_query` dimensions=[page], pageContains='all-3-high-school-options'）。現在 position **5.1**（`ops/raw/BRIEF.md`§2）。7位を超えたら黄、10位超えたら赤。あわせてCTR（現在1.84%＝自社実測5位カーブ6.7%〔`ops/COEFFICIENTS.md`§3〕の約1/4で、**既に劣化状態にある**ことに注意） |
| **発生確率** | 構造的集中は**高**（実測・断片込みで31%）。実際に順位崩壊が起きる確率は**未検証** |
| **影響** | 実クリックは648件／28日（サイト全体8,565件の7.6%）。`ops/COEFFICIENTS.md`§2の下限遷移率0.198%を機械的に適用すると **648×0.198%≈1.3件のアフィリクリック相当／28日**（上限0.537%でも3.5件）。EV/clickは案件依存でサイト単一値が存在しない（COEFFICIENTS§4）ため¥には変換しない。→ **換算可能な範囲では、この記事1本を失っても直接の換金インパクトは小さい（1〜4クリック/28日相当）**。真の実害は表示ボリューム（ブランド認知・内部リンクの起点）であり円では測れない |
| **予防** | 断片URL（目次アンカー）を「直す」施策はしない（`ops/raw/BRIEF.md`§G3で実害ゼロと確認済み・本体の順位を壊すリスクの方が大きい）。この記事だけに依存しない体制として、他の高表示面（`/hensachi`等）のCTR改善を優先する（BRIEF§Fで`/hensachi`は既にCTR改善傾向: 4.01%→5.61%） |
| **回復** | 順位低下はコード変更で戻せない＝**ロールバック手順なし**。順位低下を検知したら、まず `canEditRoute()` のガード（`data/ctr-improvement-log.json`・21日間隔）を確認し、直近に自分たちが触っていないかを先に切り分ける |
| **人間ゲート(C7)** | 不要（監視のみ） |

**2026-08-17追記(週次実測での再確認)**: `gsc_query`(dimensions=[page], 2026-08-10〜08-16の7日窓)で本体URLを
再確認したところ position **4.93**（改善）・CTR **0.84%**（上記1.84%よりさらに低いが、7日窓と28日窓は
比較対象が異なりノイズも大きいため単独では「悪化」と断定しない）。**同時にサイト全体の日次trend
(`gsc_query` dimensions=[date], 08-01〜08-14)を確認したところ、表示は4,332→9,420(2倍超)と順調に伸びる一方、
CTRは3.53%→1.92%へ一貫して低下、平均順位も6.1→7.9へ緩やかに悪化**（週次集計ではWoW 08-03〜09(順位6.91)→
08-10〜16(順位8.30)で1.39ポイント悪化・LOOP_CONTRACT§6の停止基準"7日で2位以上悪化"は未達)。
**現時点の解釈**: 本体URLのCTR/順位に単独の急落は無く、site全体の傾向も「新規/拡大中のページが
大量に露出し始めた結果、平均順位・CTRが一時的に薄まる」という既知パターン(`ops/LOOP_CONTRACT.md`§3-3c
「順位は露出とともに改善する・4週間の窓で判定」)と整合的。**緊急対応は不要と判断し監視継続とした**が、
1〜2週間後に同じWoW比較を行い、悪化が続く/加速するようなら質問ノートへ格上げすること。

---

## 脅威4（TH-2）: AI Overview / AI検索による計算機クエリの侵食

| 項目 | 内容 |
|---|---|
| **症状** | 「偏差値計算」等の頭語クエリがAIの回答内で完結し、クリックがAI回答エンジン経由に置き換わる。`/hensachi`（1,601クリック／28日＝収益面の中核）が直撃されるとサイトの柱が細る |
| **早期警戒指標** | ①**既存のトリップワイヤー③がリポジトリに実装済み**（`src/lib/velocity.ts:406-407,453,475-479`）: `AI_REFERRAL_SHARE_ALERT_THRESHOLD = 10`(%)。`evaluateTripwires()` に `aiReferralSharePercent` を渡すと自動判定し、`npm run kpi:weekly`（`scripts/weekly-kpi-report.ts`）経由で運用可能。**ただし `ops/PHASE0_FINDINGS.md` E-10指摘のとおり正準ツールとして定常運用されていない（未使用）**。②本書で実測（`mcp__ga4__ga4_run_report` dimensions=[sessionSource,eventName], eventName=ai_referral, 直近28日・2026-08-10取得）: `ai_referral` 合計31件の内訳は **bing 26／chatgpt.com 4／cn.bing.com 1**。同期間 sessions=1,047（同ツールで実測）。比率≈**2.96%**（31/1,047）＝閾値10%の約3割で、**現状はまだ黄信号ではない**。ただしGA4は実数を大幅に取りこぼす計器（memory `[[ga4-undercounts-conversions]]`）のため絶対数の信頼度は低い。分子分母が同一計測経路（GA4内)なので相対的な監視には使えるが、絶対値2.96%を「実際の割合」と読まないこと。③トリップワイヤー①（`/hensachi` CTR4週連続で閾値3.2%未満）も間接指標（COEFFICIENTS§3の実測CTR6.7%〔5位〕からの劣化を検知） |
| **発生確率** | **中**（業界トレンドとして進行中。自社への実害は現時点の比率〔2.96%<10%閾値〕では限定的＝未点灯） |
| **影響** | **円は出せない**。理由: AI Overviewが奪うのは「表示されたはずのクリック」で、反実仮想（奪われなかった場合の表示×CTR）の推定にはSERP上のAI Overview表示有無を区別するデータが必要だが、GSC APIにその次元は無い |
| **予防** | 週次でトリップワイヤー③を実際に回す運用に乗せる（**既存コードを使うだけ・実装コストは低い**） |
| **回復** | 閾値超過を検知したら、対象クエリのAI Overview表示有無を目視（WebSearch／実SERP）で確認し、影響クエリの一覧を作ってから対応を検討する（コンテンツ側の対策自体はPHASE1の別タスク） |
| **人間ゲート(C7)** | 不要（監視構築のみ）。週次の値渡し運用（GA4 MCP手動値渡し）はloopで完結可 |

---

## 脅威5（TH-3）: Google の手動対策・scaled content abuse 判定

| 項目 | 内容 |
|---|---|
| **症状** | ドメイン実質6か月（GSC起点2026-02下旬・`ops/raw/BRIEF.md`§B）で、学校ページ3,089枚を **2026-08-09 06:02 JST（commit `900fe01`）に47/47県一括解禁**したばかり。大量の類似構造ページの急速展開はGoogleの「scaled content abuse」判定パターンに構造的に類似しており、手動対策のリスクが理論上存在する。**⚠️ドメイン1年未満で手動対策を受けたら今季は消える**（`ops/raw/BRIEF.md`§B: このルールは2027年2月まで該当し続ける） |
| **早期警戒指標** | ①GSC「手動による対策」「セキュリティの問題」ページ（管理画面のみ・API取得不可）。**誰も1度も確認していない。本書執筆時点でも未確認＝本ファイルが指摘する最優先の空白（`ops/PHASE0_FINDINGS.md` P-4）**。②サイト全体のGSC週次クリック・表示トレンド。手動対策が入ると通常は数日〜1週間でクリックが顕著に落ちる。現在の日次推移（`ops/raw/BRIEF.md`§C）は順位6.2位前後で安定・CTR低下は面別要因で説明済み（BRIEF§F）なので、**現時点でこの兆候はない**。③本書で新規実測（`mcp__gsc__gsc_query` dimensions=[page], pageContains='/school/', 90日窓2026-05-09〜08-07 vs 28日窓）: **露出ページ数はどちらも233件で完全一致（rowCount 233）**。これは90日前（2026-05-09）時点ではまだ学校ページindex解禁がほぼ進んでいなかったことの裏付けで、現時点では「評価がまだ何も起きていない」ことを意味する（良くも悪くも） |
| **発生確率** | **中〜高**（構造的リスクパターンへの合致は実測。ただし実際の手動対策発生は未確認＝P-4が埋まっていないことそのものがリスク） |
| **影響** | **円は出せない**。理由: 手動対策はサイト全体のインデックス除外に至りうる壊滅的事象で、発生確率と被害規模の積が「未知×最大」になり意味のある期待値にならない。数えられるもの: 学校ページのGSC露出は現状233/3,089枚（7.5%・`ops/COEFFICIENTS.md`§6）、90日窓と28日窓で増えていない（停滞） |
| **予防** | 学校ページのindex解禁は既に県単位👤ゲートで慎重に運用してきた（`gate-decisions-2026-07-28`）。今後の新規大量展開（次の大規模ページ種別）を計画する前に、必ずP-4（手動対策ページ）を確認する運用を挟む |
| **回復** | ✅2026-08-17: `ops/ROLLBACK-SCHOOL-INDEX.md`にロールバック手順を整備した。切り替え機構自体は既に存在していた（`src/lib/school-page-lookup.ts`の`INDEXED_SCHOOL_PAGE_PREFECTURE_CODES`配列1箇所が`page.tsx`のrobotsと`sitemap.ts`双方の単一ソース）。手順書が無いことが本当の欠落だった。トリガー判断は引き続き👤専用（GSC管理画面のみ）・実行(配列編集→push)はloopが即座に代行できる |
| **人間ゲート(C7)** | **必須**（Search Console管理画面は👤のみ） |

---

## 脅威6（TH-4）: ASPプログラムの終了・条件変更

| 項目 | 内容 |
|---|---|
| **症状** | 主要ASP案件（`sora-juku-text`等）のプログラムが終了・条件変更されると、対応するhrefがデッドリンク化しEVがゼロになる。`sora-juku-text`は季節フォールバック先として複数面の既定オファーになっている（`src/lib/affiliates.ts:19,48-49`のコメント：「未approveの間はseasonalが自動で既存塾にフォールバックするのでデッドリンクは出ない」） |
| **早期警戒指標** | ①本書で実施（`curl -k --max-time 45 -A <iPhone UA> -L https://px.a8.net/svt/ejp?a8mat=4B3SN7+EDZ52Q+4YWU+5YJRM`）: **`sora-juku-text`のhrefは2026-08-10時点でhttp_code=200、1回のリダイレクトで正常遷移を確認**。✅2026-08-17: この確認を行う定期スクリプト`npm run check:affiliate-links`を新設した（`check:links` は一次ソースURL119本のみ対象で `affiliates.ts` のhrefはスコープ外だったため別スクリプトとした・詳細は下の「予防」欄）。②D1のこの案件へのクリックが急減した場合（前週比50%減等）も間接指標になる。ただし③の実測により、クリック数の増減だけでは終了/正常を判別できないことが判明している。③**本書の新規実測**（`node scripts/d1q.mjs`・D1ローリング28日・2026-08-10取得・`affiliate_id='sora-juku-text'`限定）: 85クリックのうち referer 内訳は `null` 84件・`internal_path`（人間と確証できる referer）**わずか1件**。`ops/COEFFICIENTS.md`§1の「下限＝内部パス付き」定義をこの案件だけに適用すると、**この案件が仮に今日終了しても、確証を持って失う人間のクリックは28日で1件**（サイト全体の下限17件のうち5.9%相当）という計算になる |
| **発生確率** | **低**（実測: href自体は生存）。✅2026-08-17: `npm run check:affiliate-links`の新設により「終了に気づくまでの遅延」リスクは月次実行を続ける限り縮小した（0件→デッドリンク検知まで最大1ヶ月のタイムラグに短縮。リアルタイム監視ではない点は残存） |
| **影響** | 出せる部分と出せない部分が混在。出せる部分: `sora-juku-text`のEV理論値は 0.04×0.6×¥10,000=**¥240/click**（`ops/COEFFICIENTS.md`§4、**理論値であり実測ではない**）。下限の人間クリック1件/28日を当てはめると**理論上¥240/28日相当（≒¥257/月）**がこの案件単体の見える範囲での上限。出せない部分: 84件のnull refererクリックが本当に全部botかは未証明（COEFFICIENTS§1不採用値注記のとおり、GA4は2026-08-10にaffiliate_click 1件を記録しその日のD1側は全てnull/root referer）なので、上限側は最大85件×¥240=**¥20,400/28日**にもなりうる幅がある |
| **予防** | ✅2026-08-17新設: `npm run check:affiliate-links`（`scripts/check-affiliate-links.mjs`）。affiliates.tsのlive34本(status≠'pending')のhrefを自動抽出しHEAD/GETで巡回、404/410/DNS不能のみ「壊れ」として`exit 1`（403等のbot弾き応答は到達確認済みとして警告止まり・`check-source-links.mjs`と同じ寛容な分類方針）。初回実行(2026-08-17)は34件中デッドリンク0件・警告3件（sapuri-*がHTTP403でbot弾きの可能性、到達自体は確認済み）。月次でこのコマンドを実行するだけで良い（会社ネットワークではTLS傍受で素のfetchが失敗することがあり、その場合は`NODE_TLS_REJECT_UNAUTHORIZED=0 npm run check:affiliate-links`で回避） |
| **回復** | href失効を検知したら、該当案件を `status: 'pending'` へ戻す（既存設計により `AffiliateAd` が自動的に描画を止め、季節フォールバック=既存塾へ自動切替＝デッドリンクが出ない、`affiliates.ts:49`） |
| **人間ゲート(C7)** | 不要（読み取りチェックのみ）。`status` 変更はコード変更のためデプロイは👤 |

---

## 脅威7（TH-5）: 誤った「全国統計」の配信 — ★既に発生・同日中に是正済み

| 項目 | 内容 |
|---|---|
| **症状（既に起きた）** | 2026-08-10、本番 `/api/stats/distribution`・`/api/stats/csv`・`/api/stats/percentile`・`/api/mcp`・`/stats`・`/report/2026` が**偏差値の全国平均を63.16として配信**（偏差値は定義上、母集団平均が50）。原因は「1件ずつは妥当域〔20〜90〕でも、集団としてあり得ない分布を素通しする」集計レベル不変条件の欠如（commit `ff7b6d6` メッセージ）。実測: hensachi 263件中**215件（81.7%）**が07-16/07-22/07-27/08-01/08-07の5日に集中し、偏差値70以上が263件中112件（43%）。⚠️**`ff7b6d6` のコミットメッセージにある「243件（92%）」は `date(created_at)`＝UTC日で集計した値。日本のサイトなのでJST日（`datetime(created_at,'+9 hours')`）が正であり、JST集計では 16+36+25+56+82＝215件。以後は215件（81.7%）を使う**（検算: `node scripts/d1q.mjs "SELECT substr(datetime(created_at,'+9 hours'),1,10) jd, COUNT(*) n FROM stats_submissions WHERE metric='hensachi' GROUP BY 1 ORDER BY 1"`）。汚染の事実そのものは両集計で変わらない |
| **時系列（是正）** | **即止め**: commit `ff7b6d6`（2026-08-10 **20:37:08** JST）。Layer A（`checkAggregateInvariants`、`src/lib/stats-aggregation.ts:133-141`＝標本平均が `meanCenter±meanTolerance`＝偏差値なら50±5を外れたら公開しない）とLayer B（`STATS_PUBLICATION`＝出所を検証できない指標を一旦withheld）の2層を追加。**根治**: commit `71dc270`（2026-08-10 **21:01:56** JST）＋ `0c711af`（**21:12:20** JST・名乗りの是正）。migration 0019で `trusted`／`trust_class` 列を追加し、既存319件は全て `trusted=0`（デフォルト）で自動的に集計から除外。`/api/stats/submit` に `bot-filter.ts` の `classifyClick` による送信元検査を追加（ボットUA→保存せず204、内部オリジン無し→保存はするが `trusted=0`、ブラウザUA+内部オリジン→`trusted=1`）。<br>⚠️**反映状態は時点で異なる。混同しないこと**:<br>・〜21:5x: `ff7b6d6` のみ本番反映。API応答の `sampleCount:263` と withheld文言は **`ff7b6d6` 版のハードコード**であり、`71dc270` の挙動ではない。**この時点で `curl` の `suppressedReason=provenance_unverified` を見ても「根治が反映された証拠」にはならない**（両版で同じ文字列が返るため区別できない）。critic はこの誤断定を検出した。<br>・21:5x: **本番D1へ migration 0019 を適用**（`wrangler d1 execute my-naishin-leads --remote --file=migrations/0019_...sql`）。適用後の検証: `trusted`／`trust_class` 列あり・**319件すべて `trusted=0`**・総数319（`backups/stats_submissions-prod-2026-08-10.json` と一致・欠損なし）。<br>・`71dc270`/`0c711af` は並行セッションの push に含まれて origin/main に到達済（`29c4a67` の祖先）。<br>**★教訓**: 「コードのcommit」「pushによるデプロイ」「D1 migrationの適用」は**3つ別の事象**であり、1つ確認しただけで「本番反映済み」と書いてはならない。migration未適用のまま `trusted` 列を参照するコードがデプロイされると、集計は空にフォールバックする一方で **`insertStatsSubmission` が毎回失敗し新規投稿が黙って捨てられる**（＝サイレントな計測停止）。**反映確認は必ず「スキーマ」「デプロイ済みコードの版が判別できる応答」の両方で行う** |
| **早期警戒指標（再発検知）** | ①週次で `node scripts/d1q.mjs "SELECT trust_class, COUNT(*), AVG(value) FROM stats_submissions WHERE metric='hensachi' GROUP BY 1"` を実行し、`trusted=1` の行数が `minSampleSize`(30)にどれだけ近づいているか・その平均が50±5に収まっているかを確認する。②`/api/stats/distribution?metric=hensachi` のレスポンスで `suppressedReason.code` が `provenance_unverified` から変わった（＝公開再開した）タイミングで、直後の `sampleCount` と `mean` を必ず目視確認する。③`total-score`（n=48・k閾値超過）が同一の配信状態か、**本書では確認していない**（`ops/PHASE0_FINDINGS.md` D-2は依然未実施＝次に手を付けるべき最小タスク） |
| **⚠️再発条件（重要・実装読解に基づく）** | Layer Bの出所検査は**referer（Origin相当）ベース**（`isInternalReferer`、`src/lib/bot-filter.ts:49`）であり、UA偽装＋refererヘッダ偽装（`https://my-naishin.com/hensachi` 等の内部パスを詐称）を組み合わせたスクリプトが送信すれば `trusted=1` として通過しうる。この経路をLayer Aが最終防波堤として担保する（標本平均が50±5を外れれば個別の出所判定に関わらず非公開のまま）。したがって**再発が起こりうるのは「trusted=1として偽装通過したデータが、それでも母平均50±5に収まるよう巧妙に細工された場合」に限られ、単純なbot連投（今回と同型の事故）では再発しない** |
| **発生確率** | **低**（2層ゲートが稼働中・実測確認済み）。ただし上記の理論的な再発条件が残る |
| **影響** | **円で語れない**（信頼毀損は逸失収益として計測経路が無い、DEADWIRE出血2の判定を継承）。数えられるもの: `percentile_view` 59回（2026-08-01〜08-10、GA4下限値）＝誤ったパーセンタイルが表示された下限回数 |
| **予防** | `total-score` のprovenance状態を確認する（D-2・未実施・次の空きコマで実行可能）。同型のエンドポイントを新設する際は出所検査とLayer A相当の不変条件を最初から設計に含める |
| **回復** | 手順は既に実行済み（上記2コミット）。ロールバック不要（前進的な修正のみ）。本番D1の行操作（過去データの個別復旧等）を将来行う場合は `scripts/backup-d1.ts` を先に走らせること（DEADWIREが未言及だった手順） |
| **人間ゲート(C7)** | 今回の2コミット自体はコード変更のみでデプロイは👤。将来の本番D1行操作は👤必須 |

---

## 脅威8（TH-6）: 競合が募集人員・出願者数を足してきた場合

| 項目 | 内容 |
|---|---|
| **症状** | 差-1（`ops/BAR.md:271-278`）の優位性（募集人員・出願者数の実数を47県×3〜6年度・17,142レコード保有）は、進研ゼミが商用二次ソース（育伸社）から比率データのみ購入する設計を続ける限り成立する。育伸社（または別の二次データ業者）が分子分母データも商品化し、進研ゼミ等が契約を切り替えれば、この差は埋まりうる |
| **早期警戒指標** | 進研ゼミ（`czemi.benesse.ne.jp`）・高校偏差値.net・minkouの該当ページを四半期に1回WebFetch/curlで再取得し、「募集人員」「出願者数」等の実数語がページ内に出現するようになっていないかを `grep` で確認する（`ops/BAR.md` の2026-08-02実測との差分監視。`czemi.html` 本文の「※倍率数値は株式会社育伸社の資料を掲載しています」という現行の注記が変わっていないかも合わせて見る） |
| **発生確率** | **低**（構造的障壁が高い＝BAR自身が「県ごとの判断を伴う転記労働は購入では代替しにくい」と分析済み〔`ops/BAR.md:277`〕。ただし契約変更の可能性自体はBAR V-4〔`ops/BAR.md:434`〕で「検証不能」と明記されており排除できない） |
| **影響** | **円は出せない**。理由: 検索順位への影響は「差がなくなった場合に何位まで落ちるか」という反実仮想であり、BAR自身が比較した4サイトのSERP実測（WebSearch経由・実SERPではない）からは順位変動の関数が導けない |
| **予防** | 差-2（出典明示・自己拘束）・差-3（計算エンジン統合）・差-4（構造化データ）・差-6（表記ゆれ吸収）等、募集人員・出願者数以外の複数の差を並行して強めておく（単一の差に依存しない設計は既に一部実現済み） |
| **回復** | 差-1が埋まったことを検知した場合の**ロールバック手順は無い**（競合の行動は自社の変更で戻せない）。差-5（公表当日更新体制）への重心移行が対応の骨子になる（`ops/BAR.md:305-312` で既に整理済み） |
| **人間ゲート(C7)** | 不要（定点観測のみ） |

---

## 脅威9（TH-7）: 学校ページ3,089枚の低品質判定

| 項目 | 内容 |
|---|---|
| **症状** | 28日で露出233枚・表示605・クリック7（`ops/raw/BRIEF.md`§2）。残り2,856枚（3,089−233）のインデックス状態が不明（`ops/PHASE0_FINDINGS.md` P-3）。本書の90日再取得でも露出ページはやはり233枚で不変。ページ数に対して露出比率が低いまま（7.5%）推移すると、Googleが「大量の低利用ページ」として品質シグナルを下げるリスクがある |
| **早期警戒指標** | ①月次で `mcp__gsc__gsc_query(dimensions=[page], pageContains='/school/')` の `rowCount` を追跡する。90日窓(2026-05-09〜08-07)と28日窓(2026-07-10〜08-07)のいずれも rowCount=233 で完全一致していた（停滞の根拠）。**✅2026-08-18更新: 233の停滞は解消した**。7日窓(08-09〜08-15)・28日窓(07-19〜08-15)ともAPI上限の`rowCount=1000`に到達（実数は1000以上・実測ツールの上限が律速で真の枚数は不明）。同28日窓の集計値は`clicks=402・impressions=29085`（旧基準のクリック7・表示605から57倍・48倍）。T-A1(学校ページ多年度化・`9da80cc`〜`16ce43b`)が08-12前後に反映された時期と符合する。233から動かない状態は解消したため、この指標単体では黄信号ではなくなった。②GSC「ページのインデックス登録」レポート（`ops/PHASE0_FINDINGS.md` P-3、👤の管理画面のみ）。**未確認のまま。冬の収穫量を左右する最重要の未知数**（`ops/STATE.json` 自身の表現）。③学校ページ経由のD1 `school_page_clicks`（**✅2026-08-18実測: 9件→55件**（reverse/juku-shindan内訳は未再集計）。増加トレンドが冬まで完全にゼロなら「季節性」ではなく「品質評価」の可能性を疑う根拠になる、としていたが55件への増加により「完全にゼロ」の懸念は当面後退 |
| **発生確率** | **低〜中に下方修正（2026-08-18）**（実測: 露出233枚での停滞は解消し表示・クリックとも大幅増加を確認。ただし「低品質判定」自体の直接証拠(GSC手動対策・ページのインデックス登録レポート)は👤未確認のままで、露出増加そのものが品質シグナル改善を保証するものではない点に注意） |
| **影響** | **円は出せない**。理由: 「低品質判定」を受けた場合の影響は個別ページのnoindex化からドメイン全体のシグナル低下まで幅があり、単一の期待値に落とせない。数えられるもの: 現状の未収穫在庫2,856枚、直近28日のクリック7件（1ページあたり0.0023件） |
| **予防** | 新規の大量ページ種別展開は行わず（建設フェーズ終端8/31が近い）、既存3,089枚の品質（内部リンク・コンテンツ充実度）を見直す方を優先する判断がありうる |
| **回復** | ✅2026-08-17: `ops/ROLLBACK-SCHOOL-INDEX.md`（脅威5対応で新設）の「部分ロールバック」節がそのまま使える。`INDEXED_SCHOOL_PAGE_PREFECTURE_CODES`のコメントにwave1〜wave7の県コード内訳が記録されているため、特定waveだけをnoindexへ戻すことも同じ手順で可能 |
| **人間ゲート(C7)** | P-3の確認は👤必須 |

---

## 脅威10（TH-8）: Workers / D1 の容量・コスト

| 項目 | 内容 |
|---|---|
| **症状** | 学校ページ3,089枚＋今後の学校ページクリック増加により、D1の書き込み量やWorkersのビルドサイズが上限に近づく可能性 |
| **早期警戒指標** | 本書で実測（`node scripts/d1q.mjs`、2026-08-10）: 全テーブル合計行数は clicks 632・stats_submissions 319・leads 6・school_page_clicks 9・parent_funnel_events 0・email_events 27・push_subscriptions 0・api_usage 0 ＝**約993行**。直近1日のclicks増分は17件（`WHERE date(created_at) >= date('now','-1 day')`）。この規模は現行のWorkers Paidプラン（$5/月・バンドル10MB上限、2026-07-11アップグレード・memory `[[fable5-loop-protocol]]`）の想定内で、**当面の実害リスクは低い** |
| **発生確率** | **低**（現状の書き込み量は極小）。ただし脅威7（TH-5）のような「出所未検査のエンドポイントへのbot連投」が再発すれば、書き込み量が短期間で跳ねる条件付きリスクがある（実際に2026-08-01/08-07に発生した実績があり、`stats_submissions` は最大82件/日に達した） |
| **影響** | **円は出せない**。理由: Cloudflare D1/Workersの現行の具体的な課金上限値（read/write ops等）は本セッションで確認していない（Cloudflareダッシュボードの領域・👤のみ閲覧可） |
| **予防** | 新規の書き込みエンドポイントを追加する際は、脅威7の教訓（出所検査・レート制限）を最初から組み込む設計にする |
| **回復** | 容量超過はプラン変更やデータ整理で対応可能な性質のため、緊急ロールバックの対象ではない |
| **人間ゲート(C7)** | Cloudflareダッシュボードでの現行使用量確認は👤（本書では未実施） |

---

## 脅威11（TH-11）: 収益導線の破壊 — 生徒を収益の薄いページへ移す事故

| 項目 | 内容 |
|---|---|
| **症状** | 学校ページ等への新しい導線を追加する改修が、意図せず既存の保護者共有導線（`GapToTarget`・`ParentCostBridge`）より上や同格に配置され、生徒を収益の薄いページへ先に誘導してしまう。`src/components/ResultSection.tsx:295-298` に「保護者共有導線（橋①GapToTarget・橋②送るボタン・ParentCostBridge）より必ず下に置くこと（収益の主導線を押し下げない）」というプロジェクト自身の規約が明記されている |
| **早期警戒指標** | ✅2026-08-17: `src/components/__tests__/ResultSection-cta-order.test.ts` を新設し、`ResultSection.tsx`自身の規約（`<GapToTarget`→`<ParentCostBridge`→学校ページ導線Linkの順）をソース文字位置比較で自動検証するようにした（`src/app/__tests__/school-page-cta-order.test.ts`と同型の技法・ただしそちらは別ファイルのG7規約用で本規約はカバーしていなかった）。他14件の同型ResultFlowコンポーネント（2026-07-24のZZ-2d監査で確認済み）は、調査の結果2026-08-17時点でParentCostBridgeと競合する学校ページ導線的な要素が存在せず追加の順序チェック対象が無いため未着手。将来これらに同種の導線が追加された際は同じ技法でテストを追加すること |
| **発生確率** | **中**（自動的な歯止めが無い＝いつでも壊れうる構造的脆弱性。ただし過去に実際に違反した事故の記録はまだ見つかっていない） |
| **影響** | **円は出せない**。理由: 「生徒が収益の薄いページへ流出した場合の逸失額」は、流出しなかった場合の到達数との反実仮想が必要で、A/B実験なしには推定できない |
| **予防** | ✅2026-08-17: `ResultSection.tsx`は上記テストが自動で歯止めになる（jestが落ちるため気づかず崩す事故を防げる）。他14件は引き続き目視確認の運用ルール（新しい導線コンポーネントをResultFlow系に追加する全てのコミットで、追加位置がParentCostBridge等より下であることをコミット前に目視確認する）を徹底する |
| **回復** | 順序違反を発見したら、該当コンポーネントの配置を`GapToTarget`/`ParentCostBridge`の下へ戻すだけの単純なコード修正（git revertまたは並べ替え）。**ロールバック自体は容易** |
| **人間ゲート(C7)** | 不要（コードレビュー相当） |

---

## 脅威12（TH-12）: ピーク直前の変更で順位を落とす

| 項目 | 内容 |
|---|---|
| **症状** | ピークフリーズ期間（2026-11-15〜2027-02-28、`ops/raw/BRIEF.md`§7）の直前や、フリーズ中の「データ追記」で構造・URL・大規模リライトに該当する変更を誤って行い、冬の収穫期（2月にS値計算11.7倍等）に順位が不安定化する |
| **早期警戒指標** | ①`data/ctr-improvement-log.json` の `canEditRoute()` ガード（21日間隔）を、フリーズ直前（11月上旬）に触れた面が11/15以降も測定ウィンドウを汚染していないか確認する。②ピークフリーズ中の「データ追記の可否」が **BAR V-7（`ops/BAR.md:437`）の時点で未裁定のまま**。これが未裁定だと、差-5（公表当日更新）という自社の武器が使えるかどうかが個人の判断に委ねられ、フリーズ規約の趣旨（構造・URL・大規模リライト禁止）に抵触するデータ追記が「これはデータ追記だから大丈夫」という自己判断で行われるリスクがある。③学校ページの47/47完走（2026-08-09）のような大規模一括変更が、フリーズ直前（11月上旬）に再び起きないよう監視する。建設フェーズ終端（8/31）以降の扱いが「誰の担当でもない」と `ops/PHASE0_FINDINGS.md` CL-4が指摘している空白がそのまま11月の駆け込み変更リスクに直結する |
| **発生確率** | **中**（実績: 学校ページ一括解禁が建設フェーズ終盤〔8/9〕に発生した前例がある。11月の判断も同種の「終わらせたい」圧力で起きうる） |
| **影響** | **円は出せない**。理由: 順位変動と季節ピーク（2月に11.7倍、`ops/raw/BRIEF.md`§7）が重なった場合の逸失額は、変動幅そのものが未知数のため試算不能 |
| **予防** | CL-3（BAR V-7）をピークフリーズ開始（11/15）前に👤へ確認し、データ追記の可否を明文化しておく。11月上旬の駆け込み大規模変更を避ける |
| **回復** | フリーズ中に問題が起きた場合、直前のgit revertで構造変更を戻すこと自体は可能だが、**検索順位はコードのロールバックでは即座に戻らない**（再クロール・再評価のラグがある。memory `[[gsc-read-lag]]` と同型のリスク） |
| **人間ゲート(C7)** | CL-3の裁定は👤必須 |

---

## 脅威13（TH-13）: `clicks`テーブルのreferer内部パス偽装によるアフィリクリック不正水増し（2026-08-20実測で発見）

| 項目 | 内容 |
|---|---|
| **症状** | `LOOP_CONTRACT.md`§3-2が定める「人間クリックの唯一の正しい数え方」（`referer LIKE 'https://my-naishin.com/_%'`）は、`stats_submissions`のtrusted判定と同じくreferer(Origin相当)ベースの検査でしかない（`src/lib/bot-filter.ts`の`isInternalReferer`と同型の弱点。TH-7/事故時系列の「⚠️再発条件」欄で予告されていた「UA偽装+refererヘッダ偽装の組み合わせ」が、statsテーブルではなく`clicks`テーブルで実際に発生したとみられる）。2026-08-20のT-R1週次W-2実施時、直近7日クリック850件のうち829件(97.5%)が2026-08-13〜15の3日間に集中していると判明。この829件を精査したところ: ①47都道府県のprefectureページほぼ全てに9〜13件と**不自然に均一**に分散(実際の人口・検索ボリューム分布とは無関係な平坦さ) ②distinct ip_hash 813 / 総クリック829(≒1クリック1IP=IPローテーション型) ③distinct user_agent はわずか**8種類**(すべてWindows/Mac・Chrome/Edgeの最新版のみ・モバイル皆無・Safari/Firefox皆無)で、各UAの出現数が85〜121件と**これも不自然に均一** ④時間帯は0〜23時に幅広く分散(人間の活動パターンを模倣し時間ベースの検知を回避する典型的パターン) |
| **早期警戒指標** | 週次W-2で「7日間クリック数」が前週比で急増(2〜3倍以上)した回に、`GROUP BY date(created_at)`で日次分布を見て特定の2〜3日に集中していないか確認する。集中していれば`GROUP BY user_agent`/`GROUP BY ip_hash`でdistinct数と最頻値の均一性を追加確認する（distinct ip_hash≒総件数かつdistinct user_agentが一桁で各UAの出現数がほぼ均等、という組み合わせが今回のシグネチャ）。 |
| **発生確率** | **実績あり（今回で確定）**。TH-7の事故対応時点で理論上の弱点として指摘されていた経路が、3日間・829件規模で実際に突かれた。再発条件そのものが解消されていない（Layer B相当の「もう一段の検査」が`clicks`テーブル側には無い）ため、再発確率は**高**と見るべき |
| **影響** | **円は出せない**（このクリックが実際にASP側でどう扱われたか＝成果承認/否認の実測が無い）。ただし構造的リスクとして: このサイトは複数のアフィリエイトASP(A8等)と提携しており、**もしこの829件が実際にASPのトラッキングリンクを踏んでいた場合、不正クリックとしてASP側の不正検知に引っかかりプログラム停止・アカウント審査に発展するリスクがある**（TH-6「ASPプログラムの終了・条件変更」と連動しうる）。またW-2の「人間クリック」KPIそのものが今回の3日間は信頼できない（水増しされている）ため、この期間のKPIをそのまま実績として引用しないこと |
| **予防** | ✅2026-08-20: `npm run check:click-fraud`（`scripts/check-click-fraud-burst.mjs`）を新設。過去N日(既定14)の`clicks`を日別に集計し、①1日の総クリック50件以上 ②distinct ip_hash/総クリック比0.85以上(IPローテーション型) ③distinct user_agentが12種類以下、を**同時に満たす日**を検知して`exit 1`する（TH-13実測の08-13〜15を正しく検知・他の日は0件で誤検知なしを確認済み）。**これはあくまで検知のみ**（読み取り専用・D1書き換えは行わない）。W-2(週次)の一部として毎回このコマンドも実行することを推奨。まだ運用ルール(T-R1タスクファイル)への正式な組み込みとASPへの申告要否の判断は👤待ち |
| **回復** | 該当期間(2026-08-13〜15)の829件を「人間クリック」実績から除外して集計し直すことは可能(SQLで期間指定除外)だが、**過去に遡ってD1のデータを書き換える操作はLOOP_CONTRACT§2の禁止事項（本番D1の破壊的操作）に触れる可能性があるためloop単独では実施しない**。集計時に期間を除外して参照する運用（コード側で吸収）を検討 |
| **人間ゲート(C7)** | ①の異常検知ロジック追加は👤確認推奨。ASPへの申告・問い合わせが必要か否かの判断も👤 |

---

## 今すぐ計器を付けるべき指標 上位5（実装コスト順）

| 順位 | 指標 | 対応する脅威 | 実装コスト | 実行者 |
|---|---|---|---|---|
| 1 | **GSC「手動による対策」「セキュリティの問題」ページの確認** | 脅威5(TH-3) | **最低**（Search Console管理画面を開いて見るだけ。コード・クエリ不要） | 👤 |
| 2 | ✅完了済み（2026-08-17に本表を実態へ更新）: `stats_submissions` の `trust_class`別週次集計SQLは`ops/tasks/T-R1-recurring-checks.md`のW-1として週次チェックリストに組み込み済み | 脅威7(TH-5) | 完了。以後はT-R1週次確認のタイミングで実行するだけ | loop |
| 3 | ✅完了済み（2026-08-17に本表を実態へ更新）: `evaluateTripwires()`（`src/lib/velocity.ts`）は`src/lib/weekly-kpi-report.ts`の`formatWeeklyKpiEmail()`内で既に呼び出され、`npm run kpi:weekly`の週次メール件名に警報件数として反映済み | 脅威4(TH-2) | 完了 | loop |
| 4 | ✅2026-08-17完了: `affiliates.ts` のlive 34本hrefを巡回するops側チェックスクリプト（`npm run check:affiliate-links`） | 脅威6(TH-4) | 完了。以後は月次実行するだけ | loop（実装済）→👤（月次実行） |
| 5 | **`data/affiliate-actuals.json` へのASP発生件数の転記** | 脅威2(TH-10)・脅威3(TH-1)ほか全ての円換算試算の校正点 | 技術コストはゼロだが👤の手作業（3ASP管理画面）が必須。**価値は最大**（これが埋まるまで、本書を含む全ての¥試算は仮置き係数のままであり続ける） | 👤 |

---

## 本書の限界（正直に書く）

- 脅威5（TH-3・手動対策）と脅威9（TH-7・学校ページ低品質判定）は、**PHASE 0 が一貫して指摘してきた「GSC管理画面（P-3・P-4）が未確認のまま」という空白が本書でも埋まっていない**。本書はAPI経由で取得可能な代理指標（page次元の90日窓等）までは前進させたが、決定的な確認は👤ゲートのまま残っている。
- 円換算を試みた脅威（TH-1・TH-4）でも、EV/clickがサイト単一値として存在しない（`ops/COEFFICIENTS.md`§4）という制約のため、理論値ベースの幅でしか示せていない。脅威2（TH-10）が解消されない限り、この制約は本書だけでなく全てのPHASE 1/2文書に及ぶ。
- 12脅威のうち円換算を出せたのは脅威6（TH-4・ASP終了）1件のみで、それも理論値（¥240/click〔サイト自身の仮置き係数〕）に基づく幅（¥257/月〜¥20,400/28日）であり、実測ではない。

本書で本番への書き込み・デプロイ・env変更・メール送信・フラグ点火は一切行っていない。`src/` は1行も変更していない。
