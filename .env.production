# 本番ビルド時にだけ読まれる公開設定（Next.js が next build 時に読み込む・秘密情報は絶対に書かない）
# 2026-09-24 T-ADS1: 本文中のAdSense手動ユニット（src/lib/ad-units.ts）の点火スイッチ。
# AdSlot は client コンポーネントで、この値はビルド時に埋め込まれるため wrangler.jsonc の vars では効かない（loop確認済み）。
# 戻すときはこの行を削除して push する。
NEXT_PUBLIC_ADSENSE_ENABLED=1
